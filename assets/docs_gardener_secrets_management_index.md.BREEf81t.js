import{_ as t,c as i,o as s,a2 as a}from"./chunks/framework.Bfq10Vlj.js";const g=JSON.parse('{"title":"Secrets Management","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/development","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/secrets_management.md","to":"secrets_management.md"},"persona":"Developers","title":"Secrets Management","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/secrets_management/index.md","filePath":"docs/gardener/secrets_management.md","lastUpdated":null}'),n={name:"docs/gardener/secrets_management/index.md"};function r(o,e,c,l,d,h){return s(),i("div",null,e[0]||(e[0]=[a(`<h1 id="secrets-management-for-seed-and-shoot-cluster" tabindex="-1">Secrets Management for Seed and Shoot Cluster <a class="header-anchor" href="#secrets-management-for-seed-and-shoot-cluster" aria-label="Permalink to &quot;Secrets Management for Seed and Shoot Cluster&quot;">​</a></h1><p>The gardenlet needs to create quite some amount of credentials (certificates, private keys, passwords) for seed and shoot clusters in order to ensure secure deployments. Such credentials typically should be renewed automatically when their validity expires, rotated regularly, and they potentially need to be persisted such that they don&#39;t get lost in case of a control plane migration or a lost seed cluster.</p><h2 id="secretsmanager-introduction" tabindex="-1">SecretsManager Introduction <a class="header-anchor" href="#secretsmanager-introduction" aria-label="Permalink to &quot;SecretsManager Introduction&quot;">​</a></h2><p>These requirements can be covered by using the <code>SecretsManager</code> package maintained in <a href="https://github.com/gardener/gardener/tree/master/pkg/utils/secrets/manager" target="_blank" rel="noreferrer"><code>pkg/utils/secrets/manager</code></a>. It is built on top of the <code>ConfigInterface</code> and <code>DataInterface</code> interfaces part of <a href="https://github.com/gardener/gardener/tree/master/pkg/utils/secrets" target="_blank" rel="noreferrer"><code>pkg/utils/secrets</code></a> and provides the following functions:</p><ul><li><p><code>Generate(context.Context, secrets.ConfigInterface, ...GenerateOption) (*corev1.Secret, error)</code></p><p>This method either retrieves the current secret for the given configuration or it (re)generates it in case the configuration changed, the signing CA changed (for certificate secrets), or when proactive rotation was triggered. If the configuration describes a certificate authority secret then this method automatically generates a bundle secret containing the current and potentially the old certificate. Available <code>GenerateOption</code>s:</p><ul><li><code>SignedByCA(string, ...SignedByCAOption)</code>: This is only valid for certificate secrets and automatically retrieves the correct certificate authority in order to sign the provided server or client certificate. <ul><li>There are two <code>SignedByCAOption</code>s: <ul><li><code>UseCurrentCA</code>. This option will sign server certificates with the new/current CA in case of a CA rotation. For more information, please refer to the <a href="/docs/gardener/secrets_management/#certificate-signing">&quot;Certificate Signing&quot;</a> section below.</li><li><code>UseOldCA</code>. This option will sign client certificates with the old CA in case of a CA rotation. For more information, please refer to the <a href="/docs/gardener/secrets_management/#certificate-signing">&quot;Certificate Signing&quot;</a> section below.</li></ul></li></ul></li><li><code>Persist()</code>: This marks the secret such that it gets persisted in the <code>ShootState</code> resource in the garden cluster. Consequently, it should only be used for secrets related to a shoot cluster.</li><li><code>Rotate(rotationStrategy)</code>: This specifies the strategy in case this secret is to be rotated or regenerated (either <code>InPlace</code> which immediately forgets about the old secret, or <code>KeepOld</code> which keeps the old secret in the system).</li><li><code>IgnoreOldSecrets()</code>: This specifies that old secrets should not be considered and loaded (contrary to the default behavior). It should be used when old secrets are no longer important and can be &quot;forgotten&quot; (e.g. in <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/18-shoot-CA-rotation.md#rotation-sequence-for-cluster-and-client-ca" target="_blank" rel="noreferrer">&quot;phase 2&quot; (<code>t2</code>) of the CA certificate rotation</a>). Such old secrets will be deleted on <code>Cleanup()</code>.</li><li><code>IgnoreOldSecretsAfter(time.Duration)</code>: This specifies that old secrets should not be considered and loaded once a given duration after rotation has passed. It can be used to clean up old secrets after automatic rotation (e.g. the Seed cluster CA is automatically rotated when its validity will soon end and the old CA will be cleaned up 24 hours after triggering the rotation).</li><li><code>Validity(time.Duration)</code>: This specifies how long the secret should be valid. For certificate secret configurations, the manager will automatically deduce this information from the generated certificate.</li><li><code>RenewAfterValidityPercentage(int)</code>: This specifies the percentage of validity for renewal. The secret will be renewed based on whichever comes first: The specified percentage of validity or 10 days before end of validity. If not specified, the default percentage is <code>80</code>.</li></ul></li><li><p><code>Get(string, ...GetOption) (*corev1.Secret, bool)</code></p><p>This method retrieves the current secret for the given name. In case the secret in question is a certificate authority secret then it retrieves the bundle secret by default. It is important that this method only knows about secrets for which there were prior <code>Generate</code> calls. Available <code>GetOption</code>s:</p><ul><li><code>Bundle</code> (default): This retrieves the bundle secret.</li><li><code>Current</code>: This retrieves the current secret.</li><li><code>Old</code>: This retrieves the old secret.</li></ul></li><li><p><code>Cleanup(context.Context) error</code></p><p>This method deletes secrets which are no longer required. No longer required secrets are those still existing in the system which weren&#39;t detected by prior <code>Generate</code> calls. Consequently, only call <code>Cleanup</code> after you have executed <code>Generate</code> calls for all desired secrets.</p></li></ul><p>Some exemplary usages would look as follows:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">secret, err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> k.secretsManager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Generate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ctx,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    &amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">secrets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CertificateSecretConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Name:                        </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my-server-secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        CommonName:                  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;server-abc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        DNSNames:                    []</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;first-name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;second-name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        CertType:                    secrets.ServerCert,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        SkipPublishingCACertificate: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    secretsmanager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">SignedByCA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my-ca&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    secretsmanager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Persist</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    secretsmanager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Rotate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(secretsmanager.InPlace),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> nil</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>As explained above, the caller does not need to care about the renewal, rotation or the persistence of this secret - all of these concerns are handled by the secrets manager. Automatic renewal of secrets happens when their validity approaches 80% or less than <code>10d</code> are left until expiration.</p><p>In case a CA certificate is needed by some component, then it can be retrieved as follows:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">caSecret, found </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> k.secretsManager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;my-ca&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> !</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">found {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fmt.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Errorf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret my-ca not found&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>As explained above, this returns the bundle secret for the CA <code>my-ca</code> which might potentially contain both the current and the old CA (in case of rotation/regeneration).</p><h3 id="certificate-signing" tabindex="-1">Certificate Signing <a class="header-anchor" href="#certificate-signing" aria-label="Permalink to &quot;Certificate Signing&quot;">​</a></h3><h4 id="default-behaviour" tabindex="-1">Default Behaviour <a class="header-anchor" href="#default-behaviour" aria-label="Permalink to &quot;Default Behaviour&quot;">​</a></h4><p>By default, client certificates are signed by the current CA while server certificate are signed by the old CA (if it exists). This is to ensure a smooth exchange of certificate during a CA rotation (typically has two phases, ref <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/18-shoot-CA-rotation.md#rotation-sequence-for-cluster-and-client-ca" target="_blank" rel="noreferrer">GEP-18</a>):</p><ul><li>Client certificates: <ul><li>In phase 1, clients get new certificates as soon as possible to ensure that all clients have been adapted before phase 2.</li><li>In phase 2, the respective server drops accepting certificates signed by the old CA.</li></ul></li><li>Server certificates: <ul><li>In phase 1, servers still use their old/existing certificates to allow clients to update their CA bundle used for verification of the servers&#39; certificates.</li><li>In phase 2, the old CA is dropped, hence servers need to get a certificate signed by the new/current CA. At this point in time, clients have already adapted their CA bundles.</li></ul></li></ul><h4 id="alternative-sign-server-certificates-with-current-ca" tabindex="-1">Alternative: Sign Server Certificates with Current CA <a class="header-anchor" href="#alternative-sign-server-certificates-with-current-ca" aria-label="Permalink to &quot;Alternative: Sign Server Certificates with Current CA&quot;">​</a></h4><p>In case you control all clients and update them at the same time as the server, it is possible to make the secrets manager generate even server certificates with the new/current CA. This can help to prevent certificate mismatches when the CA bundle is already exchanged while the server still serves with a certificate signed by a CA no longer part of the bundle.</p><p>Let&#39;s consider the two following examples:</p><ol><li><code>gardenlet</code> deploys a webhook server (<code>gardener-resource-manager</code>) and a corresponding <code>MutatingWebhookConfiguration</code> at the same time. In this case, the server certificate should be generated with the new/current CA to avoid above mentioned certificate mismatches during a CA rotation.</li><li><code>gardenlet</code> deploys a server (<code>etcd</code>) in one step, and a client (<code>kube-apiserver</code>) in a subsequent step. In this case, the default behaviour should apply (server certificate should be signed by old/existing CA).</li></ol><h4 id="alternative-sign-client-certificate-with-old-ca" tabindex="-1">Alternative: Sign Client Certificate with Old CA <a class="header-anchor" href="#alternative-sign-client-certificate-with-old-ca" aria-label="Permalink to &quot;Alternative: Sign Client Certificate with Old CA&quot;">​</a></h4><p>In the unusual case where the client is deployed before the server, it might be useful to always use the old CA for signing the client&#39;s certificate. This can help to prevent certificate mismatches when the client already gets a new certificate while the server still only accepts certificates signed by the old CA.</p><p>Let&#39;s consider the two following examples:</p><ol><li><code>gardenlet</code> deploys the <code>kube-apiserver</code> before the <code>kubelet</code>. However, the <code>kube-apiserver</code> has a client certificate signed by the <code>ca-kubelet</code> in order to communicate with it (e.g., when retrieving logs or forwarding ports). In this case, the client certificate should be generated with the old CA to avoid above mentioned certificate mismatches during a CA rotation.</li><li><code>gardenlet</code> deploys a server (<code>etcd</code>) in one step, and a client (<code>kube-apiserver</code>) in a subsequent step. In this case, the default behaviour should apply (client certificate should be signed by new/current CA).</li></ol><h2 id="reusing-the-secretsmanager-in-other-components" tabindex="-1">Reusing the SecretsManager in Other Components <a class="header-anchor" href="#reusing-the-secretsmanager-in-other-components" aria-label="Permalink to &quot;Reusing the SecretsManager in Other Components&quot;">​</a></h2><p>While the <code>SecretsManager</code> is primarily used by gardenlet, it can be reused by other components (e.g. extensions) as well for managing secrets that are specific to the component or extension. For example, provider extensions might use their own <code>SecretsManager</code> instance for managing the serving certificate of <code>cloud-controller-manager</code>.</p><p>External components that want to reuse the <code>SecretsManager</code> should consider the following aspects:</p><ul><li>On initialization of a <code>SecretsManager</code>, pass an <code>identity</code> specific to the component and controller. For example, gardenlet&#39;s shoot controller uses <code>gardenlet</code> as the <code>SecretsManager</code>&#39;s identity, the <code>Worker</code> controller in <code>provider-foo</code> should use <code>provider-foo-worker</code>, and the <code>ControlPlane</code> controller should use <code>provider-foo-controlplane</code> for <code>ControlPlane</code> objects. The given identity is added as a value for the <code>manager-identity</code> label on managed <code>Secret</code>s. This label is used by the <code>Cleanup</code> function to select only those <code>Secret</code>s that are actually managed by the particular <code>SecretManager</code> instance. This is done to prevent removing still needed <code>Secret</code>s that are managed by other instances.</li><li>Generate dedicated CAs for signing certificates instead of depending on CAs managed by gardenlet.</li><li>Names of <code>Secret</code>s managed by external <code>SecretsManager</code> instances must not conflict with <code>Secret</code> names from other instances (e.g. gardenlet).</li><li>For CAs that should be rotated in lock-step with the Shoot CAs managed by gardenlet, components need to pass information about the last rotation initiation time and the current rotation phase to the <code>SecretsManager</code> upon initialization. The relevant information can be retrieved from the <code>Cluster</code> resource under <code>.spec.shoot.status.credentials.rotation.certificateAuthorities</code>.</li><li>Independent of the specific identity, secrets marked with the <code>Persist</code> option are automatically saved in the <code>ShootState</code> resource by the gardenlet and are also restored by the gardenlet on Control Plane Migration to the new Seed.</li></ul><h2 id="migrating-existing-secrets-to-secretsmanager" tabindex="-1">Migrating Existing Secrets To SecretsManager <a class="header-anchor" href="#migrating-existing-secrets-to-secretsmanager" aria-label="Permalink to &quot;Migrating Existing Secrets To SecretsManager&quot;">​</a></h2><p>If you already have existing secrets which were not created with <code>SecretsManager</code>, then you can (optionally) migrate them by labeling them with <code>secrets-manager-use-data-for-name=&lt;config-name&gt;</code>. For example, if your <code>SecretsManager</code> generates a <code>CertificateConfigSecret</code> with name <code>foo</code> like this</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">secret, err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> k.secretsManager.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Generate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ctx,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    &amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">secrets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CertificateSecretConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Name:                        </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;foo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>and you already have an existing secret in your system whose data should be kept instead of regenerated, then labeling it with <code>secrets-manager-use-data-for-name=foo</code> will instruct <code>SecretsManager</code> accordingly.</p><p><strong>⚠️ Caveat: You have to make sure that the existing <code>data</code> keys match with what <code>SecretsManager</code> uses:</strong></p><table tabindex="0"><thead><tr><th>Secret Type</th><th>Data Keys</th></tr></thead><tbody><tr><td>Basic Auth</td><td><code>username</code>, <code>password</code>, <code>auth</code></td></tr><tr><td>CA Certificate</td><td><code>ca.crt</code>, <code>ca.key</code></td></tr><tr><td>Non-CA Certificate</td><td><code>tls.crt</code>, <code>tls.key</code></td></tr><tr><td>Control Plane Secret</td><td><code>ca.crt</code>, <code>username</code>, <code>password</code>, <code>token</code>, <code>kubeconfig</code></td></tr><tr><td>ETCD Encryption Key</td><td><code>key</code>, <code>secret</code></td></tr><tr><td>Kubeconfig</td><td><code>kubeconfig</code></td></tr><tr><td>RSA Private Key</td><td><code>id_rsa</code>, <code>id_rsa.pub</code></td></tr><tr><td>Static Token</td><td><code>static_tokens.csv</code></td></tr><tr><td>VPN TLS Auth</td><td><code>vpn.tlsauth</code></td></tr></tbody></table><h2 id="implementation-details" tabindex="-1">Implementation Details <a class="header-anchor" href="#implementation-details" aria-label="Permalink to &quot;Implementation Details&quot;">​</a></h2><p>The source of truth for the secrets manager is the list of <code>Secret</code>s in the Kubernetes cluster it acts upon (typically, the seed cluster). The persisted secrets in the <code>ShootState</code> are only used if and only if the shoot is in the <code>Restore</code> phase - in this case all secrets are just synced to the seed cluster so that they can be picked up by the secrets manager.</p><p>In order to prevent kubelets from unneeded watches (thus, causing some significant traffic against the <code>kube-apiserver</code>), the <code>Secret</code>s are marked as immutable. Consequently, they have a unique, deterministic name which is computed as follows:</p><ul><li>For CA secrets, the name is just exactly the name specified in the configuration (e.g., <code>ca</code>). This is for backwards-compatibility and will be dropped in a future release once all components depending on the static name have been adapted.</li><li>For all other secrets, the name specified in the configuration is used as prefix followed by an 8-digit hash. This hash is computed out of the checksum of the secret configuration and the checksum of the certificate of the signing CA (only for certificate configurations).</li></ul><p>In all cases, the name of the secrets is suffixed with a 5-digit hash computed out of the time when the rotation for this secret was last started.</p>`,38)]))}const u=t(n,[["render",r]]);export{g as __pageData,u as default};

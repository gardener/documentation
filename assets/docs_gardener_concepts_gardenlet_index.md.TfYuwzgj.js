import{_ as t,c as o,o as r,a2 as a}from"./chunks/framework.Bfq10Vlj.js";const n="/assets/gardenlet-architecture-similarities.BbqCgmxv.png",s="/assets/gardenlet-architecture-detailed.BJGa-5b9.png",f=JSON.parse('{"title":"gardenlet","description":"Understand how the gardenlet, the primary \\"agent\\" on every seed cluster, works and learn more about the different Gardener components","frontmatter":{"description":"Understand how the gardenlet, the primary \\"agent\\" on every seed cluster, works and learn more about the different Gardener components","github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/concepts","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/concepts/gardenlet.md","to":"gardenlet.md"},"title":"gardenlet","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/concepts/gardenlet/index.md","filePath":"docs/gardener/concepts/gardenlet.md","lastUpdated":null}'),i={name:"docs/gardener/concepts/gardenlet/index.md"};function d(c,e,l,h,p,u){return r(),o("div",null,e[0]||(e[0]=[a('<h1 id="gardenlet" tabindex="-1">gardenlet <a class="header-anchor" href="#gardenlet" aria-label="Permalink to &quot;gardenlet&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Gardener is implemented using the <a href="https://kubernetes.io/docs/concepts/extend-kubernetes/operator/" target="_blank" rel="noreferrer">operator pattern</a>: It uses custom controllers that act on our own custom resources, and apply Kubernetes principles to manage clusters instead of containers. Following this analogy, you can recognize components of the Gardener architecture as well-known Kubernetes components, for example, shoot clusters can be compared with pods, and seed clusters can be seen as worker nodes.</p><p>The following Gardener components play a similar role as the corresponding components in the Kubernetes architecture:</p><table tabindex="0"><thead><tr><th>Gardener Component</th><th>Kubernetes Component</th></tr></thead><tbody><tr><td><code>gardener-apiserver</code></td><td><code>kube-apiserver</code></td></tr><tr><td><code>gardener-controller-manager</code></td><td><code>kube-controller-manager</code></td></tr><tr><td><code>gardener-scheduler</code></td><td><code>kube-scheduler</code></td></tr><tr><td><code>gardenlet</code></td><td><code>kubelet</code></td></tr></tbody></table><p>Similar to how the <code>kube-scheduler</code> of Kubernetes finds an appropriate node for newly created pods, the <code>gardener-scheduler</code> of Gardener finds an appropriate seed cluster to host the control plane for newly ordered clusters. By providing multiple seed clusters for a region or provider, and distributing the workload, Gardener also reduces the blast radius of potential issues.</p><p>Kubernetes runs a primary &quot;agent&quot; on every node, the kubelet, which is responsible for managing pods and containers on its particular node. Decentralizing the responsibility to the kubelet has the advantage that the overall system is scalable. Gardener achieves the same for cluster management by using a <strong>gardenlet</strong> as a primary &quot;agent&quot; on every seed cluster, and is only responsible for shoot clusters located in its particular seed cluster:</p><p><img src="'+n+'" alt="Counterparts in the Gardener Architecture and the Kubernetes Architecture"></p><p>The <code>gardener-controller-manager</code> has controllers to manage resources of the Gardener API. However, instead of letting the <code>gardener-controller-manager</code> talk directly to seed clusters or shoot clusters, the responsibility isn’t only delegated to the gardenlet, but also managed using a reversed control flow: It&#39;s up to the gardenlet to contact the Gardener API server, for example, to share a status for its managed seed clusters.</p><p>Reversing the control flow allows placing seed clusters or shoot clusters behind firewalls without the necessity of direct access via VPN tunnels anymore.</p><p><img src="'+s+'" alt="Reversed Control Flow Using a gardenlet"></p><h2 id="tls-bootstrapping" tabindex="-1">TLS Bootstrapping <a class="header-anchor" href="#tls-bootstrapping" aria-label="Permalink to &quot;TLS Bootstrapping&quot;">​</a></h2><p>Kubernetes doesn’t manage worker nodes itself, and it’s also not responsible for the lifecycle of the kubelet running on the workers. Similarly, Gardener doesn’t manage seed clusters itself, so it is also not responsible for the lifecycle of the gardenlet running on the seeds. As a consequence, both the gardenlet and the kubelet need to prepare a trusted connection to the Gardener API server and the Kubernetes API server correspondingly.</p><p>To prepare a trusted connection between the gardenlet and the Gardener API server, the gardenlet initializes a bootstrapping process after you deployed it into your seed clusters:</p><ol><li><p>The gardenlet starts up with a bootstrap <code>kubeconfig</code> having a bootstrap token that allows to create <code>CertificateSigningRequest</code> (CSR) resources.</p></li><li><p>After the CSR is signed, the gardenlet downloads the created client certificate, creates a new <code>kubeconfig</code> with it, and stores it inside a <code>Secret</code> in the seed cluster.</p></li><li><p>The gardenlet deletes the bootstrap <code>kubeconfig</code> secret, and starts up with its new <code>kubeconfig</code>.</p></li><li><p>The gardenlet starts normal operation.</p></li></ol><p>The <code>gardener-controller-manager</code> runs a control loop that automatically signs CSRs created by gardenlets.</p><blockquote><p>The gardenlet bootstrapping process is based on the kubelet bootstrapping process. More information: <a href="https://kubernetes.io/docs/reference/access-authn-authz/kubelet-tls-bootstrapping/" target="_blank" rel="noreferrer">Kubelet&#39;s TLS bootstrapping</a>.</p></blockquote><p>If you don&#39;t want to run this bootstrap process, you can create a <code>kubeconfig</code> pointing to the garden cluster for the gardenlet yourself, and use the field <code>gardenClientConnection.kubeconfig</code> in the gardenlet configuration to share it with the gardenlet.</p><h2 id="gardenlet-certificate-rotation" tabindex="-1">gardenlet Certificate Rotation <a class="header-anchor" href="#gardenlet-certificate-rotation" aria-label="Permalink to &quot;gardenlet Certificate Rotation&quot;">​</a></h2><p>The certificate used to authenticate the gardenlet against the API server has a certain validity based on the configuration of the garden cluster (<code>--cluster-signing-duration</code> flag of the <code>kube-controller-manager</code> (default <code>1y</code>)).</p><blockquote><p>You can also configure the validity for the client certificate by specifying <code>.gardenClientConnection.kubeconfigValidity.validity</code> in the gardenlet&#39;s component configuration. Note that changing this value will only take effect when the kubeconfig is rotated again (it is not picked up immediately). The minimum validity is <code>10m</code> (that&#39;s what is enforced by the <code>CertificateSigningRequest</code> API in Kubernetes which is used by the gardenlet).</p></blockquote><p>By default, after about 70-90% of the validity has expired, the gardenlet tries to automatically replace the current certificate with a new one (certificate rotation).</p><blockquote><p>You can change these boundaries by specifying <code>.gardenClientConnection.kubeconfigValidity.autoRotationJitterPercentage{Min,Max}</code> in the gardenlet&#39;s component configuration.</p></blockquote><p>To use a certificate rotation, you need to specify the secret to store the <code>kubeconfig</code> with the rotated certificate in the field <code>.gardenClientConnection.kubeconfigSecret</code> of the gardenlet <a href="/docs/gardener/concepts/gardenlet/#component-configuration">component configuration</a>.</p><h3 id="rotate-certificates-using-bootstrap-kubeconfig" tabindex="-1">Rotate Certificates Using Bootstrap <code>kubeconfig</code> <a class="header-anchor" href="#rotate-certificates-using-bootstrap-kubeconfig" aria-label="Permalink to &quot;Rotate Certificates Using Bootstrap `kubeconfig`&quot;">​</a></h3><p>If the gardenlet created the certificate during the initial TLS Bootstrapping using the Bootstrap <code>kubeconfig</code>, certificates can be rotated automatically. The same control loop in the <code>gardener-controller-manager</code> that signs the CSRs during the initial TLS Bootstrapping also automatically signs the CSR during a certificate rotation.</p><p>ℹ️ You can trigger an immediate renewal by annotating the <code>Secret</code> in the seed cluster stated in the <code>.gardenClientConnection.kubeconfigSecret</code> field with <code>gardener.cloud/operation=renew</code>. Within <code>10s</code>, gardenlet detects this and terminates itself to request new credentials. After it has booted up again, gardenlet will issue a new certificate independent of the remaining validity of the existing one.</p><p>ℹ️ Alternatively, annotate the respective <code>Seed</code> with <code>gardener.cloud/operation=renew-kubeconfig</code>. This will make gardenlet annotate its own kubeconfig secret with <code>gardener.cloud/operation=renew</code> and triggers the process described in the previous paragraph.</p><h3 id="rotate-certificates-using-custom-kubeconfig" tabindex="-1">Rotate Certificates Using Custom <code>kubeconfig</code> <a class="header-anchor" href="#rotate-certificates-using-custom-kubeconfig" aria-label="Permalink to &quot;Rotate Certificates Using Custom `kubeconfig`&quot;">​</a></h3><p>When trying to rotate a custom certificate that wasn’t created by gardenlet as part of the TLS Bootstrap, the x509 certificate&#39;s <code>Subject</code> field needs to conform to the following:</p><ul><li>the Common Name (CN) is prefixed with <code>gardener.cloud:system:seed:</code></li><li>the Organization (O) equals <code>gardener.cloud:system:seeds</code></li></ul><p>Otherwise, the <code>gardener-controller-manager</code> doesn’t automatically sign the CSR. In this case, an external component or user needs to approve the CSR manually, for example, using the command <code>kubectl certificate approve seed-csr-&lt;...&gt;</code>). If that doesn’t happen within 15 minutes, the gardenlet repeats the process and creates another CSR.</p><h2 id="configuring-the-seed-to-work-with-gardenlet" tabindex="-1">Configuring the Seed to Work with gardenlet <a class="header-anchor" href="#configuring-the-seed-to-work-with-gardenlet" aria-label="Permalink to &quot;Configuring the Seed to Work with gardenlet&quot;">​</a></h2><p>The gardenlet works with a single seed, which must be configured in the <code>GardenletConfiguration</code> under <code>.seedConfig</code>. This must be a copy of the <code>Seed</code> resource, for example:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gardenlet.config.gardener.cloud/v1alpha1</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">GardenletConfiguration</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">seedConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-seed</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">aws</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # ...</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    settings</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      scheduling</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        visible</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span></code></pre></div><p>(see <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardenlet.yaml" target="_blank" rel="noreferrer">this yaml file</a> for a more complete example)</p><p>On startup, gardenlet registers a <code>Seed</code> resource using the given template in the <code>seedConfig</code> if it&#39;s not present already.</p><h2 id="component-configuration" tabindex="-1">Component Configuration <a class="header-anchor" href="#component-configuration" aria-label="Permalink to &quot;Component Configuration&quot;">​</a></h2><p>In the component configuration for the gardenlet, it’s possible to define:</p><ul><li>settings for the Kubernetes clients interacting with the various clusters</li><li>settings for the controllers inside the gardenlet</li><li>settings for leader election and log levels, feature gates, and seed selection or seed configuration.</li></ul><p>More information: <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardenlet.yaml" target="_blank" rel="noreferrer">Example gardenlet Component Configuration</a>.</p><h2 id="heartbeats" tabindex="-1">Heartbeats <a class="header-anchor" href="#heartbeats" aria-label="Permalink to &quot;Heartbeats&quot;">​</a></h2><p>Similar to how Kubernetes uses <code>Lease</code> objects for node heart beats (see <a href="https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/589-efficient-node-heartbeats/README.md" target="_blank" rel="noreferrer">KEP</a>), the gardenlet is using <code>Lease</code> objects for heart beats of the seed cluster. Every two seconds, the gardenlet checks that the seed cluster&#39;s <code>/healthz</code> endpoint returns HTTP status code 200. If that is the case, the gardenlet renews the lease in the Garden cluster in the <code>gardener-system-seed-lease</code> namespace and updates the <code>GardenletReady</code> condition in the <code>status.conditions</code> field of the <code>Seed</code> resource. For more information, see <a href="/docs/gardener/concepts/gardenlet/#lease-reconciler">this section</a>.</p><p>Similar to the <code>node-lifecycle-controller</code> inside the <code>kube-controller-manager</code>, the <code>gardener-controller-manager</code> features a <code>seed-lifecycle-controller</code> that sets the <code>GardenletReady</code> condition to <code>Unknown</code> in case the gardenlet fails to renew the lease. As a consequence, the <code>gardener-scheduler</code> doesn’t consider this seed cluster for newly created shoot clusters anymore.</p><h3 id="healthz-endpoint" tabindex="-1"><code>/healthz</code> Endpoint <a class="header-anchor" href="#healthz-endpoint" aria-label="Permalink to &quot;`/healthz` Endpoint&quot;">​</a></h3><p>The gardenlet includes an HTTP server that serves a <code>/healthz</code> endpoint. It’s used as a liveness probe in the <code>Deployment</code> of the gardenlet. If the gardenlet fails to renew its lease, then the endpoint returns <code>500 Internal Server Error</code>, otherwise it returns <code>200 OK</code>.</p><p>Please note that the <code>/healthz</code> only indicates whether the gardenlet could successfully probe the Seed&#39;s API server and renew the lease with the Garden cluster. It does <em>not</em> show that the Gardener extension API server (with the Gardener resource groups) is available. However, the gardenlet is designed to withstand such connection outages and retries until the connection is reestablished.</p><h2 id="controllers" tabindex="-1">Controllers <a class="header-anchor" href="#controllers" aria-label="Permalink to &quot;Controllers&quot;">​</a></h2><p>The gardenlet consists out of several controllers which are now described in more detail.</p><h3 id="backupbucket-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/backupbucket" target="_blank" rel="noreferrer"><code>BackupBucket</code> Controller</a> <a class="header-anchor" href="#backupbucket-controller" aria-label="Permalink to &quot;[`BackupBucket` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/backupbucket)&quot;">​</a></h3><p>The <code>BackupBucket</code> controller reconciles those <code>core.gardener.cloud/v1beta1.BackupBucket</code> resources whose <code>.spec.seedName</code> value is equal to the name of the <code>Seed</code> the respective <code>gardenlet</code> is responsible for. A <code>core.gardener.cloud/v1beta1.BackupBucket</code> resource is created by the <code>Seed</code> controller if <code>.spec.backup</code> is defined in the <code>Seed</code>.</p><p>The controller adds finalizers to the <code>BackupBucket</code> and the secret mentioned in the <code>.spec.secretRef</code> of the <code>BackupBucket</code>. The controller also copies this secret to the seed cluster. Additionally, it creates an <code>extensions.gardener.cloud/v1alpha1.BackupBucket</code> resource (non-namespaced) in the seed cluster and waits until the responsible extension controller reconciles it (see <a href="/docs/gardener/extensions/resources/backupbucket/">Contract: BackupBucket Resource</a> for more details). The status from the reconciliation is reported in the <code>.status.lastOperation</code> field. Once the extension resource is ready and the <code>.status.generatedSecretRef</code> is set by the extension controller, the <code>gardenlet</code> copies the referenced secret to the <code>garden</code> namespace in the garden cluster. An owner reference to the <code>core.gardener.cloud/v1beta1.BackupBucket</code> is added to this secret.</p><p>If the <code>core.gardener.cloud/v1beta1.BackupBucket</code> is deleted, the controller deletes the generated secret in the garden cluster and the <code>extensions.gardener.cloud/v1alpha1.BackupBucket</code> resource in the seed cluster and it waits for the respective extension controller to remove its finalizers from the <code>extensions.gardener.cloud/v1alpha1.BackupBucket</code>. Then it deletes the secret in the seed cluster and finally removes the finalizers from the <code>core.gardener.cloud/v1beta1.BackupBucket</code> and the referred secret.</p><h3 id="backupentry-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/backupentry" target="_blank" rel="noreferrer"><code>BackupEntry</code> Controller</a> <a class="header-anchor" href="#backupentry-controller" aria-label="Permalink to &quot;[`BackupEntry` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/backupentry)&quot;">​</a></h3><p>The <code>BackupEntry</code> controller reconciles those <code>core.gardener.cloud/v1beta1.BackupEntry</code> resources whose <code>.spec.seedName</code> value is equal to the name of a <code>Seed</code> the respective gardenlet is responsible for. Those resources are created by the <code>Shoot</code> controller (only if backup is enabled for the respective <code>Seed</code>) and there is exactly one <code>BackupEntry</code> per <code>Shoot</code>.</p><p>The controller creates an <code>extensions.gardener.cloud/v1alpha1.BackupEntry</code> resource (non-namespaced) in the seed cluster and waits until the responsible extension controller reconciled it (see <a href="/docs/gardener/extensions/resources/backupentry/">Contract: BackupEntry Resource</a> for more details). The status is populated in the <code>.status.lastOperation</code> field.</p><p>The <code>core.gardener.cloud/v1beta1.BackupEntry</code> resource has an owner reference pointing to the corresponding <code>Shoot</code>. Hence, if the <code>Shoot</code> is deleted, the <code>BackupEntry</code> resource also gets deleted. In this case, the controller deletes the <code>extensions.gardener.cloud/v1alpha1.BackupEntry</code> resource in the seed cluster and waits until the responsible extension controller has deleted it. Afterwards, the finalizer of the <code>core.gardener.cloud/v1beta1.BackupEntry</code> resource is released so that it finally disappears from the system.</p><p>If the <code>spec.seedName</code> and <code>.status.seedName</code> of the <code>core.gardener.cloud/v1beta1.BackupEntry</code> are different, the controller will migrate it by annotating the <code>extensions.gardener.cloud/v1alpha1.BackupEntry</code> in the <code>Source Seed</code> with <code>gardener.cloud/operation: migrate</code>, waiting for it to be migrated successfully and eventually deleting it from the <code>Source Seed</code> cluster. Afterwards, the controller will recreate the <code>extensions.gardener.cloud/v1alpha1.BackupEntry</code> in the <code>Destination Seed</code>, annotate it with <code>gardener.cloud/operation: restore</code> and wait for the restore operation to finish. For more details about control plane migration, please read <a href="/docs/gardener/control_plane_migration/#shoot-control-plane-migration">Shoot Control Plane Migration</a>.</p><h5 id="keep-backup-for-deleted-shoots" tabindex="-1">Keep Backup for Deleted Shoots <a class="header-anchor" href="#keep-backup-for-deleted-shoots" aria-label="Permalink to &quot;Keep Backup for Deleted Shoots&quot;">​</a></h5><p>In some scenarios it might be beneficial to not immediately delete the <code>BackupEntry</code>s (and with them, the etcd backup) for deleted <code>Shoot</code>s.</p><p>In this case you can configure the <code>.controllers.backupEntry.deletionGracePeriodHours</code> field in the component configuration of the gardenlet. For example, if you set it to <code>48</code>, then the <code>BackupEntry</code>s for deleted <code>Shoot</code>s will only be deleted <code>48</code> hours after the <code>Shoot</code> was deleted.</p><p>Additionally, you can limit the <a href="/docs/gardener/shoot/shoot_purposes/">shoot purposes</a> for which this applies by setting <code>.controllers.backupEntry.deletionGracePeriodShootPurposes[]</code>. For example, if you set it to <code>[production]</code> then only the <code>BackupEntry</code>s for <code>Shoot</code>s with <code>.spec.purpose=production</code> will be deleted after the configured grace period. All others will be deleted immediately after the <code>Shoot</code> deletion.</p><p>In case a <code>BackupEntry</code> is scheduled for future deletion but you want to delete it immediately, add the annotation <code>backupentry.core.gardener.cloud/force-deletion=true</code>.</p><h3 id="bastion-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/bastion" target="_blank" rel="noreferrer"><code>Bastion</code> Controller</a> <a class="header-anchor" href="#bastion-controller" aria-label="Permalink to &quot;[`Bastion` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/bastion)&quot;">​</a></h3><p>The <code>Bastion</code> controller reconciles those <code>operations.gardener.cloud/v1alpha1.Bastion</code> resources whose <code>.spec.seedName</code> value is equal to the name of a <code>Seed</code> the respective gardenlet is responsible for.</p><p>The controller creates an <code>extensions.gardener.cloud/v1alpha1.Bastion</code> resource in the seed cluster in the shoot namespace with the same name as <code>operations.gardener.cloud/v1alpha1.Bastion</code>. Then it waits until the responsible extension controller has reconciled it (see <a href="/docs/gardener/extensions/resources/bastion/">Contract: Bastion Resource</a> for more details). The status is populated in the <code>.status.conditions</code> and <code>.status.ingress</code> fields.</p><p>During the deletion of <code>operations.gardener.cloud/v1alpha1.Bastion</code> resources, the controller first sets the <code>Ready</code> condition to <code>False</code> and then deletes the <code>extensions.gardener.cloud/v1alpha1.Bastion</code> resource in the seed cluster. Once this resource is gone, the finalizer of the <code>operations.gardener.cloud/v1alpha1.Bastion</code> resource is released, so it finally disappears from the system.</p><h3 id="controllerinstallation-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation" target="_blank" rel="noreferrer"><code>ControllerInstallation</code> Controller</a> <a class="header-anchor" href="#controllerinstallation-controller" aria-label="Permalink to &quot;[`ControllerInstallation` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation)&quot;">​</a></h3><p>The <code>ControllerInstallation</code> controller in the <code>gardenlet</code> reconciles <code>ControllerInstallation</code> objects with the help of the following reconcilers.</p><h4 id="main-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/controllerinstallation" target="_blank" rel="noreferrer">&quot;Main&quot; Reconciler</a> <a class="header-anchor" href="#main-reconciler" aria-label="Permalink to &quot;[&quot;Main&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/controllerinstallation)&quot;">​</a></h4><p>This reconciler is responsible for <code>ControllerInstallation</code>s referencing a <code>ControllerDeployment</code> whose <code>type=helm</code>.</p><p>For each <code>ControllerInstallation</code>, it creates a namespace on the seed cluster named <code>extension-&lt;controller-installation-name&gt;</code>. Then, it creates a generic garden kubeconfig and garden access secret for the extension for <a href="/docs/gardener/extensions/garden-api-access/">accessing the garden cluster</a>.</p><p>After that, it unpacks the Helm chart tarball in the <code>ControllerDeployment</code>s <code>.providerConfig.chart</code> field and deploys the rendered resources to the seed cluster. The Helm chart values in <code>.providerConfig.values</code> will be used and extended with some information about the Gardener environment and the seed cluster:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">gardener</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;gardenlet-version&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  garden</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    clusterIdentity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;identity-of-garden-cluster&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    genericKubeconfigSecretName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;secret-name&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  gardenlet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    featureGates</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      Foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      Bar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # ...</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  seed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;seed-name&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    clusterIdentity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;identity-of-seed-cluster&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    annotations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;seed-annotations&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    labels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;seed-labels&gt;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;seed-specification&gt;</span></span></code></pre></div><p>As of today, there are a few more fields in <code>.gardener.seed</code>, but it is recommended to use the <code>.gardener.seed.spec</code> if the Helm chart needs more information about the seed configuration.</p><p>The rendered chart will be deployed via a <code>ManagedResource</code> created in the <code>garden</code> namespace of the seed cluster. It is labeled with <code>controllerinstallation-name=&lt;name&gt;</code> so that one can easily find the owning <code>ControllerInstallation</code> for an existing <code>ManagedResource</code>.</p><p>The reconciler maintains the <code>Installed</code> condition of the <code>ControllerInstallation</code> and sets it to <code>False</code> if the rendering or deployment fails.</p><h4 id="care-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/care" target="_blank" rel="noreferrer">&quot;Care&quot; Reconciler</a> <a class="header-anchor" href="#care-reconciler" aria-label="Permalink to &quot;[&quot;Care&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/care)&quot;">​</a></h4><p>This reconciler reconciles <code>ControllerInstallation</code> objects and checks whether they are in a healthy state. It checks the <code>.status.conditions</code> of the backing <code>ManagedResource</code> created in the <code>garden</code> namespace of the seed cluster.</p><ul><li>If the <code>ResourcesApplied</code> condition of the <code>ManagedResource</code> is <code>True</code>, then the <code>Installed</code> condition of the <code>ControllerInstallation</code> will be set to <code>True</code>.</li><li>If the <code>ResourcesHealthy</code> condition of the <code>ManagedResource</code> is <code>True</code>, then the <code>Healthy</code> condition of the <code>ControllerInstallation</code> will be set to <code>True</code>.</li><li>If the <code>ResourcesProgressing</code> condition of the <code>ManagedResource</code> is <code>True</code>, then the <code>Progressing</code> condition of the <code>ControllerInstallation</code> will be set to <code>True</code>.</li></ul><p>A <code>ControllerInstallation</code> is considered &quot;healthy&quot; if <code>Applied=Healthy=True</code> and <code>Progressing=False</code>.</p><h4 id="required-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/required" target="_blank" rel="noreferrer">&quot;Required&quot; Reconciler</a> <a class="header-anchor" href="#required-reconciler" aria-label="Permalink to &quot;[&quot;Required&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/controllerinstallation/required)&quot;">​</a></h4><p>This reconciler watches all resources in the <code>extensions.gardener.cloud</code> API group in the seed cluster. It is responsible for maintaining the <code>Required</code> condition on <code>ControllerInstallation</code>s. Concretely, when there is at least one extension resource in the seed cluster a <code>ControllerInstallation</code> is responsible for, then the status of the <code>Required</code> condition will be <code>True</code>. If there are no extension resources anymore, its status will be <code>False</code>.</p><p>This condition is taken into account by the <code>ControllerRegistration</code> controller part of <code>gardener-controller-manager</code> when it computes which extensions have to be deployed to which seed cluster. See <a href="/docs/gardener/concepts/controller-manager/#controllerregistration-controller">Gardener Controller Manager</a> for more details.</p><h3 id="gardenlet-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/gardenlet" target="_blank" rel="noreferrer"><code>Gardenlet</code> Controller</a> <a class="header-anchor" href="#gardenlet-controller" aria-label="Permalink to &quot;[`Gardenlet` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/gardenlet)&quot;">​</a></h3><p>The <code>Gardenlet</code> controller reconciles a <code>Gardenlet</code> resource with the same name as the <code>Seed</code> the gardenlet is responsible for. This is used to implement self-upgrades of <code>gardenlet</code> based on information pulled from the garden cluster. For a general overview, see <a href="/docs/gardener/deployment/deploy_gardenlet/">this document</a>.</p><p>On <code>Gardenlet</code> reconciliation, the controller deploys the <code>gardenlet</code> within its own cluster which after downloading the Helm chart specified in <code>.spec.deployment.helm.ociRepository</code> and rendering it with the provided values/configuration.</p><p>On <code>Gardenlet</code> deletion, nothing happens: The <code>gardenlet</code> does not terminate itself - deleting a <code>Gardenlet</code> object effectively means that self-upgrades are stopped.</p><h3 id="managedseed-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/managedseed" target="_blank" rel="noreferrer"><code>ManagedSeed</code> Controller</a> <a class="header-anchor" href="#managedseed-controller" aria-label="Permalink to &quot;[`ManagedSeed` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/managedseed)&quot;">​</a></h3><p>The <code>ManagedSeed</code> controller in the <code>gardenlet</code> reconciles <code>ManagedSeed</code>s that refers to <code>Shoot</code> scheduled on <code>Seed</code> the gardenlet is responsible for. Additionally, the controller monitors <code>Seed</code>s, which are owned by <code>ManagedSeed</code>s for which the gardenlet is responsible.</p><p>On <code>ManagedSeed</code> reconciliation, the controller first waits for the referenced <code>Shoot</code> to undergo a reconciliation process. Once the <code>Shoot</code> is successfully reconciled, the controller sets the <code>ShootReconciled</code> status of the <code>ManagedSeed</code> to <code>true</code>. Then, it creates <code>garden</code> namespace within the target shoot cluster. The controller also manages secrets related to <code>Seed</code>s, such as the <code>backup</code> and <code>kubeconfig</code> secrets. It ensures that these secrets are created and updated according to the <code>ManagedSeed</code> spec. Finally, it deploys the <code>gardenlet</code> within the specified shoot cluster which registers the <code>Seed</code> cluster.</p><p>On <code>ManagedSeed</code> deletion, the controller first deletes the corresponding <code>Seed</code> that was originally created by the controller. Subsequently, it deletes the <code>gardenlet</code> instance within the shoot cluster. The controller also ensures the deletion of related <code>Seed</code> secrets. Finally, the dedicated <code>garden</code> namespace within the shoot cluster is deleted.</p><h3 id="networkpolicy-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/networkpolicy" target="_blank" rel="noreferrer"><code>NetworkPolicy</code> Controller</a> <a class="header-anchor" href="#networkpolicy-controller" aria-label="Permalink to &quot;[`NetworkPolicy` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/networkpolicy)&quot;">​</a></h3><p>The <code>NetworkPolicy</code> controller reconciles <code>NetworkPolicy</code>s in all relevant namespaces in the seed cluster and provides so-called &quot;general&quot; policies for access to the runtime cluster&#39;s API server, DNS, public networks, etc.</p><p>The controller resolves the IP address of the Kubernetes service in the <code>default</code> namespace and creates an egress <code>NetworkPolicy</code>s for it.</p><p>For more details about <code>NetworkPolicy</code>s in Gardener, please see <a href="/docs/gardener/network_policies/"><code>NetworkPolicy</code>s In Garden, Seed, Shoot Clusters</a>.</p><h3 id="seed-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed" target="_blank" rel="noreferrer"><code>Seed</code> Controller</a> <a class="header-anchor" href="#seed-controller" aria-label="Permalink to &quot;[`Seed` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed)&quot;">​</a></h3><p>The <code>Seed</code> controller in the <code>gardenlet</code> reconciles <code>Seed</code> objects with the help of the following reconcilers.</p><h4 id="main-reconciler-1" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/seed" target="_blank" rel="noreferrer">&quot;Main Reconciler&quot;</a> <a class="header-anchor" href="#main-reconciler-1" aria-label="Permalink to &quot;[&quot;Main Reconciler&quot;](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/seed)&quot;">​</a></h4><p>This reconciler is responsible for managing the seed&#39;s system components. Those comprise CA certificates, the various <code>CustomResourceDefinition</code>s, the logging and monitoring stacks, and few central components like <code>gardener-resource-manager</code>, <code>etcd-druid</code>, <code>istio</code>, etc.</p><p>The reconciler also deploys a <code>BackupBucket</code> resource in the garden cluster in case the <code>Seed&#39;</code>s <code>.spec.backup</code> is set. It also checks whether the seed cluster&#39;s Kubernetes version is at least the <a href="/docs/gardener/shoot-operations/supported_k8s_versions/#seed-cluster-versions">minimum supported version</a> and errors in case this constraint is not met.</p><p>This reconciler maintains the <code>.status.lastOperation</code> field, i.e. it sets it:</p><ul><li>to <code>state=Progressing</code> before it executes its reconciliation flow.</li><li>to <code>state=Error</code> in case an error occurs.</li><li>to <code>state=Succeeded</code> in case the reconciliation succeeded.</li></ul><h4 id="care-reconciler-1" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/care" target="_blank" rel="noreferrer">&quot;Care&quot; Reconciler</a> <a class="header-anchor" href="#care-reconciler-1" aria-label="Permalink to &quot;[&quot;Care&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/care)&quot;">​</a></h4><p>This reconciler checks whether the seed system components (deployed by the &quot;main&quot; reconciler) are healthy. It checks the <code>.status.conditions</code> of the backing <code>ManagedResource</code> created in the <code>garden</code> namespace of the seed cluster. A <code>ManagedResource</code> is considered &quot;healthy&quot; if the conditions <code>ResourcesApplied=ResourcesHealthy=True</code> and <code>ResourcesProgressing=False</code>.</p><p>If all <code>ManagedResource</code>s are healthy, then the <code>SeedSystemComponentsHealthy</code> condition of the <code>Seed</code> will be set to <code>True</code>. Otherwise, it will be set to <code>False</code>.</p><p>If at least one <code>ManagedResource</code> is unhealthy and there is threshold configuration for the conditions (in <code>.controllers.seedCare.conditionThresholds</code>), then the status of the <code>SeedSystemComponentsHealthy</code> condition will be set:</p><ul><li>to <code>Progressing</code> if it was <code>True</code> before.</li><li>to <code>Progressing</code> if it was <code>Progressing</code> before and the <code>lastUpdateTime</code> of the condition does not exceed the configured threshold duration yet.</li><li>to <code>False</code> if it was <code>Progressing</code> before and the <code>lastUpdateTime</code> of the condition exceeds the configured threshold duration.</li></ul><p>The condition thresholds can be used to prevent reporting issues too early just because there is a rollout or a short disruption. Only if the unhealthiness persists for at least the configured threshold duration, then the issues will be reported (by setting the status to <code>False</code>).</p><p>In order to compute the condition statuses, this reconciler considers <code>ManagedResource</code>s (in the <code>garden</code> and <code>istio-system</code> namespace) and their status, see <a href="/docs/gardener/concepts/resource-manager/#conditions">this document</a> for more information. The following table explains which <code>ManagedResource</code>s are considered for which condition type:</p><table tabindex="0"><thead><tr><th>Condition Type</th><th><code>ManagedResource</code>s are considered when</th></tr></thead><tbody><tr><td><code>SeedSystemComponentsHealthy</code></td><td><code>.spec.class</code> is set</td></tr></tbody></table><h4 id="lease-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/lease" target="_blank" rel="noreferrer">&quot;Lease&quot; Reconciler</a> <a class="header-anchor" href="#lease-reconciler" aria-label="Permalink to &quot;[&quot;Lease&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/seed/lease)&quot;">​</a></h4><p>This reconciler checks whether the connection to the seed cluster&#39;s <code>/healthz</code> endpoint works. If this succeeds, then it renews a <code>Lease</code> resource in the garden cluster&#39;s <code>gardener-system-seed-lease</code> namespace. This indicates a heartbeat to the external world, and internally the <code>gardenlet</code> sets its health status to <code>true</code>. In addition, the <code>GardenletReady</code> condition in the <code>status</code> of the <code>Seed</code> is set to <code>True</code>. The whole process is similar to what the <code>kubelet</code> does to report heartbeats for its <code>Node</code> resource and its <code>KubeletReady</code> condition. For more information, see <a href="/docs/gardener/concepts/gardenlet/#heartbeats">this section</a>.</p><p>If the connection to the <code>/healthz</code> endpoint or the update of the <code>Lease</code> fails, then the internal health status of <code>gardenlet</code> is set to <code>false</code>. Also, this internal health status is set to <code>false</code> automatically after some time, in case the controller gets stuck for whatever reason. This internal health status is available via the <code>gardenlet</code>&#39;s <code>/healthz</code> endpoint and is used for the <code>livenessProbe</code> in the <code>gardenlet</code> pod.</p><h3 id="shoot-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot" target="_blank" rel="noreferrer"><code>Shoot</code> Controller</a> <a class="header-anchor" href="#shoot-controller" aria-label="Permalink to &quot;[`Shoot` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot)&quot;">​</a></h3><p>The <code>Shoot</code> controller in the <code>gardenlet</code> reconciles <code>Shoot</code> objects with the help of the following reconcilers.</p><h4 id="main-reconciler-2" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/shoot" target="_blank" rel="noreferrer">&quot;Main&quot; Reconciler</a> <a class="header-anchor" href="#main-reconciler-2" aria-label="Permalink to &quot;[&quot;Main&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/shoot)&quot;">​</a></h4><p>This reconciler is responsible for managing all shoot cluster components and implements the core logic for creating, updating, hibernating, deleting, and migrating shoot clusters. It is also responsible for syncing the <a href="/docs/gardener/extensions/cluster/"><code>Cluster</code> cluster</a> to the seed cluster before and after each successful shoot reconciliation.</p><p>The main reconciliation logic is performed in 3 different task flows dedicated to specific operation types:</p><ul><li><code>reconcile</code> (operations: create, reconcile, restore): this is the main flow responsible for creation and regular reconciliation of shoots. Hibernating a shoot also triggers this flow. It is also used for restoration of the shoot control plane on the new seed (second half of a <a href="/docs/gardener/control_plane_migration/#shoot-control-plane-migration">Control Plane Migration</a>)</li><li><code>migrate</code>: this flow is triggered when <code>spec.seedName</code> specifies a different seed than <code>status.seedName</code>. It performs the first half of the <a href="/docs/gardener/control_plane_migration/#shoot-control-plane-migration">Control Plane Migration</a>, i.e., a backup (<code>migrate</code> operation) of all control plane components followed by a &quot;shallow delete&quot;.</li><li><code>delete</code>: this flow is triggered when the shoot&#39;s <code>deletionTimestamp</code> is set, i.e., when it is deleted.</li></ul><p>The gardenlet takes special care to prevent unnecessary shoot reconciliations. This is important for several reasons, e.g., to not overload the seed API servers and to not exhaust infrastructure rate limits too fast. The gardenlet performs shoot reconciliations according to the following rules:</p><ul><li>If <code>status.observedGeneration</code> is less than <code>metadata.generation</code>: this is the case, e.g., when the spec was changed, a <a href="/docs/gardener/shoot-operations/shoot_operations/">manual reconciliation operation</a> was triggered, or the shoot was deleted.</li><li>If the <a href="/docs/gardener/shoot/shoot_status/">last operation</a> was not successful.</li><li>If the shoot is in a <a href="/docs/gardener/shoot/shoot_status/">failed state</a>, the gardenlet does not perform any reconciliation on the shoot (unless the retry operation was triggered). However, it syncs the <code>Cluster</code> resource to the seed in order to inform the extension controllers about the failed state.</li><li>Regular reconciliations are performed with every <code>GardenletConfiguration.controllers.shoot.syncPeriod</code> (defaults to <code>1h</code>).</li><li>Shoot reconciliations are not performed if the assigned seed cluster is not healthy or has not been reconciled by the current gardenlet version yet (determined by the <code>Seed.status.gardener</code> section). This is done to make sure that shoots are reconciled with fully rolled out seed system components after a Gardener upgrade. Otherwise, the gardenlet might perform operations of the new version that doesn&#39;t match the old version of the deployed seed system components, which might lead to unspecified behavior.</li></ul><p>There are a few special cases that overwrite or confine how often and under which circumstances periodic shoot reconciliations are performed:</p><ul><li>In case the gardenlet config allows it (<code>controllers.shoot.respectSyncPeriodOverwrite</code>, disabled by default), the sync period for a shoot can be increased individually by setting the <code>shoot.gardener.cloud/sync-period</code> annotation. This is always allowed for shoots in the <code>garden</code> namespace. Shoots are not reconciled with a higher frequency than specified in <code>GardenletConfiguration.controllers.shoot.syncPeriod</code>.</li><li>In case the gardenlet config allows it (<code>controllers.shoot.respectSyncPeriodOverwrite</code>, disabled by default), shoots can be marked as &quot;ignored&quot; by setting the <code>shoot.gardener.cloud/ignore</code> annotation. In this case, the gardenlet does not perform any reconciliation for the shoot.</li><li>In case <code>GardenletConfiguration.controllers.shoot.reconcileInMaintenanceOnly</code> is enabled (disabled by default), the gardenlet performs regular shoot reconciliations only once in the respective maintenance time window (<code>GardenletConfiguration.controllers.shoot.syncPeriod</code> is ignored). The gardenlet randomly distributes shoot reconciliations over the maintenance time window to avoid high bursts of reconciliations (see <a href="/docs/gardener/shoot/shoot_maintenance/#cluster-reconciliation">Shoot Maintenance</a>).</li><li>In case <code>Shoot.spec.maintenance.confineSpecUpdateRollout</code> is enabled (disabled by default), changes to the shoot specification are not rolled out immediately but only during the respective maintenance time window (see <a href="/docs/gardener/shoot/shoot_maintenance/">Shoot Maintenance</a>).</li></ul><h4 id="care-reconciler-2" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/care" target="_blank" rel="noreferrer">&quot;Care&quot; Reconciler</a> <a class="header-anchor" href="#care-reconciler-2" aria-label="Permalink to &quot;[&quot;Care&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/care)&quot;">​</a></h4><p>This reconciler performs three &quot;care&quot; actions related to <code>Shoot</code>s.</p><h5 id="conditions" tabindex="-1">Conditions <a class="header-anchor" href="#conditions" aria-label="Permalink to &quot;Conditions&quot;">​</a></h5><p>It maintains the following conditions:</p><ul><li><code>APIServerAvailable</code>: The <code>/healthz</code> endpoint of the shoot&#39;s <code>kube-apiserver</code> is called and considered healthy when it responds with <code>200 OK</code>.</li><li><code>ControlPlaneHealthy</code>: The control plane is considered healthy when the respective <code>Deployment</code>s (for example <code>kube-apiserver</code>,<code>kube-controller-manager</code>), and <code>Etcd</code>s (for example <code>etcd-main</code>) exist and are healthy.</li><li><code>ObservabilityComponentsHealthy</code>: This condition is considered healthy when the respective <code>Deployment</code>s (for example <code>plutono</code>) and <code>StatefulSet</code>s (for example <code>prometheus</code>,<code>vali</code>) exist and are healthy.</li><li><code>EveryNodeReady</code>: The conditions of the worker nodes are checked (e.g., <code>Ready</code>, <code>MemoryPressure</code>). Also, it&#39;s checked whether the Kubernetes version of the installed <code>kubelet</code> matches the desired version specified in the <code>Shoot</code> resource.</li><li><code>SystemComponentsHealthy</code>: The conditions of the <code>ManagedResource</code>s are checked (e.g., <code>ResourcesApplied</code>). Also, it is verified whether the VPN tunnel connection is established (which is required for the <code>kube-apiserver</code> to communicate with the worker nodes).</li></ul><p>Sometimes, <code>ManagedResource</code>s can have both <code>Healthy</code> and <code>Progressing</code> conditions set to <code>True</code> (e.g., when a <code>DaemonSet</code> rolls out one-by-one on a large cluster with many nodes) while this is not reflected in the <code>Shoot</code> status. In order to catch issues where the rollout gets stuck, one can set <code>.controllers.shootCare.managedResourceProgressingThreshold</code> in the <code>gardenlet</code>&#39;s component configuration. If the <code>Progressing</code> condition is still <code>True</code> for more than the configured duration, the <code>SystemComponentsHealthy</code> condition in the <code>Shoot</code> is set to <code>False</code>, eventually.</p><p>Each condition can optionally also have error <code>codes</code> in order to indicate which type of issue was detected (see <a href="/docs/gardener/shoot/shoot_status/">Shoot Status</a> for more details).</p><p>Apart from the above, extension controllers can also contribute to the <code>status</code> or error <code>codes</code> of these conditions (see <a href="/docs/gardener/extensions/shoot-health-status-conditions/">Contributing to Shoot Health Status Conditions</a> for more details).</p><p>If all checks for a certain conditions are succeeded, then its <code>status</code> will be set to <code>True</code>. Otherwise, it will be set to <code>False</code>.</p><p>If at least one check fails and there is threshold configuration for the conditions (in <code>.controllers.seedCare.conditionThresholds</code>), then the status will be set:</p><ul><li>to <code>Progressing</code> if it was <code>True</code> before.</li><li>to <code>Progressing</code> if it was <code>Progressing</code> before and the <code>lastUpdateTime</code> of the condition does not exceed the configured threshold duration yet.</li><li>to <code>False</code> if it was <code>Progressing</code> before and the <code>lastUpdateTime</code> of the condition exceeds the configured threshold duration.</li></ul><p>The condition thresholds can be used to prevent reporting issues too early just because there is a rollout or a short disruption. Only if the unhealthiness persists for at least the configured threshold duration, then the issues will be reported (by setting the status to <code>False</code>).</p><p>Besides directly checking the status of <code>Deployment</code>s, <code>Etcd</code>s, <code>StatefulSet</code>s in the shoot namespace, this reconciler also considers <code>ManagedResource</code>s (in the shoot namespace) and their status in order to compute the condition statuses, see <a href="/docs/gardener/concepts/resource-manager/#conditions">this document</a> for more information. The following table explains which <code>ManagedResource</code>s are considered for which condition type:</p><table tabindex="0"><thead><tr><th>Condition Type</th><th><code>ManagedResource</code>s are considered when</th></tr></thead><tbody><tr><td><code>ControlPlaneHealthy</code></td><td><code>.spec.class=seed</code> and <code>care.gardener.cloud/condition-type</code> label either unset, or set to <code>ControlPlaneHealthy</code></td></tr><tr><td><code>ObservabilityComponentsHealthy</code></td><td><code>care.gardener.cloud/condition-type</code> label set to <code>ObservabilityComponentsHealthy</code></td></tr><tr><td><code>SystemComponentsHealthy</code></td><td><code>.spec.class</code> unset or <code>care.gardener.cloud/condition-type</code> label set to <code>SystemComponentsHealthy</code></td></tr></tbody></table><h5 id="constraints-and-automatic-webhook-remediation" tabindex="-1">Constraints And Automatic Webhook Remediation <a class="header-anchor" href="#constraints-and-automatic-webhook-remediation" aria-label="Permalink to &quot;Constraints And Automatic Webhook Remediation&quot;">​</a></h5><p>Please see <a href="/docs/gardener/shoot/shoot_status/#constraints">Shoot Status</a> for more details.</p><h5 id="garbage-collection" tabindex="-1">Garbage Collection <a class="header-anchor" href="#garbage-collection" aria-label="Permalink to &quot;Garbage Collection&quot;">​</a></h5><p>Stale pods in the shoot namespace in the seed cluster and in the <code>kube-system</code> namespace in the shoot cluster are deleted. A pod is considered stale when:</p><ul><li>it was terminated with reason <code>Evicted</code>.</li><li>it was terminated with reason starting with <code>OutOf</code> (e.g., <code>OutOfCpu</code>).</li><li>it was terminated with reason <code>NodeAffinity</code>.</li><li>it is stuck in termination (i.e., if its <code>deletionTimestamp</code> is more than <code>5m</code> ago).</li></ul><h4 id="lease-reconciler-1" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/lease" target="_blank" rel="noreferrer">&quot;Lease&quot; Reconciler</a> <a class="header-anchor" href="#lease-reconciler-1" aria-label="Permalink to &quot;[&quot;Lease&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/lease)&quot;">​</a></h4><p>This reconciler is only enabled for self-hosted shoot clusters. It checks whether the connection to the shoot cluster&#39;s <code>/healthz</code> endpoint works. If this succeeds, then it renews a <code>Lease</code> resource in the garden cluster in the same namespace as the <code>Shoot</code> the <code>gardenlet</code> is responsible for. This indicates a heartbeat to the external world, and internally the <code>gardenlet</code> sets its health status to <code>true</code>. In addition, the <code>GardenletReady</code> condition in the <code>status</code> of the <code>Shoot</code> is set to <code>True</code>. The whole process is similar to what the <code>kubelet</code> does to report heartbeats for its <code>Node</code> resource and its <code>KubeletReady</code> condition. For more information, see <a href="/docs/gardener/concepts/gardenlet/#heartbeats">this section</a>.</p><p>If the connection to the <code>/healthz</code> endpoint or the update of the <code>Lease</code> fails, then the internal health status of <code>gardenlet</code> is set to <code>false</code>. Also, this internal health status is set to <code>false</code> automatically after some time, in case the controller gets stuck for whatever reason. This internal health status is available via the <code>gardenlet</code>&#39;s <code>/healthz</code> endpoint and is used for the <code>livenessProbe</code> in the <code>gardenlet</code> pod.</p><h4 id="state-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/state" target="_blank" rel="noreferrer">&quot;State&quot; Reconciler</a> <a class="header-anchor" href="#state-reconciler" aria-label="Permalink to &quot;[&quot;State&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/state)&quot;">​</a></h4><p>This reconciler periodically (default: every <code>6h</code>) performs backups of the state of <code>Shoot</code> clusters and persists them into <code>ShootState</code> resources into the same namespace as the <code>Shoot</code>s in the garden cluster. It is only started in case the <code>gardenlet</code> is responsible for an unmanaged <code>Seed</code>, i.e. a <code>Seed</code> which is not backed by a <code>seedmanagement.gardener.cloud/v1alpha1.ManagedSeed</code> object. Alternatively, it can be disabled by setting the <code>concurrentSyncs=0</code> for the controller in the <code>gardenlet</code>&#39;s component configuration.</p><p>Please refer to <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/22-improved-usage-of-shootstate-api.md" target="_blank" rel="noreferrer">GEP-22: Improved Usage of the <code>ShootState</code> API</a> for all information.</p><h3 id="status-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/status" target="_blank" rel="noreferrer">&quot;Status&quot; Reconciler</a> <a class="header-anchor" href="#status-reconciler" aria-label="Permalink to &quot;[&quot;Status&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/shoot/status)&quot;">​</a></h3><p>This reconciler watches for the <code>extensionsv1alpha1.Worker</code> resource in the control plane namespace of the <code>Shoot</code> and if its <code>status.inPlaceUpdates.workerPoolToHashMap</code> has changed, it requeues the corresponding <code>Shoot</code>. A worker pool is removed from <code>status.inPlaceUpdates.pendingWorkersRollouts.manualInPlaceUpdate</code> field in the <code>Shoot</code> if the hash of the worker pool in the <code>Shoot</code> spec and the <code>Worker</code> status field matches. This indicates that all the nodes of that worker pool are successfully updated and are no longer pending manual in-place updates.</p><h3 id="tokenrequestor-controller-for-serviceaccounts" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controller/tokenrequestor" target="_blank" rel="noreferrer"><code>TokenRequestor</code> Controller For <code>ServiceAccount</code>s</a> <a class="header-anchor" href="#tokenrequestor-controller-for-serviceaccounts" aria-label="Permalink to &quot;[`TokenRequestor` Controller For `ServiceAccount`s](https://github.com/gardener/gardener/tree/master/pkg/controller/tokenrequestor)&quot;">​</a></h3><p>The <code>gardenlet</code> uses an instance of the <code>TokenRequestor</code> controller which initially was developed in the context of the <code>gardener-resource-manager</code>, please read <a href="/docs/gardener/concepts/resource-manager/#tokenrequestor-controller">this document</a> for further information.</p><p><code>gardenlet</code> uses it for requesting tokens for components running in the seed cluster that need to communicate with the garden cluster. The mechanism works the same way as for shoot control plane components running in the seed which need to communicate with the shoot cluster. However, <code>gardenlet</code>&#39;s instance of the <code>TokenRequestor</code> controller is restricted to <code>Secret</code>s labeled with <code>resources.gardener.cloud/class=garden</code>. Furthermore, it doesn&#39;t respect the <code>serviceaccount.resources.gardener.cloud/namespace</code> annotation. Instead, it always uses the seed&#39;s namespace in the garden cluster for managing <code>ServiceAccounts</code> and their tokens.</p><h3 id="tokenrequestor-controller-for-workloadidentitys" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/tokenrequestor/workloadidentity" target="_blank" rel="noreferrer"><code>TokenRequestor</code> Controller For <code>WorkloadIdentity</code>s</a> <a class="header-anchor" href="#tokenrequestor-controller-for-workloadidentitys" aria-label="Permalink to &quot;[`TokenRequestor` Controller For `WorkloadIdentity`s](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/tokenrequestor/workloadidentity)&quot;">​</a></h3><p>The <code>TokenRequestorWorkloadIdentity</code> controller in the <code>gardenlet</code> reconciles <code>Secret</code>s labeled with <code>security.gardener.cloud/purpose=workload-identity-token-requestor</code>. When it encounters such <code>Secret</code>, it associates the <code>Secret</code> with a specific <code>WorkloadIdentity</code> using the annotations <code>workloadidentity.security.gardener.cloud/name</code> and <code>workloadidentity.security.gardener.cloud/namespace</code>. Any workload creating such <code>Secret</code>s is responsible to label and annotate the <code>Secret</code>s accordingly. After the association is made, the <code>gardenlet</code> requests a token for the specific <code>WorkloadIdentity</code> from the Gardener API Server and writes it back in the <code>Secret</code>&#39;s data against the <code>token</code> key. The <code>gardenlet</code> is responsible to keep this token valid by refreshing it periodically. The token is then used by components running in the seed cluster in order to present the said <code>WorkloadIdentity</code> before external systems, e.g. by calling cloud provider APIs.</p><p>Please refer to <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/26-workload-identity.md" target="_blank" rel="noreferrer">GEP-26: Workload Identity - Trust Based Authentication</a> for more details.</p><h3 id="vpaevictionrequirements-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/vpaevictionrequirements" target="_blank" rel="noreferrer"><code>VPAEvictionRequirements</code> Controller</a> <a class="header-anchor" href="#vpaevictionrequirements-controller" aria-label="Permalink to &quot;[`VPAEvictionRequirements` Controller](https://github.com/gardener/gardener/tree/master/pkg/gardenlet/controller/vpaevictionrequirements)&quot;">​</a></h3><p>The <code>VPAEvictionRequirements</code> controller in the <code>gardenlet</code> reconciles <code>VerticalPodAutoscaler</code> objects labeled with <code>autoscaling.gardener.cloud/eviction-requirements: managed-by-controller</code>. It manages the <a href="https://github.com/kubernetes/autoscaler/tree/master/vertical-pod-autoscaler/enhancements/4831-control-eviction-behavior" target="_blank" rel="noreferrer"><code>EvictionRequirements</code></a> on a VPA object, which are used to restrict when and how a Pod can be evicted to apply a new resource recommendation. Specifically, the following actions will be taken for the respective label and annotation configuration:</p><ul><li>If the VPA has the annotation <code>eviction-requirements.autoscaling.gardener.cloud/downscale-restriction: never</code>, an <code>EvictionRequirement</code> is added to the VPA object that allows evictions for upscaling only</li><li>If the VPA has the annotation <code>eviction-requirements.autoscaling.gardener.cloud/downscale-restriction: in-maintenance-window-only</code>, the same <code>EvictionRequirement</code> is added to the VPA object when the Shoot is currently outside of its maintenance window. When the Shoot is inside its maintenance window, the <code>EvictionRequirement</code> is removed. Information about the Shoot maintenance window times are stored in the annotation <code>shoot.gardener.cloud/maintenance-window</code> on the VPA</li></ul><h2 id="managed-seeds" tabindex="-1">Managed Seeds <a class="header-anchor" href="#managed-seeds" aria-label="Permalink to &quot;Managed Seeds&quot;">​</a></h2><p>Gardener users can use shoot clusters as seed clusters, so-called &quot;managed seeds&quot; (aka &quot;shooted seeds&quot;), by creating <code>ManagedSeed</code> resources. By default, the gardenlet that manages this shoot cluster then automatically creates a clone of itself with the same version and the same configuration that it currently has. Then it deploys the gardenlet clone into the managed seed cluster.</p><p>For more information, see <a href="/docs/gardener/managed_seed/"><code>ManagedSeed</code>s: Register Shoot as Seed</a>.</p><h2 id="migrating-from-previous-gardener-versions" tabindex="-1">Migrating from Previous Gardener Versions <a class="header-anchor" href="#migrating-from-previous-gardener-versions" aria-label="Permalink to &quot;Migrating from Previous Gardener Versions&quot;">​</a></h2><p>If your Gardener version doesn’t support gardenlets yet, no special migration is required, but the following prerequisites must be met:</p><ul><li>Your Gardener version is at least 0.31 before upgrading to v1.</li><li>You have to make sure that your garden cluster is exposed in a way that it’s reachable from all your seed clusters.</li></ul><p>With previous Gardener versions, you had deployed the Gardener Helm chart (incorporating the API server, <code>controller-manager</code>, and scheduler). With v1, this stays the same, but you now have to deploy the gardenlet Helm chart as well into all of your seeds (if they aren’t managed, as mentioned earlier).</p><p>See <a href="/docs/gardener/deployment/deploy_gardenlet/">Deploy a gardenlet</a> for all instructions.</p><h2 id="related-links" tabindex="-1">Related Links <a class="header-anchor" href="#related-links" aria-label="Permalink to &quot;Related Links&quot;">​</a></h2><ul><li><a href="/docs/gardener/concepts/architecture/">Gardener Architecture</a></li><li><a href="https://github.com/gardener/gardener/issues/356" target="_blank" rel="noreferrer">#356: Implement Gardener Scheduler</a></li><li><a href="https://github.com/gardener/gardener/pull/2309" target="_blank" rel="noreferrer">#2309: Add /healthz endpoint for gardenlet</a></li></ul>',170)]))}const m=t(i,[["render",d]]);export{f as __pageData,m as default};

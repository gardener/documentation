import{_ as s,c as t,o as i,a2 as a}from"./chunks/framework.Bfq10Vlj.js";const p=JSON.parse('{"title":"Shoot Serviceaccounts","description":"","frontmatter":{"aliases":["/docs/gardener/shoot_serviceaccounts/"],"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/usage/security","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/security/shoot_serviceaccounts.md","to":"shoot_serviceaccounts.md"},"persona":"Users","title":"Shoot Serviceaccounts","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/security/shoot_serviceaccounts/index.md","filePath":"docs/gardener/security/shoot_serviceaccounts.md","lastUpdated":null}'),o={name:"docs/gardener/security/shoot_serviceaccounts/index.md"};function n(r,e,c,d,l,h){return i(),t("div",null,e[0]||(e[0]=[a(`<h1 id="serviceaccount-configurations-for-shoot-clusters" tabindex="-1"><code>ServiceAccount</code> Configurations for Shoot Clusters <a class="header-anchor" href="#serviceaccount-configurations-for-shoot-clusters" aria-label="Permalink to &quot;\`ServiceAccount\` Configurations for Shoot Clusters&quot;">​</a></h1><p>The <code>Shoot</code> specification allows to configure some of the settings for the handling of <code>ServiceAccount</code>s:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">core.gardener.cloud/v1beta1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Shoot</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  kubernetes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    kubeAPIServer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      serviceAccountConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        issuer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">foo</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        acceptedIssuers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">foo1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">foo2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        extendTokenExpiration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        maxTokenExpiration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">45d</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">...</span></span></code></pre></div><h2 id="issuer-and-accepted-issuers" tabindex="-1">Issuer and Accepted Issuers <a class="header-anchor" href="#issuer-and-accepted-issuers" aria-label="Permalink to &quot;Issuer and Accepted Issuers&quot;">​</a></h2><p>The <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.{issuer,acceptedIssuers}</code> fields are translated to the <code>--service-account-issuer</code> flag for the <code>kube-apiserver</code>. The issuer will assert its identifier in the <code>iss</code> claim of the issued tokens. According to the <a href="https://kubernetes.io/docs/reference/command-line-tools-reference/kube-apiserver/" target="_blank" rel="noreferrer">upstream specification</a>, values need to meet the following requirements:</p><blockquote><p>This value is a string or URI. If this option is not a valid URI per the OpenID Discovery 1.0 spec, the ServiceAccountIssuerDiscovery feature will remain disabled, even if the feature gate is set to true. It is highly recommended that this value comply with the OpenID spec: <a href="https://openid.net/specs/openid-connect-discovery-1_0.html" target="_blank" rel="noreferrer">https://openid.net/specs/openid-connect-discovery-1_0.html</a>. In practice, this means that service-account-issuer must be an https URL. It is also highly recommended that this URL be capable of serving OpenID discovery documents at {service-account-issuer}/.well-known/openid-configuration.</p></blockquote><p>By default, Gardener uses the internal cluster domain as issuer (e.g., <code>https://api.foo.bar.example.com</code>). If you specify the <code>issuer</code>, then this default issuer will always be part of the list of accepted issuers (you don&#39;t need to specify it yourself).</p><div class="caution custom-block github-alert"><p class="custom-block-title">CAUTION</p><p>If you change from the default issuer to a custom <code>issuer</code>, all previously issued tokens will still be valid/accepted. However, if you change from a custom <code>issuer</code> <code>A</code> to another <code>issuer</code> <code>B</code> (custom or default), then you have to add <code>A</code> to the <code>acceptedIssuers</code> so that previously issued tokens are not invalidated. Otherwise, the control plane components as well as system components and your workload pods might fail. You can remove <code>A</code> from the <code>acceptedIssuers</code> when all currently active tokens have been issued solely by <code>B</code>. This can be ensured by using <a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/#service-account-token-volume-projection" target="_blank" rel="noreferrer">projected token volumes</a> with a short validity, or by rolling out all pods. Additionally, all <a href="https://kubernetes.io/docs/concepts/configuration/secret/#service-account-token-secrets" target="_blank" rel="noreferrer"><code>ServiceAccount</code> token secrets</a> should be recreated. Apart from this, you should wait for at least <code>12h</code> to make sure the control plane and system components have received a new token from Gardener.</p></div><h2 id="token-expirations" tabindex="-1">Token Expirations <a class="header-anchor" href="#token-expirations" aria-label="Permalink to &quot;Token Expirations&quot;">​</a></h2><p>The <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.extendTokenExpiration</code> configures the <code>--service-account-extend-token-expiration</code> flag of the <code>kube-apiserver</code>. It is enabled by default and has the following specification:</p><blockquote><p>Turns on projected service account expiration extension during token generation, which helps safe transition from legacy token to bound service account token feature. If this flag is enabled, admission injected tokens would be extended up to 1 year to prevent unexpected failure during transition, ignoring value of service-account-max-token-expiration.</p></blockquote><p>The <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.maxTokenExpiration</code> configures the <code>--service-account-max-token-expiration</code> flag of the <code>kube-apiserver</code>. It has the following specification:</p><blockquote><p>The maximum validity duration of a token created by the service account token issuer. If an otherwise valid TokenRequest with a validity duration larger than this value is requested, a token will be issued with a validity duration of this value.</p></blockquote><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>The value for this field must be in the <code>[30d,90d]</code> range. The background for this limitation is that all Gardener components rely on the <code>TokenRequest</code> API and the Kubernetes service account token projection feature with short-lived, auto-rotating tokens. Any values lower than <code>30d</code> risk impacting the SLO for shoot clusters, and any values above <code>90d</code> violate security best practices with respect to maximum validity of credentials before they must be rotated. Given that the field just specifies the upper bound, end-users can still use lower values for their individual workload by specifying the <code>.spec.volumes[].projected.sources[].serviceAccountToken.expirationSeconds</code> in the <code>PodSpec</code>s.</p></div><h2 id="managed-service-account-issuer" tabindex="-1">Managed Service Account Issuer <a class="header-anchor" href="#managed-service-account-issuer" aria-label="Permalink to &quot;Managed Service Account Issuer&quot;">​</a></h2><p>Gardener also provides a way to manage the service account issuer of a shoot cluster as well as serving its OIDC discovery documents from a centrally managed server called <a href="https://github.com/gardener/gardener-discovery-server" target="_blank" rel="noreferrer">Gardener Discovery Server</a>. This ability removes the need for changing the <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.issuer</code> and exposing it separately.</p><h3 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h3><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>The following prerequisites are responsibility of the Gardener Administrators and are not something that end users can configure by themselves. If uncertain that these requirements are met, please contact your Gardener Administrator.</p></div><p>Prerequisites:</p><ul><li>The Garden Cluster should have the Gardener Discovery Server deployed and configured. The easiest way to handle this is by using the <a href="/docs/gardener/concepts/operator/#gardener-discovery-server">gardener-operator</a>.</li></ul><h3 id="enablement" tabindex="-1">Enablement <a class="header-anchor" href="#enablement" aria-label="Permalink to &quot;Enablement&quot;">​</a></h3><p>If the prerequisites are met then the feature can be enabled for a shoot cluster by annotating it with <code>authentication.gardener.cloud/issuer=managed</code>. Mind that once enabled, this feature cannot be disabled.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>After annotating the shoot with <code>authentication.gardener.cloud/issuer=managed</code> the reconciliation will not be triggered immediately. One can wait for the shoot maintenance window or trigger reconciliation by annotating the shoot with <code>gardener.cloud/operation=reconcile</code>.</p></div><p>After the shoot is reconciled, you can retrieve the new shoot service account issuer value from the shoot&#39;s status. A sample query that will retrieve the managed issuer looks like this:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kubectl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> shoot</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-shoot</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jsonpath=&#39;{.status.advertisedAddresses[?(@.name==&quot;service-account-issuer&quot;)].url}&#39;</span></span></code></pre></div><p>Once retrieved, the shoot&#39;s OIDC discovery documents can be explored by querying the <code>/.well-known/openid-configuration</code> endpoint of the issuer.</p><p>Mind that this annotation is incompatible with the <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.issuer</code> field, so if you want to enable it then the <code>issuer</code> field should not be set in the shoot specification.</p><div class="caution custom-block github-alert"><p class="custom-block-title">CAUTION</p><p>If you change from the default issuer to a managed issuer, all previously issued tokens will still be valid/accepted. However, if you change from a custom <code>issuer</code> <code>A</code> to a managed issuer, then you have to add <code>A</code> to the <code>.spec.kubernetes.kubeAPIServer.serviceAccountConfig.acceptedIssuers</code> so that previously issued tokens are not invalidated. Otherwise, the control plane components as well as system components and your workload pods might fail. You can remove <code>A</code> from the <code>acceptedIssuers</code> when all currently active tokens have been issued solely by the managed issuer. This can be ensured by using <a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/#service-account-token-volume-projection" target="_blank" rel="noreferrer">projected token volumes</a> with a short validity, or by rolling out all pods. Additionally, all <a href="https://kubernetes.io/docs/concepts/configuration/secret/#service-account-token-secrets" target="_blank" rel="noreferrer"><code>ServiceAccount</code> token secrets</a> should be recreated. Apart from this, you should wait for at least <code>12h</code> to make sure the control plane and system components have received a new token from Gardener.</p></div>`,28)]))}const k=s(o,[["render",n]]);export{p as __pageData,k as default};

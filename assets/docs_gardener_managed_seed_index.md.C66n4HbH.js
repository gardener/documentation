import{_ as a,c as t,o as s,a2 as o}from"./chunks/framework.Bfq10Vlj.js";const p=JSON.parse('{"title":"Managed Seed","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/operations","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/managed_seed.md","to":"managed_seed.md"},"persona":"Operators","title":"Managed Seed","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/managed_seed/index.md","filePath":"docs/gardener/managed_seed.md","lastUpdated":null}'),n={name:"docs/gardener/managed_seed/index.md"};function i(r,e,d,l,c,h){return s(),t("div",null,e[0]||(e[0]=[o(`<h1 id="managedseeds-register-shoot-as-seed" tabindex="-1"><code>ManagedSeed</code>s: Register Shoot as Seed <a class="header-anchor" href="#managedseeds-register-shoot-as-seed" aria-label="Permalink to &quot;\`ManagedSeed\`s: Register Shoot as Seed&quot;">​</a></h1><p>An existing shoot can be registered as a seed by creating a <code>ManagedSeed</code> resource. This resource contains:</p><ul><li>The name of the shoot that should be registered as seed.</li><li>A <code>gardenlet</code> section that contains: <ul><li><code>gardenlet</code> deployment parameters, such as the number of replicas, the image, etc.</li><li>The <code>GardenletConfiguration</code> resource that contains controllers configuration, feature gates, and a <code>seedConfig</code> section that contains the <code>Seed</code> spec and parts of its metadata.</li><li>Additional configuration parameters, such as the garden connection bootstrap mechanism (see <a href="/docs/gardener/concepts/gardenlet/#tls-bootstrapping">TLS Bootstrapping</a>), and whether to merge the provided configuration with the configuration of the parent <code>gardenlet</code>.</li></ul></li></ul><p><code>gardenlet</code> is deployed to the shoot, and it registers a new seed upon startup based on the <code>seedConfig</code> section.</p><blockquote><p><strong>Note:</strong> Earlier Gardener allowed specifying a <code>seedTemplate</code> directly in the <code>ManagedSeed</code> resource. This feature is discontinued, any seed configuration must be via the <code>GardenletConfiguration</code>.</p></blockquote><p>Note the following important aspects:</p><ul><li>Unlike the <code>Seed</code> resource, the <code>ManagedSeed</code> resource is namespaced. Currently, managed seeds are restricted to the <code>garden</code> namespace.</li><li>The newly created <code>Seed</code> resource always has the same name as the <code>ManagedSeed</code> resource. Attempting to specify a different name in the <code>seedConfig</code> will fail.</li><li>The <code>ManagedSeed</code> resource must always refer to an existing shoot. Attempting to create a <code>ManagedSeed</code> referring to a non-existing shoot will fail.</li><li>A shoot that is being referred to by a <code>ManagedSeed</code> cannot be deleted. Attempting to delete such a shoot will fail.</li><li>You can omit practically everything from the <code>gardenlet</code> section, including all or most of the <code>Seed</code> spec fields. Proper defaults will be supplied in all cases, based either on the most common use cases or the information already available in the <code>Shoot</code> resource.</li><li>Also, if your seed is configured to host HA shoot control planes, then <code>gardenlet</code> will be deployed with multiple replicas across nodes or availability zones by default.</li><li>Some <code>Seed</code> spec fields, for example the provider type and region, networking CIDRs for pods, services, and nodes, etc., must be the same as the corresponding <code>Shoot</code> spec fields of the shoot that is being registered as seed. Attempting to use different values (except empty ones, so that they are supplied by the defaulting mechanism) will fail.</li></ul><h2 id="deploying-gardenlet-to-the-shoot" tabindex="-1">Deploying gardenlet to the Shoot <a class="header-anchor" href="#deploying-gardenlet-to-the-shoot" aria-label="Permalink to &quot;Deploying gardenlet to the Shoot&quot;">​</a></h2><p>To register a shoot as a seed and deploy <code>gardenlet</code> to the shoot using a default configuration, create a <code>ManagedSeed</code> resource similar to the following:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">seedmanagement.gardener.cloud/v1alpha1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ManagedSeed</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-managed-seed</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">garden</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  shoot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">crazy-botany</span></span></code></pre></div><p>For an example that uses non-default configuration, see <a href="https://github.com/gardener/gardener/blob/master/example/55-managedseed-gardenlet.yaml" target="_blank" rel="noreferrer">55-managed-seed-gardenlet.yaml</a></p><h3 id="renewing-the-gardenlet-kubeconfig-secret" tabindex="-1">Renewing the Gardenlet Kubeconfig Secret <a class="header-anchor" href="#renewing-the-gardenlet-kubeconfig-secret" aria-label="Permalink to &quot;Renewing the Gardenlet Kubeconfig Secret&quot;">​</a></h3><p>In order to make the <code>ManagedSeed</code> controller renew the gardenlet&#39;s kubeconfig secret, annotate the <code>ManagedSeed</code> with <code>gardener.cloud/operation=renew-kubeconfig</code>. This will trigger a reconciliation during which the kubeconfig secret is deleted and the bootstrapping is performed again (during which gardenlet obtains a new client certificate).</p><p>It is also possible to trigger the renewal on the secret directly, see <a href="/docs/gardener/concepts/gardenlet/#rotate-certificates-using-bootstrap-kubeconfig">Rotate Certificates Using Bootstrap kubeconfig</a>.</p><h3 id="enforced-configuration-options" tabindex="-1">Enforced Configuration Options <a class="header-anchor" href="#enforced-configuration-options" aria-label="Permalink to &quot;Enforced Configuration Options&quot;">​</a></h3><p>The following configuration options are enforced by Gardener API server for the ManagedSeed resources:</p><ol><li><p>The vertical pod autoscaler should be enabled from the Shoot specification.</p><p>The vertical pod autoscaler is a prerequisite for a Seed cluster. It is possible to enable the VPA feature for a Seed <a href="/docs/gardener/seed_settings/#vertical-pod-autoscaler">(using the Seed spec)</a> and for a Shoot <a href="/docs/gardener/autoscaling/shoot_autoscaling/#vertical-pod-auto-scaling">(using the Shoot spec)</a>. In context of <code>ManagedSeed</code>s, enabling the VPA in the Seed spec (instead of the Shoot spec) offers less flexibility and increases the network transfer and cost. Due to these reasons, the Gardener API server enforces the vertical pod autoscaler to be enabled from the Shoot specification.</p></li><li><p>The nginx-ingress addon should not be enabled for a Shoot referred by a ManagedSeed.</p><p>An Ingress controller is also a prerequisite for a Seed cluster. For a Seed cluster, it is possible to enable Gardener managed Ingress controller or to deploy self-managed Ingress controller. There is also the nginx-ingress addon that can be enabled for a Shoot (using the Shoot spec). However, the Shoot nginx-ingress addon is in deprecated mode and it is not recommended for production clusters. Due to these reasons, the Gardener API server does not allow the Shoot nginx-ingress addon to be enabled for ManagedSeeds.</p></li></ol>`,17)]))}const u=a(n,[["render",i]]);export{p as __pageData,u as default};

import{_ as t,c as o,o as s,a2 as a}from"./chunks/framework.Bfq10Vlj.js";const u=JSON.parse('{"title":"Shoot Status","description":"Shoot conditions, constraints, and error codes","frontmatter":{"aliases":["/docs/gardener/shoot_status/"],"description":"Shoot conditions, constraints, and error codes","github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/usage/shoot","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/shoot/shoot_status.md","to":"shoot_status.md"},"persona":"Users","title":"Shoot Status","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/shoot/shoot_status/index.md","filePath":"docs/gardener/shoot/shoot_status.md","lastUpdated":null}'),i={name:"docs/gardener/shoot/shoot_status/index.md"};function r(n,e,d,c,l,h){return s(),o("div",null,e[0]||(e[0]=[a(`<h1 id="shoot-status" tabindex="-1">Shoot Status <a class="header-anchor" href="#shoot-status" aria-label="Permalink to &quot;Shoot Status&quot;">​</a></h1><p>This document provides an overview of the <a href="/docs/gardener/api-reference/core/#shootstatus">ShootStatus</a>.</p><h2 id="conditions" tabindex="-1">Conditions <a class="header-anchor" href="#conditions" aria-label="Permalink to &quot;Conditions&quot;">​</a></h2><p>The Shoot status consists of a set of conditions. A <a href="/docs/gardener/api-reference/core/#condition">Condition</a> has the following fields:</p><table tabindex="0"><thead><tr><th>Field name</th><th>Description</th></tr></thead><tbody><tr><td><code>type</code></td><td>Name of the condition.</td></tr><tr><td><code>status</code></td><td>Indicates whether the condition is applicable, with possible values <code>True</code>, <code>False</code>, <code>Unknown</code> or <code>Progressing</code>.</td></tr><tr><td><code>lastTransitionTime</code></td><td>Timestamp for when the condition last transitioned from one status to another.</td></tr><tr><td><code>lastUpdateTime</code></td><td>Timestamp for when the condition was updated. Usually changes when <code>reason</code> or <code>message</code> in condition is updated.</td></tr><tr><td><code>reason</code></td><td>Machine-readable, UpperCamelCase text indicating the reason for the condition&#39;s last transition.</td></tr><tr><td><code>message</code></td><td>Human-readable message indicating details about the last status transition.</td></tr><tr><td><code>codes</code></td><td>Well-defined error codes in case the condition reports a problem.</td></tr></tbody></table><p>Currently, the available Shoot condition types are:</p><ul><li><code>APIServerAvailable</code></li><li><code>ControlPlaneHealthy</code></li><li><code>EveryNodeReady</code></li><li><code>ObservabilityComponentsHealthy</code></li><li><code>SystemComponentsHealthy</code></li></ul><p>The Shoot conditions are maintained by the <a href="https://github.com/gardener/gardener/blob/master/pkg/gardenlet/controller/shoot/care/reconciler.go" target="_blank" rel="noreferrer">shoot care reconciler</a> of the gardenlet. Find more information in the <a href="/docs/gardener/concepts/gardenlet/#shoot-controller">gardelent documentation</a>.</p><h3 id="sync-period" tabindex="-1">Sync Period <a class="header-anchor" href="#sync-period" aria-label="Permalink to &quot;Sync Period&quot;">​</a></h3><p>The condition checks are executed periodically at an interval which is configurable in the <code>GardenletConfiguration</code> (<code>.controllers.shootCare.syncPeriod</code>, defaults to <code>1m</code>).</p><h3 id="condition-thresholds" tabindex="-1">Condition Thresholds <a class="header-anchor" href="#condition-thresholds" aria-label="Permalink to &quot;Condition Thresholds&quot;">​</a></h3><p>The <code>GardenletConfiguration</code> also allows configuring condition thresholds (<code>controllers.shootCare.conditionThresholds</code>). A condition threshold is the amount of time to consider a condition as <code>Processing</code> on condition status changes.</p><p>Let&#39;s check the following example to get a better understanding. Let&#39;s say that the <code>APIServerAvailable</code> condition of our Shoot is with status <code>True</code>. If the next condition check fails (for example kube-apiserver becomes unreachable), then the condition first goes to <code>Processing</code> state. Only if this state remains for condition threshold amount of time, then the condition is finally updated to <code>False</code>.</p><h3 id="constraints" tabindex="-1">Constraints <a class="header-anchor" href="#constraints" aria-label="Permalink to &quot;Constraints&quot;">​</a></h3><p>Constraints represent conditions of a Shoot’s current state that constraint some operations on it. The current constraints are:</p><p><strong><code>HibernationPossible</code></strong>:</p><p>This constraint indicates whether a Shoot is allowed to be hibernated. The rationale behind this constraint is that a Shoot can have <code>ValidatingWebhookConfiguration</code>s or <code>MutatingWebhookConfiguration</code>s acting on resources that are critical for waking up a cluster. For example, if a webhook has rules for <code>CREATE/UPDATE</code> Pods or Nodes and <code>failurePolicy=Fail</code>, the webhook will block joining <code>Nodes</code> and creating critical system component Pods and thus block the entire wakeup operation, because the server backing the webhook is not running.</p><p>Even if the <code>failurePolicy</code> is set to <code>Ignore</code>, high timeouts (<code>&gt;15s</code>) can lead to blocking requests of control plane components. That&#39;s because most control-plane API calls are made with a client-side timeout of <code>30s</code>, so if a webhook has <code>timeoutSeconds=30</code> the overall request might still fail as there is overhead in communication with the API server and potential other webhooks.</p><p>Generally, it&#39;s <a href="https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#timeouts" target="_blank" rel="noreferrer">best practice</a> to specify low timeouts in WebhookConfigs.</p><p>As an effort to correct this common problem, the webhook remediator has been created. This is enabled by setting <code>.controllers.shootCare.webhookRemediatorEnabled=true</code> in the <code>gardenlet</code>&#39;s configuration. This feature simply checks whether webhook configurations in shoot clusters match a set of rules described <a href="https://github.com/gardener/gardener/blob/master/pkg/gardenlet/operation/botanist/matchers/matcher.go" target="_blank" rel="noreferrer">here</a>. If at least one of the rules matches, it will change set <code>status=False</code> for the <code>.status.constraints</code> of type <code>HibernationPossible</code> and <code>MaintenancePreconditionsSatisfied</code> in the <code>Shoot</code> resource. In addition, the <code>failurePolicy</code> in the affected webhook configurations will be set from <code>Fail</code> to <code>Ignore</code>. Gardenlet will also add an annotation to make it visible to end-users that their webhook configurations were mutated and should be fixed/adapted according to the rules and best practices.</p><p>In most cases, you can avoid this by simply excluding the <code>kube-system</code> namespace from your webhook via the <code>namespaceSelector</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">admissionregistration.k8s.io/v1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">MutatingWebhookConfiguration</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">webhooks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-webhook.example.com</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    namespaceSelector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      matchExpressions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gardener.cloud/purpose</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">NotIn</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        values</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kube-system</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    rules</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">operations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        apiGroups</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        apiVersions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;v1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        resources</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pods&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        scope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Namespaced&quot;</span></span></code></pre></div><p>However, some other resources (some of them cluster-scoped) might still trigger the remediator, namely:</p><ul><li>endpoints</li><li>nodes</li><li>clusterroles</li><li>clusterrolebindings</li><li>customresourcedefinitions</li><li>apiservices</li><li>certificatesigningrequests</li><li>priorityclasses</li></ul><p>If one of the above resources triggers the remediator, the preferred solution is to remove that particular resource from your webhook&#39;s <code>rules</code>. You can also use the <code>objectSelector</code> to reduce the scope of webhook&#39;s <code>rules</code>. However, in special cases where a webhook is absolutely needed for the workload, it is possible to add the <code>remediation.webhook.shoot.gardener.cloud/exclude=true</code> label to your webhook so that the remediator ignores it. This label <strong>should not be used to silence an alert</strong>, but rather to confirm that a webhook won&#39;t cause problems. Note that all of this is no perfect solution and just done on a best effort basis, and only the owner of the webhook can know whether it indeed is problematic and configured correctly.</p><p>In a special case, if a webhook has a rule for <code>CREATE/UPDATE</code> lease resources in <code>kube-system</code> namespace, its <code>timeoutSeconds</code> is updated to 3 seconds. This is required to ensure the proper functioning of the leader election of essential control plane controllers.</p><p>You can also find more help from the <a href="https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#best-practices-and-warnings" target="_blank" rel="noreferrer">Kubernetes documentation</a></p><p><strong><code>MaintenancePreconditionsSatisfied</code></strong>:</p><p>This constraint indicates whether all preconditions for a safe maintenance operation are satisfied (see <a href="/docs/gardener/shoot/shoot_maintenance/">Shoot Maintenance</a> for more information about what happens during a shoot maintenance). As of today, the same checks as in the <code>HibernationPossible</code> constraint are being performed (user-deployed webhooks that might interfere with potential rolling updates of shoot worker nodes). There is no further action being performed on this constraint&#39;s status (maintenance is still being performed). It is meant to make the user aware of potential problems that might occur due to his configurations.</p><p><strong><code>CACertificateValiditiesAcceptable</code></strong>:</p><p>This constraint indicates that there is at least one CA certificate which expires in less than <code>1y</code>. It will not be added to the <code>.status.constraints</code> if there is no such CA certificate. However, if it&#39;s visible, then a <a href="/docs/gardener/shoot-operations/shoot_credentials_rotation/#certificate-authorities">credentials rotation operation</a> should be considered.</p><p><strong><code>CRDsWithProblematicConversionWebhooks</code></strong>:</p><p>This constraint indicates that there is at least one <code>CustomResourceDefinition</code> in the cluster which has multiple stored versions and a conversion webhook configured. This could break the reconciliation flow of a <code>Shoot</code> cluster in some cases. See <a href="https://github.com/gardener/gardener/issues/7471" target="_blank" rel="noreferrer">https://github.com/gardener/gardener/issues/7471</a> for more details. It will not be added to the <code>.status.constraints</code> if there is no such CRD. However, if it&#39;s visible, then you should consider upgrading the existing objects to the current stored version. See <a href="https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definition-versioning/#upgrade-existing-objects-to-a-new-stored-version" target="_blank" rel="noreferrer">Upgrade existing objects to a new stored version</a> for detailed steps.</p><p><strong><code>ManualInPlaceWorkersUpdated</code></strong>:</p><p>This constraint indicates that at least one worker pool with the update strategy <code>ManualInPlaceUpdate</code> is pending an update. Despite this, the <code>Shoot</code> reconciliation will still succeed. The constraint is not added to <code>.status.constraints</code> if all such worker pools are already up-to-date. Once the user manually labels all the relevant nodes with <code>node.machine.sapcloud.io/selected-for-update</code> and the update process completes, the constraint will be automatically removed.</p><h3 id="last-operation" tabindex="-1">Last Operation <a class="header-anchor" href="#last-operation" aria-label="Permalink to &quot;Last Operation&quot;">​</a></h3><p>The Shoot status holds information about the last operation that is performed on the Shoot. The last operation field reflects overall progress and the tasks that are currently being executed. Allowed operation types are <code>Create</code>, <code>Reconcile</code>, <code>Delete</code>, <code>Migrate</code>, and <code>Restore</code>. Allowed operation states are <code>Processing</code>, <code>Succeeded</code>, <code>Error</code>, <code>Failed</code>, <code>Pending</code>, and <code>Aborted</code>. An operation in <code>Error</code> state is an operation that will be retried for a configurable amount of time (<code>controllers.shoot.retryDuration</code> field in <code>GardenletConfiguration</code>, defaults to <code>12h</code>). If the operation cannot complete successfully for the configured retry duration, it will be marked as <code>Failed</code>. An operation in <code>Failed</code> state is an operation that won&#39;t be retried automatically (to retry such an operation, see <a href="/docs/gardener/shoot-operations/shoot_operations/#retry-failed-operation">Retry failed operation</a>).</p><h3 id="last-errors" tabindex="-1">Last Errors <a class="header-anchor" href="#last-errors" aria-label="Permalink to &quot;Last Errors&quot;">​</a></h3><p>The Shoot status also contains information about the last occurred error(s) (if any) during an operation. A <a href="/docs/gardener/api-reference/core/#lasterror">LastError</a> consists of identifier of the task returned error, human-readable message of the error and error codes (if any) associated with the error.</p><h3 id="error-codes" tabindex="-1">Error Codes <a class="header-anchor" href="#error-codes" aria-label="Permalink to &quot;Error Codes&quot;">​</a></h3><p>Known error codes and their classification are:</p><table tabindex="0"><thead><tr><th>Error code</th><th style="text-align:center;">User error</th><th>Description</th></tr></thead><tbody><tr><td><code>ERR_INFRA_UNAUTHENTICATED</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to the client request not being completed because it lacks valid authentication credentials for the requested resource. It is classified as a non-retryable error code.</td></tr><tr><td><code>ERR_INFRA_UNAUTHORIZED</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to the server understanding the request but refusing to authorize it. It is classified as a non-retryable error code.</td></tr><tr><td><code>ERR_INFRA_QUOTA_EXCEEDED</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to infrastructure quota limits. It is classified as a non-retryable error code.</td></tr><tr><td><code>ERR_INFRA_RATE_LIMITS_EXCEEDED</code></td><td style="text-align:center;">false</td><td>Indicates that the last error occurred due to exceeded infrastructure request rate limits.</td></tr><tr><td><code>ERR_INFRA_DEPENDENCIES</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to dependent objects on the infrastructure level. It is classified as a non-retryable error code.</td></tr><tr><td><code>ERR_RETRYABLE_INFRA_DEPENDENCIES</code></td><td style="text-align:center;">false</td><td>Indicates that the last error occurred due to dependent objects on the infrastructure level, but the operation should be retried.</td></tr><tr><td><code>ERR_INFRA_RESOURCES_DEPLETED</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to depleted resource in the infrastructure.</td></tr><tr><td><code>ERR_CLEANUP_CLUSTER_RESOURCES</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to resources in the cluster that are stuck in deletion.</td></tr><tr><td><code>ERR_CONFIGURATION_PROBLEM</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to a configuration problem. It is classified as a non-retryable error code.</td></tr><tr><td><code>ERR_RETRYABLE_CONFIGURATION_PROBLEM</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to a retryable configuration problem. &quot;Retryable&quot; means that the occurred error is likely to be resolved in a ungraceful manner after given period of time.</td></tr><tr><td><code>ERR_PROBLEMATIC_WEBHOOK</code></td><td style="text-align:center;">true</td><td>Indicates that the last error occurred due to a webhook not following the <a href="https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/#best-practices-and-warnings" target="_blank" rel="noreferrer">Kubernetes best practices</a>.</td></tr></tbody></table><p><strong>Please note:</strong> Errors classified as <code>User error: true</code> do not require a Gardener operator to resolve but can be remediated by the user (e.g. by refreshing expired infrastructure credentials). Even though <code>ERR_INFRA_RATE_LIMITS_EXCEEDED</code> and <code>ERR_RETRYABLE_INFRA_DEPENDENCIES</code> is mentioned as User error: false\` operator can&#39;t provide any resolution because it is related to cloud provider issue.</p><h3 id="status-label" tabindex="-1">Status Label <a class="header-anchor" href="#status-label" aria-label="Permalink to &quot;Status Label&quot;">​</a></h3><p>Shoots will be automatically labeled with the <code>shoot.gardener.cloud/status</code> label. Its value might either be <code>healthy</code>, <code>progressing</code>, <code>unhealthy</code> or <code>unknown</code> depending on the <code>.status.conditions</code>, <code>.status.lastOperation</code>, and <code>status.lastErrors</code> of the <code>Shoot</code>. This can be used as an easy filter method to find shoots based on their &quot;health&quot; status.</p>`,45)]))}const g=t(i,[["render",r]]);export{u as __pageData,g as default};

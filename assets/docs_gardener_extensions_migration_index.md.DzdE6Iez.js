import{_ as t,c as s,o as a,a2 as n}from"./chunks/framework.Bfq10Vlj.js";const u=JSON.parse('{"title":"Migration","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/extensions","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/extensions/migration.md","to":"migration.md"},"title":"Migration","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/extensions/migration/index.md","filePath":"docs/gardener/extensions/migration.md","lastUpdated":null}'),o={name:"docs/gardener/extensions/migration/index.md"};function r(i,e,l,c,d,h){return a(),s("div",null,e[0]||(e[0]=[n(`<h1 id="control-plane-migration" tabindex="-1">Control Plane Migration <a class="header-anchor" href="#control-plane-migration" aria-label="Permalink to &quot;Control Plane Migration&quot;">​</a></h1><p><em>Control Plane Migration</em> is a new Gardener feature that has been recently implemented as proposed in <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/07-shoot-control-plane-migration.md" target="_blank" rel="noreferrer">GEP-7 Shoot Control Plane Migration</a>. It should be properly supported by all extensions controllers. This document outlines some important points that extension maintainers should keep in mind to properly support migration in their extensions.</p><h2 id="overall-principles" tabindex="-1">Overall Principles <a class="header-anchor" href="#overall-principles" aria-label="Permalink to &quot;Overall Principles&quot;">​</a></h2><p>The following principles should always be upheld:</p><ul><li>All states maintained by the extension that is external from the seed cluster, for example infrastructure resources in a cloud provider, DNS entries, etc., should be kept during the migration. No such state should be deleted and then recreated, as this might cause disruption in the availability of the shoot cluster.</li><li>All Kubernetes resources maintained by the extension in the shoot cluster itself should also be kept during the migration. No such resources should be deleted and then recreated.</li></ul><h2 id="migrate-and-restore-operations" tabindex="-1">Migrate and Restore Operations <a class="header-anchor" href="#migrate-and-restore-operations" aria-label="Permalink to &quot;Migrate and Restore Operations&quot;">​</a></h2><p>Two new operations have been introduced in Gardener. They can be specified as values of the <code>gardener.cloud/operation</code> annotation on an extension resource to indicate that an operation different from a normal <code>reconcile</code> should be performed by the corresponding extension controller:</p><ul><li>The <code>migrate</code> operation is used to ask the extension controller in the source seed to stop reconciling extension resources (in case they are requeued due to errors) and perform cleanup activities, if such are required. These cleanup activities might involve removing finalizers on resources in the shoot namespace that have been previously created by the extension controller and deleting them without actually deleting any resources external to the seed cluster. This is also the last opportunity for extensions to persist their state into the <code>.status.state</code> field of the reconciled extension resource before its restored in the new destination seed cluster.</li><li>The <code>restore</code> operation is used to ask the extension controller in the destination seed to restore any state saved in the extension resource <code>status</code>, before performing the actual reconciliation.</li></ul><p>Unlike the <a href="/docs/gardener/extensions/reconcile-trigger/">reconcile operation</a>, extension controllers must remove the <code>gardener.cloud/operation</code> annotation at the end of a successful reconciliation when the current operation is <code>migrate</code> or <code>restore</code>, not at the beginning of a reconciliation.</p><h2 id="cleaning-up-source-seed-resources" tabindex="-1">Cleaning-Up Source Seed Resources <a class="header-anchor" href="#cleaning-up-source-seed-resources" aria-label="Permalink to &quot;Cleaning-Up Source Seed Resources&quot;">​</a></h2><p>All resources in the source seed that have been created by an extension controller, for example secrets, config maps, <a href="/docs/gardener/extensions/managedresources/">managed resources</a>, etc., should be properly cleaned up by the extension controller when the current operation is <code>migrate</code>. As mentioned above, such resources should be deleted without actually deleting any resources external to the seed cluster.</p><p>There is one exception to this: <code>Secret</code>s labeled with <code>persist=true</code> created via the <a href="/docs/gardener/secrets_management/">secrets manager</a>. They should be kept (i.e., the <code>Cleanup</code> function of secrets manager should not be called) and will be garbage collected automatically at the end of the <code>migrate</code> operation. This ensures that they can be properly persisted in the <code>ShootState</code> resource and get restored on the new destination seed cluster.</p><p>For many custom resources, for example MCM resources, the above requirement means in practice that any finalizers should be removed before deleting the resource, in addition to ensuring that the resource deletion is not reconciled by its respective controller if there is no finalizer. For managed resources, the above requirement means in practice that the <code>spec.keepObjects</code> field should be set to <code>true</code> before deleting the extension resource.</p><p>Here it is assumed that any resources that contain state needed by the extension controller can be safely deleted, since any such state has been saved as described in <a href="/docs/gardener/extensions/migration/#saving-and-restoring-extension-states">Saving and Restoring Extension States</a> at the end of the last successful reconciliation.</p><h2 id="saving-and-restoring-extension-states" tabindex="-1">Saving and Restoring Extension States <a class="header-anchor" href="#saving-and-restoring-extension-states" aria-label="Permalink to &quot;Saving and Restoring Extension States&quot;">​</a></h2><p>Some extension controllers create and maintain their own state when reconciling extension resources. For example, most infrastructure controllers use Terraform and maintain the terraform state in a special config map in the shoot namespace. This state must be properly migrated to the new seed cluster during control plane migration, so that subsequent reconciliations in the new seed could find and use it appropriately.</p><p>All extension controllers that require such state migration must save their state in the <code>status.state</code> field of their extension resource at the end of a successful reconciliation. They must also restore their state from that same field upon reconciling an extension resource when the current operation is <code>restore</code>, as specified by the <code>gardener.cloud/operation</code> annotation, before performing the actual reconciliation.</p><p>As an example, an infrastructure controller that uses Terraform must save the terraform state in the <code>status.state</code> field of the <code>Infrastructure</code> resource. An <code>Infrastructure</code> resource with a properly saved state might look as follows:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">extensions.gardener.cloud/v1alpha1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Infrastructure</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">infrastructure</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">shoot--foo--bar</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">azure</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  region</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">eu-west-1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  secretRef</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cloudprovider</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">shoot--foo--bar</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  providerConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">azure.provider.extensions.gardener.cloud/v1alpha1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">InfrastructureConfig</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    resourceGroup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mygroup</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    ...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  state</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;version&quot;: 3,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;terraform_version&quot;: &quot;0.11.14&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;serial&quot;: 2,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;lineage&quot;: &quot;3a1e2faa-e7b6-f5f0-5043-368dd8ea6c10&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      ...</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    }</span></span></code></pre></div><p>Extension controllers that do not use a saved state and therefore do not require state migration could leave the <code>status.state</code> field as <code>nil</code> at the end of a successful reconciliation, and just perform a normal reconciliation when the current operation is <code>restore</code>.</p><p>In addition, extension controllers that use <a href="/docs/gardener/extensions/referenced-resources/">referenced resources</a> (usually secrets) must also make sure that these resources are added to the <code>status.resources</code> field of their extension resource at the end of a successful reconciliation, so they could be properly migrated by Gardener to the destination seed.</p><h2 id="implementation-details" tabindex="-1">Implementation Details <a class="header-anchor" href="#implementation-details" aria-label="Permalink to &quot;Implementation Details&quot;">​</a></h2><h3 id="migrate-and-restore-actuator-methods" tabindex="-1">Migrate and Restore Actuator Methods <a class="header-anchor" href="#migrate-and-restore-actuator-methods" aria-label="Permalink to &quot;Migrate and Restore Actuator Methods&quot;">​</a></h3><p>Most extension controller implementations follow a common pattern where a generic <code>Reconciler</code> implementation delegates to an <code>Actuator</code> interface that contains the methods <code>Reconcile</code> and <code>Delete</code>, provided by the extension. Two methods <code>Migrate</code> and <code>Restore</code> are available in all such <code>Actuator</code> interfaces, see <a href="https://github.com/gardener/gardener/blob/master/extensions/pkg/controller/infrastructure/actuator.go" target="_blank" rel="noreferrer">the infrastructure <code>Actuator</code> interface</a> as an example. These methods are called by the generic reconcilers for the <a href="/docs/gardener/extensions/migration/#migrate-and-restore-operations">migrate and restore operations</a> respectively, and should be implemented by the extension according to the above guidelines.</p><h3 id="extension-controllers-based-on-generic-actuators" tabindex="-1">Extension Controllers Based on Generic Actuators <a class="header-anchor" href="#extension-controllers-based-on-generic-actuators" aria-label="Permalink to &quot;Extension Controllers Based on Generic Actuators&quot;">​</a></h3><p>In practice, the implementation of many extension controllers (for example, the <code>ControlPlane</code> and <code>Worker</code> controllers in most provider extensions) are based on a <em>generic <code>Actuator</code> implementation</em> that only delegates to extension methods for behavior that is truly provider specific. In all such cases, the <code>Migrate</code> and <code>Restore</code> methods have already been implemented properly in the generic actuators and there is nothing more to do in the extension itself.</p><p>In some rare cases, extension controllers based on a generic actuator might still introduce a custom <code>Actuator</code> implementation to override some of the generic actuator methods in order to enhance or change their behavior in a certain way. In such cases, the <code>Migrate</code> and <code>Restore</code> methods might need to be overridden as well, see the <a href="https://github.com/gardener/gardener-extension-provider-azure/tree/master/pkg/controller/controlplane" target="_blank" rel="noreferrer">Azure controlplane controller</a> as an example.</p><h4 id="worker-state" tabindex="-1"><code>Worker</code> State <a class="header-anchor" href="#worker-state" aria-label="Permalink to &quot;\`Worker\` State&quot;">​</a></h4><p>Note that the machine state is handled specially by <code>gardenlet</code> (i.e., all relevant objects in the <code>machine.sapcloud.io/v1alpha1</code> API are directly persisted by <code>gardenlet</code> and <strong>NOT</strong> by the generic actuators). In the past, they were persisted to the <code>Worker</code>&#39;s <code>.status.state</code> field by the so-called &quot;worker state reconciler&quot;, however, this reconciler was dropped and changed as part of <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/22-improved-usage-of-shootstate-api.md#eliminating-the-worker-state-reconciler" target="_blank" rel="noreferrer">GEP-22</a>. Nowadays, <code>gardenlet</code> directly writes the state to the <code>ShootState</code> resource during the <code>Migrate</code> phase of a <code>Shoot</code> (without the detour of the <code>Worker</code>&#39;s <code>.status.state</code> field). On restoration, unlike for other extension kinds, <code>gardenlet</code> no longer populates the machine state into the <code>Worker</code>&#39;s <code>.status.state</code> field. Instead, the extension controller should read the machine state directly from the <code>ShootState</code> in the garden cluster (see <a href="/docs/gardener/extensions/garden-api-access/">this document</a> for information how to access the garden cluster) and use it to subsequently restore the relevant <code>machine.sapcloud.io/v1alpha1</code> resources. This flow is implemented in the <a href="https://github.com/gardener/gardener/blob/master/extensions/pkg/controller/worker/genericactuator/actuator_restore.go" target="_blank" rel="noreferrer">generic <code>Worker</code> actuator</a>. As a result, Extension controllers using this generic actuator do not need to implement any custom logic.</p><h3 id="extension-controllers-not-based-on-generic-actuators" tabindex="-1">Extension Controllers Not Based on Generic Actuators <a class="header-anchor" href="#extension-controllers-not-based-on-generic-actuators" aria-label="Permalink to &quot;Extension Controllers Not Based on Generic Actuators&quot;">​</a></h3><p>The implementation of some extension controllers (for example, the infrastructure controllers in all provider extensions) are not based on a generic <code>Actuator</code> implementation. Such extension controllers must always provide a proper implementation of the <code>Migrate</code> and <code>Restore</code> methods according to the above guidelines, see the <a href="https://github.com/gardener/gardener-extension-provider-aws/tree/master/pkg/controller/infrastructure" target="_blank" rel="noreferrer">AWS infrastructure controller</a> as an example. In practice, this might result in code duplication between the different extensions, since the <code>Migrate</code> and <code>Restore</code> code is usually not provider or OS-specific.</p><blockquote><p>If you do not use the generic <code>Worker</code> actuator, see <a href="/docs/gardener/extensions/migration/#worker-state">this section</a> for information how to handle the machine state related to the <code>Worker</code> resource.</p></blockquote>`,32)]))}const g=t(o,[["render",r]]);export{u as __pageData,g as default};

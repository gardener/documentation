import{_ as o,c as t,o as d,a2 as s}from"./chunks/framework.Bfq10Vlj.js";const c="/assets/gardenlet_api_access_flow.Cpvcc8tu.png",a="/assets/gardenlet_api_access_graph.DzBcVQjw.png",f=JSON.parse('{"title":"Scoped API Access for gardenlets and Extensions","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/deployment","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/deployment/gardenlet_api_access.md","to":"gardenlet_api_access.md"},"title":"Scoped API Access for gardenlets and Extensions","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/deployment/gardenlet_api_access/index.md","filePath":"docs/gardener/deployment/gardenlet_api_access.md","lastUpdated":null}'),n={name:"docs/gardener/deployment/gardenlet_api_access/index.md"};function r(i,e,l,h,p,g){return d(),t("div",null,e[0]||(e[0]=[s('<h1 id="scoped-api-access-for-gardenlets-and-extensions" tabindex="-1">Scoped API Access for gardenlets and Extensions <a class="header-anchor" href="#scoped-api-access-for-gardenlets-and-extensions" aria-label="Permalink to &quot;Scoped API Access for gardenlets and Extensions&quot;">​</a></h1><p>By default, <code>gardenlet</code>s have administrative access in the garden cluster. They are able to execute any API request on any object independent of whether the object is related to the seed cluster the <code>gardenlet</code> is responsible for. As RBAC is not powerful enough for fine-grained checks and for the sake of security, Gardener provides two optional but recommended configurations for your environments that scope the API access for <code>gardenlet</code>s.</p><p>Similar to the <a href="https://kubernetes.io/docs/reference/access-authn-authz/node/" target="_blank" rel="noreferrer"><code>Node</code> authorization mode in Kubernetes</a>, Gardener features a <code>SeedAuthorizer</code> plugin. It is a special-purpose authorization plugin that specifically authorizes API requests made by the <code>gardenlet</code>s.</p><p>Likewise, similar to the <a href="https://kubernetes.io/docs/reference/access-authn-authz/admission-controllers/#noderestriction" target="_blank" rel="noreferrer"><code>NodeRestriction</code> admission plugin in Kubernetes</a>, Gardener features a <code>SeedRestriction</code> plugin. It is a special-purpose admission plugin that specifically limits the Kubernetes objects <code>gardenlet</code>s can modify.</p><p>📚 You might be interested to look into the <a href="https://github.com/kubernetes/design-proposals-archive/blob/main/node/kubelet-authorizer.md" target="_blank" rel="noreferrer">design proposal for scoped Kubelet API access</a> from the Kubernetes community. It can be translated to Gardener and Gardenlets with their <code>Seed</code> and <code>Shoot</code> resources.</p><p>Historically, <code>gardenlet</code> has been the only component running in the seed cluster that has access to both the seed cluster and the garden cluster. Starting from Gardener <a href="https://github.com/gardener/gardener/releases/v1.74.0" target="_blank" rel="noreferrer"><code>v1.74.0</code></a>, extensions running on seed clusters can also get <a href="/docs/gardener/extensions/garden-api-access/">access to the garden cluster</a> using a token for a dedicated ServiceAccount. Extensions using this mechanism only get permission to read global resources like <code>CloudProfiles</code> (this is granted to all authenticated users) unless the plugins described in this document are enabled.</p><p>Generally, the plugins handle extension clients exactly like gardenlet clients with some minor <a href="/docs/gardener/deployment/gardenlet_api_access/#rule-exceptions-for-extension-clients">exceptions</a>. Extension clients in the sense of the plugins are clients authenticated as a <code>ServiceAccount</code> with the <code>extension-</code> name prefix in a <code>seed-</code> namespace of the garden cluster. Other <code>ServiceAccounts</code> are not considered as seed clients, not handled by the plugins, and only get the described read access to global resources.</p><h2 id="flow-diagram" tabindex="-1">Flow Diagram <a class="header-anchor" href="#flow-diagram" aria-label="Permalink to &quot;Flow Diagram&quot;">​</a></h2><p>The following diagram shows how the two plugins are included in the request flow of a <code>gardenlet</code>. When they are not enabled, then the <code>kube-apiserver</code> is internally authorizing the request via RBAC before forwarding the request directly to the <code>gardener-apiserver</code>, i.e., the <code>gardener-admission-controller</code> would not be consulted (this is not entirely correct because it also serves other admission webhook handlers, but for simplicity reasons this document focuses on the API access scope only).</p><p>When enabling the plugins, there is one additional step for each before the <code>gardener-apiserver</code> responds to the request.</p><p><img src="'+c+`" alt="Flow Diagram"></p><p>Please note that the example shows a request to an object (<code>Shoot</code>) residing in one of the API groups served by <code>gardener-apiserver</code>. However, the <code>gardenlet</code> is also interacting with objects in API groups served by the <code>kube-apiserver</code> (e.g., <code>Secret</code>,<code>ConfigMap</code>). In this case, the consultation of the <code>SeedRestriction</code> admission plugin is performed by the <code>kube-apiserver</code> itself before it forwards the request to the <code>gardener-apiserver</code>.</p><h2 id="implemented-rules" tabindex="-1">Implemented Rules <a class="header-anchor" href="#implemented-rules" aria-label="Permalink to &quot;Implemented Rules&quot;">​</a></h2><p>Today, the following rules are implemented:</p><table tabindex="0"><thead><tr><th>Resource</th><th>Verbs</th><th>Path(s)</th><th>Description</th></tr></thead><tbody><tr><td><code>BackupBucket</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>create</code>, <code>update</code>, <code>patch</code>, <code>delete</code></td><td><code>BackupBucket</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>BackupBucket</code>s. Allow only <code>create</code>, <code>update</code>, <code>patch</code>, <code>delete</code> requests for <code>BackupBucket</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>BackupEntry</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>create</code>, <code>update</code>, <code>patch</code></td><td><code>BackupEntry</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>BackupEntry</code>s. Allow only <code>create</code>, <code>update</code>, <code>patch</code> requests for <code>BackupEntry</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code> and referencing <code>BackupBucket</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Bastion</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>create</code>, <code>update</code>, <code>patch</code></td><td><code>Bastion</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>Bastion</code>s. Allow only <code>create</code>, <code>update</code>, <code>patch</code> requests for <code>Bastion</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>CertificateSigningRequest</code></td><td><code>get</code>, <code>create</code></td><td><code>CertificateSigningRequest</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code>, <code>create</code> requests for <code>CertificateSigningRequest</code>s related to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>CloudProfile</code></td><td><code>get</code></td><td><code>CloudProfile</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>CloudProfile</code>s referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>ClusterRoleBinding</code></td><td><code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code>, <code>delete</code></td><td><code>ClusterRoleBinding</code> -&gt; <code>ManagedSeed</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code> requests for <code>ManagedSeed</code>s in the bootstrapping phase assigned to the gardenlet&#39;s <code>Seed</code>s. Allow <code>delete</code> requests from gardenlets bootstrapped via <code>ManagedSeed</code>s.</td></tr><tr><td><code>ConfigMap</code></td><td><code>get</code></td><td><code>ConfigMap</code> -&gt; <code>Seed</code>, <code>ConfigMap</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>ConfigMap</code>s referenced by <code>Seed</code>s, or <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>. Allow reading the <code>kube-system/cluster-identity</code> <code>ConfigMap</code>.</td></tr><tr><td><code>ControllerRegistration</code></td><td><code>get</code>, <code>list</code>, <code>watch</code></td><td><code>ControllerRegistration</code> -&gt; <code>ControllerInstallation</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>ControllerRegistration</code>s.</td></tr><tr><td><code>ControllerDeployment</code></td><td><code>get</code></td><td><code>ControllerDeployment</code> -&gt; <code>ControllerInstallation</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code> requests for <code>ControllerDeployments</code>s referenced by <code>ControllerInstallation</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>ControllerInstallation</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>update</code>, <code>patch</code></td><td><code>ControllerInstallation</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>ControllerInstallation</code>s. Allow only <code>update</code>, <code>patch</code> requests for <code>ControllerInstallation</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>CredentialsBinding</code></td><td><code>get</code></td><td><code>CredentialsBinding</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>CredentialsBinding</code>s referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Event</code></td><td><code>create</code>, <code>patch</code></td><td>none</td><td>Allow to <code>create</code> or <code>patch</code> all kinds of <code>Event</code>s.</td></tr><tr><td><code>ExposureClass</code></td><td><code>get</code></td><td><code>ExposureClass</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code> requests for <code>ExposureClass</code>es referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>. Deny <code>get</code> requests to other <code>ExposureClass</code>es.</td></tr><tr><td><code>Gardenlet</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>update</code>, <code>patch</code>, <code>create</code></td><td><code>Gardenlet</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>Gardenlet</code>s. Allow only <code>create</code>, <code>update</code>, and <code>patch</code> requests for <code>Gardenlet</code>s belonging to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Lease</code></td><td><code>create</code>, <code>get</code>, <code>watch</code>, <code>update</code></td><td><code>Lease</code> -&gt; <code>Seed</code></td><td>Allow <code>create</code>, <code>get</code>, <code>update</code>, and <code>delete</code> requests for <code>Lease</code>s of the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>ManagedSeed</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>update</code>, <code>patch</code></td><td><code>ManagedSeed</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>ManagedSeed</code>s. Allow only <code>update</code>, <code>patch</code> requests for <code>ManagedSeed</code>s referencing a <code>Shoot</code> assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Namespace</code></td><td><code>get</code></td><td><code>Namespace</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code> requests for <code>Namespace</code>s of <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>. Always allow <code>get</code> requests for the <code>garden</code> <code>Namespace</code>.</td></tr><tr><td><code>NamespacedCloudProfile</code></td><td><code>get</code></td><td><code>NamespacedCloudProfile</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>NamespacedCloudProfile</code>s referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Project</code></td><td><code>get</code></td><td><code>Project</code> -&gt; <code>Namespace</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code> requests for <code>Project</code>s referenced by the <code>Namespace</code> of <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>SecretBinding</code></td><td><code>get</code></td><td><code>SecretBinding</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>SecretBinding</code>s referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>Secret</code></td><td><code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code>, <code>delete</code>(, <code>list</code>, <code>watch</code>)</td><td><code>Secret</code> -&gt; <code>Seed</code>, <code>Secret</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code>, <code>Secret</code> -&gt; <code>SecretBinding</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code>, <code>Secret</code> -&gt; <code>CredentialsBinding</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code>, <code>BackupBucket</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>Secret</code>s in the <code>seed-&lt;name&gt;</code> namespace. Allow only <code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code>, <code>delete</code> requests for the <code>Secret</code>s related to resources assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>s.</td></tr><tr><td><code>Seed</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>create</code>, <code>update</code>, <code>patch</code>, <code>delete</code></td><td><code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>Seed</code>s. Allow only <code>create</code>, <code>update</code>, <code>patch</code>, <code>delete</code> requests for the <code>gardenlet</code>&#39;s <code>Seed</code>s. [1]</td></tr><tr><td><code>ServiceAccount</code></td><td><code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code>, <code>delete</code></td><td><code>ServiceAccount</code> -&gt; <code>ManagedSeed</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code>, <code>ServiceAccount</code> -&gt; <code>Namespace</code> -&gt; <code>Seed</code></td><td>Allow <code>create</code>, <code>get</code>, <code>update</code>, <code>patch</code> requests for <code>ManagedSeed</code>s in the bootstrapping phase assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>s. Allow <code>delete</code> requests from gardenlets bootstrapped via <code>ManagedSeed</code>s. Allow all verbs on <code>ServiceAccount</code>s in seed-specific namespace.</td></tr><tr><td><code>Shoot</code></td><td><code>get</code>, <code>list</code>, <code>watch</code>, <code>update</code>, <code>patch</code></td><td><code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow <code>get</code>, <code>list</code>, <code>watch</code> requests for all <code>Shoot</code>s. Allow only <code>update</code>, <code>patch</code> requests for <code>Shoot</code>s assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>ShootState</code></td><td><code>get</code>, <code>create</code>, <code>update</code>, <code>patch</code></td><td><code>ShootState</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code>, <code>create</code>, <code>update</code>, <code>patch</code> requests for <code>ShootState</code>s belonging by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr><tr><td><code>WorkloadIdentity</code></td><td><code>get</code></td><td><code>WorkloadIdentity</code> -&gt; <code>CredentialsBinding</code> -&gt; <code>Shoot</code> -&gt; <code>Seed</code></td><td>Allow only <code>get</code> requests for <code>WorkloadIdentities</code> referenced by <code>CredentialsBinding</code>s referenced by <code>Shoot</code>s that are assigned to the <code>gardenlet</code>&#39;s <code>Seed</code>.</td></tr></tbody></table><blockquote><p>[1] If you use <code>ManagedSeed</code> resources then the <code>gardenlet</code> reconciling them (&quot;parent <code>gardenlet</code>&quot;) may be allowed to submit certain requests for the <code>Seed</code> resources resulting out of such <code>ManagedSeed</code> reconciliations (even if the &quot;parent <code>gardenlet</code>&quot; is not responsible for them):</p></blockquote><p>ℹ️ It is allowed to delete the <code>Seed</code> resources if the corresponding <code>ManagedSeed</code> objects already have a <code>deletionTimestamp</code> (this is secure as <code>gardenlet</code>s themselves don&#39;t have permissions for deleting <code>ManagedSeed</code>s).</p><h3 id="rule-exceptions-for-extension-clients" tabindex="-1">Rule Exceptions for Extension Clients <a class="header-anchor" href="#rule-exceptions-for-extension-clients" aria-label="Permalink to &quot;Rule Exceptions for Extension Clients&quot;">​</a></h3><p>Extension clients are allowed to perform the same operations as gardenlet clients with the following exceptions:</p><ul><li>Extension clients are granted the read-only subset of verbs for <code>CertificateSigningRequests</code>, <code>ClusterRoleBindings</code>, and <code>ServiceAccounts</code> (to prevent privilege escalation).</li><li>Extension clients are granted full access to <code>Lease</code> objects but only in the seed-specific namespace.</li></ul><p>When the need arises, more exceptions might be added to the access rules for resources that are already handled by the plugins. E.g., if an extension needs to populate additional shoot-specific <code>InternalSecrets</code>, according handling can be introduced. Permissions for resources that are not handled by the plugins can be granted using additional RBAC rules (independent of the plugins).</p><h2 id="seedauthorizer-authorization-webhook-enablement" tabindex="-1"><code>SeedAuthorizer</code> Authorization Webhook Enablement <a class="header-anchor" href="#seedauthorizer-authorization-webhook-enablement" aria-label="Permalink to &quot;\`SeedAuthorizer\` Authorization Webhook Enablement&quot;">​</a></h2><p>The <code>SeedAuthorizer</code> is implemented as a <a href="https://kubernetes.io/docs/reference/access-authn-authz/webhook/" target="_blank" rel="noreferrer">Kubernetes authorization webhook</a> and part of the <a href="/docs/gardener/concepts/admission-controller/"><code>gardener-admission-controller</code></a> component running in the garden cluster.</p><p>🎛 In order to activate it, you have to follow these steps:</p><ol><li><p>Set the following flags for the <code>kube-apiserver</code> of the garden cluster (i.e., the <code>kube-apiserver</code> whose API is extended by Gardener):</p><ul><li><code>--authorization-mode=RBAC,Node,Webhook</code> (please note that <code>Webhook</code> should appear after <code>RBAC</code> in the list [1]; <code>Node</code> might not be needed if you use a virtual garden cluster)</li><li><code>--authorization-webhook-config-file=&lt;path-to-the-webhook-config-file&gt;</code></li><li><code>--authorization-webhook-cache-authorized-ttl=0</code></li><li><code>--authorization-webhook-cache-unauthorized-ttl=0</code></li></ul></li><li><p>The webhook config file (stored at <code>&lt;path-to-the-webhook-config-file&gt;</code>) should look as follows:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">v1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Config</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">clusters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">garden</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  cluster</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    certificate-authority-data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">base64(CA-CERT-OF-GARDENER-ADMISSION-CONTROLLER)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://gardener-admission-controller.garden/webhooks/auth/seed</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">users</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kube-apiserver</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  user</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">contexts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">auth-webhook</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cluster</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">garden</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    user</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kube-apiserver</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">current-context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">auth-webhook</span></span></code></pre></div></li><li><p>When deploying the <a href="https://github.com/gardener/gardener/tree/master/charts/gardener/controlplane" target="_blank" rel="noreferrer">Gardener <code>controlplane</code> Helm chart</a>, set <code>.global.rbac.seedAuthorizer.enabled=true</code>. This will ensure that the RBAC resources granting global access for all <code>gardenlet</code>s will be deployed.</p></li><li><p>Delete the existing RBAC resources granting global access for all <code>gardenlet</code>s by running:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kubectl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> delete</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  clusterrole.rbac.authorization.k8s.io/gardener.cloud:system:seeds</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  clusterrolebinding.rbac.authorization.k8s.io/gardener.cloud:system:seeds</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --ignore-not-found</span></span></code></pre></div></li></ol><p>Please note that you should activate the <a href="/docs/gardener/deployment/gardenlet_api_access/#seedrestriction-admission-webhook-enablement"><code>SeedRestriction</code></a> admission handler as well.</p><blockquote><p>[1] The reason for the fact that <code>Webhook</code> authorization plugin should appear after <code>RBAC</code> is that the <code>kube-apiserver</code> will be depending on the <code>gardener-admission-controller</code> (serving the webhook). However, the <code>gardener-admission-controller</code> can only start when <code>gardener-apiserver</code> runs, but <code>gardener-apiserver</code> itself can only start when <code>kube-apiserver</code> runs. If <code>Webhook</code> is before <code>RBAC</code>, then <code>gardener-apiserver</code> might not be able to start, leading to a deadlock.</p></blockquote><h3 id="authorizer-decisions" tabindex="-1">Authorizer Decisions <a class="header-anchor" href="#authorizer-decisions" aria-label="Permalink to &quot;Authorizer Decisions&quot;">​</a></h3><p>As mentioned earlier, it&#39;s the authorizer&#39;s job to evaluate API requests and return one of the following decisions:</p><ul><li><code>DecisionAllow</code>: The request is allowed, further configured authorizers won&#39;t be consulted.</li><li><code>DecisionDeny</code>: The request is denied, further configured authorizers won&#39;t be consulted.</li><li><code>DecisionNoOpinion</code>: A decision cannot be made, further configured authorizers will be consulted.</li></ul><p>For backwards compatibility, no requests are denied at the moment, so that they are still deferred to a subsequent authorizer like RBAC. Though, this might change in the future.</p><p>First, the <code>SeedAuthorizer</code> extracts the <code>Seed</code> name from the API request. This step considers the following two cases:</p><ol><li>If the authenticated user belongs to the <code>gardener.cloud:system:seeds</code> group, it is considered a gardenlet client. <ul><li>This requires a proper TLS certificate that the <code>gardenlet</code> uses to contact the API server and is automatically given if <a href="/docs/gardener/concepts/gardenlet/#TLS-Bootstrapping">TLS bootstrapping</a> is used.</li><li>The authorizer extracts the seed name from the username by stripping the <code>gardener.cloud:system:seed:</code> prefix.</li><li>In cases where this information is missing e.g., when a custom Kubeconfig is used, the authorizer cannot make any decision. Thus, RBAC is still a considerable option to restrict the <code>gardenlet</code>&#39;s access permission if the above explained preconditions are not given.</li></ul></li><li>If the authenticated user belongs to the <code>system:serviceaccounts</code> group, it is considered an extension client under the following conditions: <ul><li>The <code>ServiceAccount</code> must be located in a <code>seed-</code> namespace. I.e., the user has to belong to a group with the <code>system:serviceaccounts:seed-</code> prefix. The seed name is extracted from this group by stripping the prefix.</li><li>The <code>ServiceAccount</code> must have the <code>extension-</code> prefix. I.e., the username must have the <code>system:serviceaccount:seed-&lt;seed-name&gt;:extension-</code> prefix.</li></ul></li></ol><p>With the <code>Seed</code> name at hand, the authorizer checks for an <strong>existing path</strong> from the resource that a request is being made for to the <code>Seed</code> belonging to the <code>gardenlet</code>/extension. Take a look at the <a href="/docs/gardener/deployment/gardenlet_api_access/#implementation-details">Implementation Details</a> section for more information.</p><h3 id="implementation-details" tabindex="-1">Implementation Details <a class="header-anchor" href="#implementation-details" aria-label="Permalink to &quot;Implementation Details&quot;">​</a></h3><p>Internally, the <code>SeedAuthorizer</code> uses a directed, acyclic graph data structure in order to efficiently respond to authorization requests for <code>gardenlet</code>s/extensions:</p><ul><li>A vertex in this graph represents a Kubernetes resource with its kind, namespace, and name (e.g., <code>Shoot:garden-my-project/my-shoot</code>).</li><li>An edge from vertex <code>u</code> to vertex <code>v</code> in this graph exists when <ul><li>(1) <code>v</code> is referred by <code>u</code> and <code>v</code> is a <code>Seed</code>, or when</li><li>(2) <code>u</code> is referred by <code>v</code>, or when</li><li>(3) <code>u</code> is strictly associated with <code>v</code>.</li></ul></li></ul><p>For example, a <code>Shoot</code> refers to a <code>Seed</code>, a <code>CloudProfile</code>, a <code>SecretBinding</code>, etc., so it has an outgoing edge to the <code>Seed</code> (1) and incoming edges from the <code>CloudProfile</code> and <code>SecretBinding</code> vertices (2). However, there might also be a <code>ShootState</code> or a <code>BackupEntry</code> resource strictly associated with this <code>Shoot</code>, hence, it has incoming edges from these vertices (3).</p><p><img src="`+a+`" alt="Resource Dependency Graph"></p><p>In the above picture, the resources that are actively watched are shaded. Gardener resources are green, while Kubernetes resources are blue. It shows the dependencies between the resources and how the graph is built based on the above rules.</p><p>ℹ️ The above picture shows all resources that may be accessed by <code>gardenlet</code>s/extensions, except for the <code>Quota</code> resource which is only included for completeness.</p><p>Now, when a <code>gardenlet</code>/extension wants to access certain resources, then the <code>SeedAuthorizer</code> uses a Depth-First traversal starting from the vertex representing the resource in question, e.g., from a <code>Project</code> vertex. If there is a path from the <code>Project</code> vertex to the vertex representing the <code>Seed</code> the <code>gardenlet</code>/extension is responsible for. then it allows the request.</p><h4 id="metrics" tabindex="-1">Metrics <a class="header-anchor" href="#metrics" aria-label="Permalink to &quot;Metrics&quot;">​</a></h4><p>The <code>SeedAuthorizer</code> registers the following metrics related to the mentioned graph implementation:</p><table tabindex="0"><thead><tr><th>Metric</th><th>Description</th></tr></thead><tbody><tr><td><code>gardener_admission_controller_seed_authorizer_graph_update_duration_seconds</code></td><td>Histogram of duration of resource dependency graph updates in seed authorizer, i.e., how long does it take to update the graph&#39;s vertices/edges when a resource is created, changed, or deleted.</td></tr><tr><td><code>gardener_admission_controller_seed_authorizer_graph_path_check_duration_seconds</code></td><td>Histogram of duration of checks whether a path exists in the resource dependency graph in seed authorizer.</td></tr></tbody></table><h4 id="debug-handler" tabindex="-1">Debug Handler <a class="header-anchor" href="#debug-handler" aria-label="Permalink to &quot;Debug Handler&quot;">​</a></h4><p>When the <code>.server.enableDebugHandlers</code> field in the <code>gardener-admission-controller</code>&#39;s component configuration is set to <code>true</code>, then it serves a handler that can be used for debugging the resource dependency graph under <code>/debug/resource-dependency-graph</code>.</p><p>🚨 Only use this setting for development purposes, as it enables unauthenticated users to view all data if they have access to the <code>gardener-admission-controller</code> component.</p><p>The handler renders an HTML page displaying the current graph with a list of vertices and its associated incoming and outgoing edges to other vertices. Depending on the size of the Gardener landscape (and consequently, the size of the graph), it might not be possible to render it in its entirety. If there are more than 2000 vertices, then the default filtering will selected for <code>kind=Seed</code> to prevent overloading the output.</p><p><em>Example output</em>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>-------------------------------------------------------------------------------</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>| # Seed:my-seed</span></span>
<span class="line"><span>|   &lt;- (11)</span></span>
<span class="line"><span>|     BackupBucket:73972fe2-3d7e-4f61-a406-b8f9e670e6b7</span></span>
<span class="line"><span>|     BackupEntry:garden-my-project/shoot--dev--my-shoot--4656a460-1a69-4f00-9372-7452cbd38ee3</span></span>
<span class="line"><span>|     ControllerInstallation:dns-external-mxt8m</span></span>
<span class="line"><span>|     ControllerInstallation:extension-shoot-cert-service-4qw5j</span></span>
<span class="line"><span>|     ControllerInstallation:networking-calico-bgrb2</span></span>
<span class="line"><span>|     ControllerInstallation:os-gardenlinux-qvb5z</span></span>
<span class="line"><span>|     ControllerInstallation:provider-gcp-w4mvf</span></span>
<span class="line"><span>|     Secret:garden/backup</span></span>
<span class="line"><span>|     Shoot:garden-my-project/my-shoot</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>-------------------------------------------------------------------------------</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>| # Shoot:garden-my-project/my-shoot</span></span>
<span class="line"><span>|   &lt;- (5)</span></span>
<span class="line"><span>|     CloudProfile:gcp</span></span>
<span class="line"><span>|     Namespace:garden-my-project</span></span>
<span class="line"><span>|     Secret:garden-my-project/my-dns-secret</span></span>
<span class="line"><span>|     SecretBinding:garden-my-project/my-credentials</span></span>
<span class="line"><span>|     ShootState:garden-my-project/my-shoot</span></span>
<span class="line"><span>|   -&gt; (1)</span></span>
<span class="line"><span>|     Seed:my-seed</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>-------------------------------------------------------------------------------</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>| # ShootState:garden-my-project/my-shoot</span></span>
<span class="line"><span>|   -&gt; (1)</span></span>
<span class="line"><span>|     Shoot:garden-my-project/my-shoot</span></span>
<span class="line"><span>|</span></span>
<span class="line"><span>-------------------------------------------------------------------------------</span></span>
<span class="line"><span></span></span>
<span class="line"><span>... (etc., similarly for the other resources)</span></span></code></pre></div><p>There are anchor links to easily jump from one resource to another, and the page provides means for filtering the results based on the <code>kind</code>, <code>namespace</code>, and/or <code>name</code>.</p><h4 id="pitfalls" tabindex="-1">Pitfalls <a class="header-anchor" href="#pitfalls" aria-label="Permalink to &quot;Pitfalls&quot;">​</a></h4><p>When there is a relevant update to an existing resource, i.e., when a reference to another resource is changed, then the corresponding vertex (along with all associated edges) is first deleted from the graph before it gets added again with the up-to-date edges. However, this does only work for vertices belonging to resources that are only created in exactly one &quot;watch handler&quot;. For example, the vertex for a <code>SecretBinding</code> can either be created in the <code>SecretBinding</code> handler itself or in the <code>Shoot</code> handler. In such cases, deleting the vertex before (re-)computing the edges might lead to race conditions and potentially renders the graph invalid. Consequently, instead of deleting the vertex, only the edges the respective handler is responsible for are deleted. If the vertex ends up with no remaining edges, then it also gets deleted automatically. Afterwards, the vertex can either be added again or the updated edges can be created.</p><h2 id="seedrestriction-admission-webhook-enablement" tabindex="-1"><code>SeedRestriction</code> Admission Webhook Enablement <a class="header-anchor" href="#seedrestriction-admission-webhook-enablement" aria-label="Permalink to &quot;\`SeedRestriction\` Admission Webhook Enablement&quot;">​</a></h2><p>The <code>SeedRestriction</code> is implemented as <a href="https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/" target="_blank" rel="noreferrer">Kubernetes admission webhook</a> and part of the <a href="/docs/gardener/concepts/admission-controller/"><code>gardener-admission-controller</code></a> component running in the garden cluster.</p><p>🎛 In order to activate it, you have to set <code>.global.admission.seedRestriction.enabled=true</code> when using the <a href="https://github.com/gardener/gardener/tree/master/charts/gardener/controlplane" target="_blank" rel="noreferrer">Gardener <code>controlplane</code> Helm chart</a>. This will add an additional webhook in the existing <code>ValidatingWebhookConfiguration</code> of the <code>gardener-admission-controller</code> which contains the configuration for the <code>SeedRestriction</code> handler. Please note that it should only be activated when the <code>SeedAuthorizer</code> is active as well.</p><h3 id="admission-decisions" tabindex="-1">Admission Decisions <a class="header-anchor" href="#admission-decisions" aria-label="Permalink to &quot;Admission Decisions&quot;">​</a></h3><p>The admission&#39;s purpose is to perform extended validation on requests which require the body of the object in question. Additionally, it handles <code>CREATE</code> requests of <code>gardenlet</code>s/extensions (the above discussed resource dependency graph cannot be used in such cases because there won&#39;t be any vertex/edge for non-existing resources).</p><p>Gardenlets/extensions are restricted to only create new resources which are somehow related to the seed clusters they are responsible for.</p>`,60)]))}const m=o(n,[["render",r]]);export{f as __pageData,m as default};

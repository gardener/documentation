import{_ as r,c as t,o as a,a2 as o}from"./chunks/framework.Bfq10Vlj.js";const s="/assets/gardener-data-model-overview.Cc-z82V-.png",m=JSON.parse('{"title":"Gardener API Server","description":"Understand the Gardener API server extension and the resources it exposes","frontmatter":{"categories":["Users"],"description":"Understand the Gardener API server extension and the resources it exposes","github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/concepts","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/concepts/apiserver.md","to":"apiserver.md"},"title":"Gardener API Server","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/concepts/apiserver/index.md","filePath":"docs/gardener/concepts/apiserver.md","lastUpdated":null}'),n={name:"docs/gardener/concepts/apiserver/index.md"};function i(c,e,d,l,h,u){return a(),t("div",null,e[0]||(e[0]=[o('<h1 id="gardener-api-server" tabindex="-1">Gardener API Server <a class="header-anchor" href="#gardener-api-server" aria-label="Permalink to &quot;Gardener API Server&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The Gardener API server is a Kubernetes-native extension based on its <a href="https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/apiserver-aggregation/" target="_blank" rel="noreferrer">aggregation layer</a>. It is registered via an <code>APIService</code> object and designed to run inside a Kubernetes cluster whose API it wants to extend.</p><p>After registration, it exposes the following resources:</p><h2 id="cloudprofiles" tabindex="-1"><code>CloudProfile</code>s <a class="header-anchor" href="#cloudprofiles" aria-label="Permalink to &quot;`CloudProfile`s&quot;">​</a></h2><p><code>CloudProfile</code>s are resources that describe a specific environment of an underlying infrastructure provider, e.g. AWS, Azure, etc. Each shoot has to reference a <code>CloudProfile</code> to declare the environment it should be created in. In a <code>CloudProfile</code>, the gardener operator specifies certain constraints like available machine types, regions, which Kubernetes versions they want to offer, etc. End-users can read <code>CloudProfile</code>s to see these values, but only operators can change the content or create/delete them. When a shoot is created or updated, then an admission plugin checks that only allowed values are used via the referenced <code>CloudProfile</code>.</p><p>Additionally, a <code>CloudProfile</code> may contain a <code>providerConfig</code>, which is a special configuration dedicated for the infrastructure provider. Gardener does not evaluate or understand this config, but extension controllers might need it for declaration of provider-specific constraints, or global settings.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/30-cloudprofile.yaml" target="_blank" rel="noreferrer">this</a> example manifest and consult the documentation of your provider extension controller to get information about its <code>providerConfig</code>.</p><h3 id="namespacedcloudprofiles" tabindex="-1"><code>NamespacedCloudProfile</code>s <a class="header-anchor" href="#namespacedcloudprofiles" aria-label="Permalink to &quot;`NamespacedCloudProfile`s&quot;">​</a></h3><p>In addition to <code>CloudProfile</code>s, <code>NamespacedCloudProfile</code>s exist to enable project-level customizations of <code>CloudProfile</code>s. Project administrators can create and manage cloud profiles with overrides or extensions specific to their project.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/35-namespacedcloudprofile.yaml" target="_blank" rel="noreferrer">this</a> example manifest and <a href="/docs/gardener/project/namespaced-cloud-profiles/">this</a> usage documentation for further information.</p><h2 id="internalsecrets" tabindex="-1"><code>InternalSecret</code>s <a class="header-anchor" href="#internalsecrets" aria-label="Permalink to &quot;`InternalSecret`s&quot;">​</a></h2><p>End-users can read and/or write <code>Secret</code>s in their project namespaces in the garden cluster. This prevents Gardener components from storing such &quot;Gardener-internal&quot; secrets in the respective project namespace. <code>InternalSecret</code>s are resources that contain shoot or project-related secrets that are &quot;Gardener-internal&quot;, i.e., secrets used and managed by the system that end-users don&#39;t have access to. <code>InternalSecret</code>s are defined like plain Kubernetes <code>Secret</code>s, behave exactly like them, and can be used in the same manners. The only difference is, that the <code>InternalSecret</code> resource is a dedicated API resource (exposed by gardener-apiserver). This allows separating access to &quot;normal&quot; secrets and internal secrets by the usual RBAC means.</p><p>Gardener uses an <code>InternalSecret</code> per Shoot for syncing the client CA to the project namespace in the garden cluster (named <code>&lt;shoot-name&gt;.ca-client</code>). The <a href="/docs/gardener/shoot/shoot_access/#shootsadminkubeconfig-subresource"><code>shoots/adminkubeconfig</code> subresource</a> signs short-lived client certificates by retrieving the CA from the <code>InternalSecret</code>.</p><p>Operators should configure <code>gardener-apiserver</code> to encrypt the <code>internalsecrets.core.gardener.cloud</code> resource in etcd.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/11-internal-secret.yaml" target="_blank" rel="noreferrer">this</a> example manifest.</p><h2 id="seeds" tabindex="-1"><code>Seed</code>s <a class="header-anchor" href="#seeds" aria-label="Permalink to &quot;`Seed`s&quot;">​</a></h2><p><code>Seed</code>s are resources that represent seed clusters. Gardener does not care about how a seed cluster got created - the only requirement is that it is of at least Kubernetes v1.30 and passes the Kubernetes conformance tests. The Gardener operator has to either deploy the gardenlet into the cluster they want to use as seed (recommended, then the gardenlet will create the <code>Seed</code> object itself after bootstrapping) or provide the kubeconfig to the cluster inside a secret (that is referenced by the <code>Seed</code> resource) and create the <code>Seed</code> resource themselves.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/45-secret-seed-backup.yaml" target="_blank" rel="noreferrer">this</a>, <a href="https://github.com/gardener/gardener/blob/master/example/50-seed.yaml" target="_blank" rel="noreferrer">this</a>, and optionally <a href="https://github.com/gardener/gardener/blob/master/example/40-secret-seed.yaml" target="_blank" rel="noreferrer">this</a> example manifests.</p><h2 id="shoot-quotas" tabindex="-1">Shoot <code>Quota</code>s <a class="header-anchor" href="#shoot-quotas" aria-label="Permalink to &quot;Shoot `Quota`s&quot;">​</a></h2><p>To allow end-users not having their dedicated infrastructure account to try out Gardener, the operator can register an account owned by them that they allow to be used for trial clusters. Trial clusters can be put under quota so that they don&#39;t consume too many resources (resulting in costs) and that one user cannot consume all resources on their own. These clusters are automatically terminated after a specified time, but end-users may extend the lifetime manually if needed.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/60-quota.yaml" target="_blank" rel="noreferrer">this</a> example manifest.</p><h2 id="projects" tabindex="-1"><code>Project</code>s <a class="header-anchor" href="#projects" aria-label="Permalink to &quot;`Project`s&quot;">​</a></h2><p>The first thing before creating a shoot cluster is to create a <code>Project</code>. A project is used to group multiple shoot clusters together. End-users can invite colleagues to the project to enable collaboration, and they can either make them <code>admin</code> or <code>viewer</code>. After an end-user has created a project, they will get a dedicated namespace in the garden cluster for all their shoots.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/05-project-dev.yaml" target="_blank" rel="noreferrer">this</a> example manifest.</p><h2 id="secretbindings" tabindex="-1"><code>SecretBinding</code>s <a class="header-anchor" href="#secretbindings" aria-label="Permalink to &quot;`SecretBinding`s&quot;">​</a></h2><p>Now that the end-user has a namespace the next step is registering their infrastructure provider account.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/70-secret-provider.yaml" target="_blank" rel="noreferrer">this</a> example manifest and consult the documentation of the extension controller for the respective infrastructure provider to get information about which keys are required in this secret.</p><p>After the secret has been created, the end-user has to create a special <code>SecretBinding</code> resource that binds this secret. Later, when creating shoot clusters, they will reference such binding.</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/80-secretbinding.yaml" target="_blank" rel="noreferrer">this</a> example manifest.</p><h2 id="shoots" tabindex="-1"><code>Shoot</code>s <a class="header-anchor" href="#shoots" aria-label="Permalink to &quot;`Shoot`s&quot;">​</a></h2><p>Shoot cluster contain various settings that influence how end-user Kubernetes clusters will look like in the end. As Gardener heavily relies on extension controllers for operating system configuration, networking, and infrastructure specifics, the end-user has the possibility (and responsibility) to provide these provider-specific configurations as well. Such configurations are not evaluated by Gardener (because it doesn&#39;t know/understand them), but they are only transported to the respective extension controller.</p><p>⚠️ This means that any configuration issues/mistake on the end-user side that relates to a provider-specific flag or setting cannot be caught during the update request itself but only later during the reconciliation (unless a validator webhook has been registered in the garden cluster by an operator).</p><p>Please see <a href="https://github.com/gardener/gardener/blob/master/example/90-shoot.yaml" target="_blank" rel="noreferrer">this</a> example manifest and consult the documentation of the provider extension controller to get information about its <code>spec.provider.controlPlaneConfig</code>, <code>.spec.provider.infrastructureConfig</code>, and <code>.spec.provider.workers[].providerConfig</code>.</p><h2 id="cluster-openidconnectpresets" tabindex="-1"><code>(Cluster)OpenIDConnectPreset</code>s <a class="header-anchor" href="#cluster-openidconnectpresets" aria-label="Permalink to &quot;`(Cluster)OpenIDConnectPreset`s&quot;">​</a></h2><p>Please see <a href="/docs/gardener/security/openidconnect-presets/">this</a> separate documentation file.</p><h2 id="overview-data-model" tabindex="-1">Overview Data Model <a class="header-anchor" href="#overview-data-model" aria-label="Permalink to &quot;Overview Data Model&quot;">​</a></h2><p><img src="'+s+'" alt="Gardener Overview Data Model"></p>',38)]))}const f=r(n,[["render",i]]);export{m as __pageData,f as default};

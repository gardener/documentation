import{_ as t,c as i,o as a,a2 as s}from"./chunks/framework.Bfq10Vlj.js";const u=JSON.parse('{"title":"Machine Error Codes","description":"","frontmatter":{"github_repo":"https://github.com/gardener/machine-controller-manager","github_subdir":"docs/development","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/other-components/machine-controller-manager/machine_error_codes.md","to":"machine_error_codes.md"},"persona":"Developers","title":"Machine Error Codes","prev":false,"next":false},"headers":[],"relativePath":"docs/other-components/machine-controller-manager/machine_error_codes/index.md","filePath":"docs/other-components/machine-controller-manager/machine_error_codes.md","lastUpdated":null}'),n={name:"docs/other-components/machine-controller-manager/machine_error_codes/index.md"};function o(r,e,d,l,c,h){return a(),i("div",null,e[0]||(e[0]=[s(`<h1 id="machine-error-code-handling" tabindex="-1">Machine Error code handling <a class="header-anchor" href="#machine-error-code-handling" aria-label="Permalink to &quot;Machine Error code handling&quot;">​</a></h1><h2 id="notational-conventions" tabindex="-1">Notational Conventions <a class="header-anchor" href="#notational-conventions" aria-label="Permalink to &quot;Notational Conventions&quot;">​</a></h2><p>The keywords &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;, &quot;SHOULD&quot;, &quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;NOT RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; are to be interpreted as described in <a href="https://datatracker.ietf.org/doc/html/rfc2119" target="_blank" rel="noreferrer">RFC 2119</a> (Bradner, S., &quot;Key words for use in RFCs to Indicate Requirement Levels&quot;, BCP 14, RFC 2119, March 1997).</p><p>The key words &quot;unspecified&quot;, &quot;undefined&quot;, and &quot;implementation-defined&quot; are to be interpreted as described in the <a href="https://www.open-std.org/jtc1/sc22/wg14/www/C99RationaleV5.10.pdf#page=18" target="_blank" rel="noreferrer">rationale for the C99 standard</a>.</p><p>An implementation is not compliant if it fails to satisfy one or more of the MUST, REQUIRED, or SHALL requirements for the protocols it implements. An implementation is compliant if it satisfies all the MUST, REQUIRED, and SHALL requirements for the protocols it implements.</p><h2 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to &quot;Terminology&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Term</th><th>Definition</th></tr></thead><tbody><tr><td>CR</td><td>Custom Resource (CR) is defined by a cluster admin using the Kubernetes Custom Resource Definition primitive.</td></tr><tr><td>VM</td><td>A Virtual Machine (VM) provisioned and managed by a provider. It could also refer to a physical machine in case of a bare metal provider.</td></tr><tr><td>Machine</td><td>Machine refers to a VM that is provisioned/managed by MCM. It typically describes the metadata used to store/represent a Virtual Machine</td></tr><tr><td>Node</td><td>Native kubernetes <code>Node</code> object. The objects you get to see when you do a &quot;kubectl get nodes&quot;. Although nodes can be either physical/virtual machines, for the purposes of our discussions it refers to a VM.</td></tr><tr><td>MCM</td><td><a href="https://github.com/gardener/machine-controller-manager" target="_blank" rel="noreferrer">Machine Controller Manager (MCM)</a> is the controller used to manage higher level Machine Custom Resource (CR) such as machine-set and machine-deployment CRs.</td></tr><tr><td>Provider/Driver/MC</td><td><code>Provider</code> (or) <code>Driver</code> (or) <code>Machine Controller (MC)</code> is the driver responsible for managing machine objects present in the cluster from whom it manages these machines. A simple example could be creation/deletion of VM on the provider.</td></tr></tbody></table><h2 id="pre-requisite" tabindex="-1">Pre-requisite <a class="header-anchor" href="#pre-requisite" aria-label="Permalink to &quot;Pre-requisite&quot;">​</a></h2><h3 id="machineclass-resources" tabindex="-1">MachineClass Resources <a class="header-anchor" href="#machineclass-resources" aria-label="Permalink to &quot;MachineClass Resources&quot;">​</a></h3><p>MCM introduces the CRD <code>MachineClass</code>. This is a blueprint for creating machines that join a certain cluster as nodes in a certain role. The provider only works with <code>MachineClass</code> resources that have the structure described here.</p><h4 id="providerspec" tabindex="-1">ProviderSpec <a class="header-anchor" href="#providerspec" aria-label="Permalink to &quot;ProviderSpec&quot;">​</a></h4><p>The <code>MachineClass</code> resource contains a <code>providerSpec</code> field that is passed in the <code>ProviderSpec</code> request field to CMI methods such as <a href="/docs/other-components/machine-controller-manager/machine_error_codes/#createmachine">CreateMachine</a>. The <code>ProviderSpec</code> can be thought of as a machine template from which the VM specification must be adopted. It can contain key-value pairs of these specs. An example for these key-value pairs are given below.</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Mandatory</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>vmPool</code></td><td>Yes</td><td><code>string</code></td><td>VM pool name, e.g. <code>TEST-WOKER-POOL</code></td></tr><tr><td><code>size</code></td><td>Yes</td><td><code>string</code></td><td>VM size, e.g. <code>xsmall</code>, <code>small</code>, etc. Each size maps to a number of CPUs and memory size.</td></tr><tr><td><code>rootFsSize</code></td><td>No</td><td><code>int</code></td><td>Root (<code>/</code>) filesystem size in GB</td></tr><tr><td><code>tags</code></td><td>Yes</td><td><code>map</code></td><td>Tags to be put on the created VM</td></tr></tbody></table><p>Most of the <code>ProviderSpec</code> fields are not mandatory. If not specified, the provider passes an empty value in the respective <code>Create VM</code> parameter.</p><p>The <code>tags</code> can be used to map a VM to its corresponding machine object&#39;s Name</p><p>The <code>ProviderSpec</code> is validated by methods that receive it as a request field for presence of all mandatory parameters and tags, and for validity of all parameters.</p><h4 id="secrets" tabindex="-1">Secrets <a class="header-anchor" href="#secrets" aria-label="Permalink to &quot;Secrets&quot;">​</a></h4><p>The <code>MachineClass</code> resource also contains a <code>secretRef</code> field that contains a reference to a secret. The keys of this secret are passed in the <code>Secrets</code> request field to CMI methods.</p><p>The secret can contain sensitive data such as</p><ul><li><code>cloud-credentials</code> secret data used to authenticate at the provider</li><li><code>cloud-init</code> scripts used to initialize a new VM. The cloud-init script is expected to contain scripts to initialize the Kubelet and make it join the cluster.</li></ul><h4 id="identifying-cluster-machines" tabindex="-1">Identifying Cluster Machines <a class="header-anchor" href="#identifying-cluster-machines" aria-label="Permalink to &quot;Identifying Cluster Machines&quot;">​</a></h4><p>To implement certain methods, the provider should be able to identify all machines associated with a particular Kubernetes cluster. This can be achieved using one/more of the below mentioned ways:</p><ul><li>Names of VMs created by the provider are prefixed by the cluster ID specified in the ProviderSpec.</li><li>VMs created by the provider are tagged with the special tags like <code>kubernetes.io/cluster</code> (for the cluster ID) and <code>kubernetes.io/role</code> (for the role), specified in the ProviderSpec.</li><li>Mapping <code>Resource Groups</code> to individual cluster.</li></ul><h3 id="error-scheme" tabindex="-1">Error Scheme <a class="header-anchor" href="#error-scheme" aria-label="Permalink to &quot;Error Scheme&quot;">​</a></h3><p>All provider API calls defined in this spec MUST return a <a href="https://github.com/gardener/machine-controller-manager/blob/master/pkg/util/provider/machinecodes/codes/codes.go" target="_blank" rel="noreferrer">machine error status</a>, which is very similar to <a href="https://github.com/grpc/grpc/blob/master/src/proto/grpc/status/status.proto" target="_blank" rel="noreferrer">standard machine status</a>.</p><h3 id="machine-provider-interface" tabindex="-1">Machine Provider Interface <a class="header-anchor" href="#machine-provider-interface" aria-label="Permalink to &quot;Machine Provider Interface&quot;">​</a></h3><ul><li>The provider MUST have a unique way to map a <code>machine object</code> to a <code>VM</code> which triggers the deletion for the corresponding VM backing the machine object.</li><li>The provider SHOULD have a unique way to map the <code>ProviderSpec</code> of a machine-class to a unique <code>Cluster</code>. This avoids deletion of other machines, not backed by the MCM.</li></ul><h4 id="createmachine" tabindex="-1"><code>CreateMachine</code> <a class="header-anchor" href="#createmachine" aria-label="Permalink to &quot;\`CreateMachine\`&quot;">​</a></h4><p>A Provider is REQUIRED to implement this interface method. This interface method will be called by the MCM to provision a new VM on behalf of the requesting machine object.</p><ul><li><p>This call requests the provider to create a VM backing the machine-object.</p></li><li><p>If VM backing the <code>Machine.Name</code> already exists, and is compatible with the specified <code>Machine</code> object in the <code>CreateMachineRequest</code>, the Provider MUST reply <code>0 OK</code> with the corresponding <code>CreateMachineResponse</code>.</p></li><li><p>The provider can OPTIONALLY make use of the MachineClass supplied in the <code>MachineClass</code> in the <code>CreateMachineRequest</code> to communicate with the provider.</p></li><li><p>The provider can OPTIONALLY make use of the secrets supplied in the <code>Secret</code> in the <code>CreateMachineRequest</code> to communicate with the provider.</p></li><li><p>The provider can OPTIONALLY make use of the <code>Status.LastKnownState</code> in the <code>Machine</code> object to decode the state of the VM operation based on the last known state of the VM. This can be useful to restart/continue an operations which are mean&#39;t to be atomic.</p></li><li><p>The provider MUST have a unique way to map a <code>machine object</code> to a <code>VM</code>. This could be implicitly provided by the provider by letting you set VM-names (or) could be explicitly specified by the provider using appropriate tags to map the same.</p></li><li><p>This operation SHOULD be idempotent.</p></li><li><p>The <code>CreateMachineResponse</code> returned by this method is expected to return</p><ul><li><code>ProviderID</code> that uniquely identifys the VM at the provider. This is expected to match with the node.Spec.ProviderID on the node object.</li><li><code>NodeName</code> that is the expected name of the machine when it joins the cluster. It must match with the node name.</li><li><code>LastKnownState</code> is an OPTIONAL field that can store details of the last known state of the VM. It can be used by future operation calls to determine current infrastucture state. This state is saved on the machine object.</li></ul></li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// CreateMachine call is responsible for VM creation on the provider</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CreateMachine(context.Context, *CreateMachineRequest) (*CreateMachineResponse, error)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// CreateMachineRequest is the create request for VM creation</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type CreateMachineRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Machine object from whom VM is to be created</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Machine *v1alpha1.Machine</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass backing the machine object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//  Secret backing the machineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Secret *corev1.Secret</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// CreateMachineResponse is the create response for VM creation</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type CreateMachineResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID is the unique identification of the VM at the cloud provider.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID typically matches with the node.Spec.ProviderID on the node object.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Eg: gce://project-name/region/vm-ID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	ProviderID string</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// NodeName is the name of the node-object registered to kubernetes.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	NodeName string</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// LastKnownState represents the last state of the VM during an creation/deletion error</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	LastKnownState string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="createmachine-errors" tabindex="-1">CreateMachine Errors <a class="header-anchor" href="#createmachine-errors" aria-label="Permalink to &quot;CreateMachine Errors&quot;">​</a></h5><p>If the provider is unable to complete the CreateMachine call successfully, it MUST return a non-ok ginterface method code in the machine status. If the conditions defined below are encountered, the provider MUST return the specified machine error code. The MCM MUST implement the specified error recovery behavior when it encounters the machine error code.</p><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call was successful in creating/adopting a VM that matches supplied creation request. The <code>CreateMachineResponse</code> is returned with desired values</td><td></td><td>N</td></tr><tr><td>1 CANCELED</td><td>Cancelled</td><td>Call was cancelled. Perform any pending clean-up tasks and return the call</td><td></td><td>N</td></tr><tr><td>2 UNKNOWN</td><td>Something went wrong</td><td>Not enough information on what went wrong</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>3 INVALID_ARGUMENT</td><td>Re-check supplied parameters</td><td>Re-check the supplied <code>Machine.Name</code> and <code>ProviderSpec</code>. Make sure all parameters are in permitted range of values. Exact issue to be given in <code>.message</code></td><td>Update providerSpec to fix issues.</td><td>N</td></tr><tr><td>4 DEADLINE_EXCEEDED</td><td>Timeout</td><td>The call processing exceeded supplied deadline</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>6 ALREADY_EXISTS</td><td>Already exists but desired parameters doesn&#39;t match</td><td>Parameters of the existing VM don&#39;t match the ProviderSpec</td><td>Create machine with a different name</td><td>N</td></tr><tr><td>7 PERMISSION_DENIED</td><td>Insufficent permissions</td><td>The requestor doesn&#39;t have enough permissions to create an VM and it&#39;s required dependencies</td><td>Update requestor permissions to grant the same</td><td>N</td></tr><tr><td>8 RESOURCE_EXHAUSTED</td><td>Resource limits have been reached</td><td>The requestor doesn&#39;t have enough resource limits to process this creation request</td><td>Enhance resource limits associated with the user/account to process this</td><td>N</td></tr><tr><td>9 PRECONDITION_FAILED</td><td>VM is in inconsistent state</td><td>The VM is in a state that is invalid for this operation</td><td>Manual intervention might be needed to fix the state of the VM</td><td>N</td></tr><tr><td>10 ABORTED</td><td>Operation is pending</td><td>Indicates that there is already an operation pending for the specified machine</td><td>Wait until previous pending operation is processed</td><td>Y</td></tr><tr><td>11 OUT_OF_RANGE</td><td>Resources were out of range</td><td>The requested number of CPUs, memory size, of FS size in ProviderSpec falls outside of the corresponding valid range</td><td>Update request paramaters to request valid resource requests</td><td>N</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Retry with an alternate logic or implement this method at the provider. Most methods by default are in this state</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Needs manual intervension to fix this</td><td>N</td></tr><tr><td>14 UNAVAILABLE</td><td>Not Available</td><td>Unavailable indicates the service is currently unavailable.</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>16 UNAUTHENTICATED</td><td>Missing provider credentials</td><td>Request does not have valid authentication credentials for the operation</td><td>Fix the provider credentials</td><td>N</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="initializemachine" tabindex="-1"><code>InitializeMachine</code> <a class="header-anchor" href="#initializemachine" aria-label="Permalink to &quot;\`InitializeMachine\`&quot;">​</a></h4><p>Provider can OPTIONALLY implement this driver call. Else should return a <code>UNIMPLEMENTED</code> status in error.<br> This interface method will be called by the MCM to initialize a new VM just after creation. This can be used to configure network configuration etc.</p><ul><li>This call requests the provider to initialize a newly created VM backing the machine-object.</li><li>The <code>InitializeMachineResponse</code> returned by this method is expected to return <ul><li><code>ProviderID</code> that uniquely identifys the VM at the provider. This is expected to match with the <code>node.Spec.ProviderID</code> on the node object.</li><li><code>NodeName</code> that is the expected name of the machine when it joins the cluster. It must match with the node name.</li></ul></li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// InitializeMachine call is responsible for VM initialization on the provider.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">InitializeMachine(context.Context, *InitializeMachineRequest) (*InitializeMachineResponse, error)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// InitializeMachineRequest encapsulates params for the VM Initialization operation (Driver.InitializeMachine).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type InitializeMachineRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Machine object representing VM that must be initialized</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Machine *v1alpha1.Machine</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass backing the machine object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Secret backing the machineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Secret *corev1.Secret</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// InitializeMachineResponse is the response for VM instance initialization (Driver.InitializeMachine).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type InitializeMachineResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID is the unique identification of the VM at the cloud provider.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID typically matches with the node.Spec.ProviderID on the node object.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Eg: gce://project-name/region/vm-ID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	ProviderID string</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// NodeName is the name of the node-object registered to kubernetes.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	NodeName string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="initializemachine-errors" tabindex="-1">InitializeMachine Errors <a class="header-anchor" href="#initializemachine-errors" aria-label="Permalink to &quot;InitializeMachine Errors&quot;">​</a></h5><p>If the provider is unable to complete the <code>InitializeMachine</code> call successfully, it MUST return a non-ok machine code in the machine status.</p><p>If the conditions defined below are encountered, the provider MUST return the specified machine error code. The MCM MUST implement the specified error recovery behavior when it encounters the machine error code.</p><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call was successful in initializing a VM that matches supplied initialization request. The <code>InitializeMachineResponse</code> is returned with desired values</td><td></td><td>N</td></tr><tr><td>5 NOT_FOUND</td><td>Timeout</td><td>VM Instance for Machine isn&#39;t found at provider</td><td>Skip Initialization and Continue</td><td>N</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Skip Initialization and continue</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken.</td><td>Needs investigation and possible intervention to fix this</td><td>Y</td></tr><tr><td>17 UNINITIALIZED</td><td>Failed Initialization</td><td>VM Instance could not be initializaed</td><td>Initialization is reattempted in next reconcile cycle</td><td>Y</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="deletemachine" tabindex="-1"><code>DeleteMachine</code> <a class="header-anchor" href="#deletemachine" aria-label="Permalink to &quot;\`DeleteMachine\`&quot;">​</a></h4><p>A Provider is REQUIRED to implement this driver call. This driver call will be called by the MCM to deprovision/delete/terminate a VM backed by the requesting machine object.</p><ul><li><p>If a VM corresponding to the specified machine-object&#39;s name does not exist or the artifacts associated with the VM do not exist anymore (after deletion), the Provider MUST reply <code>0 OK</code>.</p></li><li><p>The provider SHALL only act on machines belonging to the cluster-id/cluster-name obtained from the <code>ProviderSpec</code>.</p></li><li><p>The provider can OPTIONALY make use of the secrets supplied in the <code>Secrets</code> map in the <code>DeleteMachineRequest</code> to communicate with the provider.</p></li><li><p>The provider can OPTIONALY make use of the <code>Spec.ProviderID</code> map in the <code>Machine</code> object.</p></li><li><p>The provider can OPTIONALLY make use of the <code>Status.LastKnownState</code> in the <code>Machine</code> object to decode the state of the VM operation based on the last known state of the VM. This can be useful to restart/continue an operations which are mean&#39;t to be atomic.</p></li><li><p>This operation SHOULD be idempotent.</p></li><li><p>The provider must have a unique way to map a <code>machine object</code> to a <code>VM</code> which triggers the deletion for the corresponding VM backing the machine object.</p></li><li><p>The <code>DeleteMachineResponse</code> returned by this method is expected to return</p><ul><li><code>LastKnownState</code> is an OPTIONAL field that can store details of the last known state of the VM. It can be used by future operation calls to determine current infrastucture state. This state is saved on the machine object.</li></ul></li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DeleteMachine call is responsible for VM deletion/termination on the provider</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DeleteMachine(context.Context, *DeleteMachineRequest) (*DeleteMachineResponse, error)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DeleteMachineRequest is the delete request for VM deletion</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type DeleteMachineRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Machine object from whom VM is to be deleted</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Machine *v1alpha1.Machine</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass backing the machine object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Secret backing the machineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Secret *corev1.Secret</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// DeleteMachineResponse is the delete response for VM deletion</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type DeleteMachineResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// LastKnownState represents the last state of the VM during an creation/deletion error</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	LastKnownState string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="deletemachine-errors" tabindex="-1">DeleteMachine Errors <a class="header-anchor" href="#deletemachine-errors" aria-label="Permalink to &quot;DeleteMachine Errors&quot;">​</a></h5><p>If the provider is unable to complete the DeleteMachine call successfully, it MUST return a non-ok machine code in the machine status. If the conditions defined below are encountered, the provider MUST return the specified machine error code.</p><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call was successful in deleting a VM that matches supplied deletion request.</td><td></td><td>N</td></tr><tr><td>1 CANCELED</td><td>Cancelled</td><td>Call was cancelled. Perform any pending clean-up tasks and return the call</td><td></td><td>N</td></tr><tr><td>2 UNKNOWN</td><td>Something went wrong</td><td>Not enough information on what went wrong</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>3 INVALID_ARGUMENT</td><td>Re-check supplied parameters</td><td>Re-check the supplied <code>Machine.Name</code> and make sure that it is in the desired format and not a blank value. Exact issue to be given in <code>.message</code></td><td>Update <code>Machine.Name</code> to fix issues.</td><td>N</td></tr><tr><td>4 DEADLINE_EXCEEDED</td><td>Timeout</td><td>The call processing exceeded supplied deadline</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>7 PERMISSION_DENIED</td><td>Insufficent permissions</td><td>The requestor doesn&#39;t have enough permissions to delete an VM and it&#39;s required dependencies</td><td>Update requestor permissions to grant the same</td><td>N</td></tr><tr><td>9 PRECONDITION_FAILED</td><td>VM is in inconsistent state</td><td>The VM is in a state that is invalid for this operation</td><td>Manual intervention might be needed to fix the state of the VM</td><td>N</td></tr><tr><td>10 ABORTED</td><td>Operation is pending</td><td>Indicates that there is already an operation pending for the specified machine</td><td>Wait until previous pending operation is processed</td><td>Y</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Retry with an alternate logic or implement this method at the provider. Most methods by default are in this state</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Needs manual intervension to fix this</td><td>N</td></tr><tr><td>14 UNAVAILABLE</td><td>Not Available</td><td>Unavailable indicates the service is currently unavailable.</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>16 UNAUTHENTICATED</td><td>Missing provider credentials</td><td>Request does not have valid authentication credentials for the operation</td><td>Fix the provider credentials</td><td>N</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="getmachinestatus" tabindex="-1"><code>GetMachineStatus</code> <a class="header-anchor" href="#getmachinestatus" aria-label="Permalink to &quot;\`GetMachineStatus\`&quot;">​</a></h4><p>A Provider can OPTIONALLY implement this driver call. Else should return a <code>UNIMPLEMENTED</code> status in error. This call will be invoked by the MC to get the status of a machine. This optional driver call helps in optimizing the working of the provider by avoiding unwanted calls to <code>CreateMachine()</code> and <code>DeleteMachine()</code>.</p><ul><li>If a VM corresponding to the specified machine object&#39;s <code>Machine.Name</code> exists on provider the <code>GetMachineStatusResponse</code> fields are to be filled similar to the <code>CreateMachineResponse</code>.</li><li>The provider SHALL only act on machines belonging to the cluster-id/cluster-name obtained from the <code>ProviderSpec</code>.</li><li>The provider can OPTIONALY make use of the secrets supplied in the <code>Secrets</code> map in the <code>GetMachineStatusRequest</code> to communicate with the provider.</li><li>The provider can OPTIONALY make use of the VM unique ID (returned by the provider on machine creation) passed in the <code>ProviderID</code> map in the <code>GetMachineStatusRequest</code>.</li><li>This operation MUST be idempotent.</li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GetMachineStatus call get&#39;s the status of the VM backing the machine object on the provider</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GetMachineStatus(context.Context, *GetMachineStatusRequest) (*GetMachineStatusResponse, error)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GetMachineStatusRequest is the get request for VM info</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GetMachineStatusRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Machine object from whom VM status is to be fetched</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Machine *v1alpha1.Machine</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass backing the machine object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	//  Secret backing the machineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Secret *corev1.Secret</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GetMachineStatusResponse is the get response for VM info</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GetMachineStatusResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID is the unique identification of the VM at the cloud provider.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderID typically matches with the node.Spec.ProviderID on the node object.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Eg: gce://project-name/region/vm-ID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	ProviderID string</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// NodeName is the name of the node-object registered to kubernetes.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	NodeName string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="getmachinestatus-errors" tabindex="-1">GetMachineStatus Errors <a class="header-anchor" href="#getmachinestatus-errors" aria-label="Permalink to &quot;GetMachineStatus Errors&quot;">​</a></h5><p>If the provider is unable to complete the GetMachineStatus call successfully, it MUST return a non-ok machine code in the machine status. If the conditions defined below are encountered, the provider MUST return the specified machine error code.</p><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call was successful in getting machine details for given machine <code>Machine.Name</code></td><td></td><td>N</td></tr><tr><td>1 CANCELED</td><td>Cancelled</td><td>Call was cancelled. Perform any pending clean-up tasks and return the call</td><td></td><td>N</td></tr><tr><td>2 UNKNOWN</td><td>Something went wrong</td><td>Not enough information on what went wrong</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>3 INVALID_ARGUMENT</td><td>Re-check supplied parameters</td><td>Re-check the supplied <code>Machine.Name</code> and make sure that it is in the desired format and not a blank value. Exact issue to be given in <code>.message</code></td><td>Update <code>Machine.Name</code> to fix issues.</td><td>N</td></tr><tr><td>4 DEADLINE_EXCEEDED</td><td>Timeout</td><td>The call processing exceeded supplied deadline</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>5 NOT_FOUND</td><td>Machine isn&#39;t found at provider</td><td>The machine could not be found at provider</td><td>Not required</td><td>N</td></tr><tr><td>7 PERMISSION_DENIED</td><td>Insufficent permissions</td><td>The requestor doesn&#39;t have enough permissions to get details for the VM and it&#39;s required dependencies</td><td>Update requestor permissions to grant the same</td><td>N</td></tr><tr><td>9 PRECONDITION_FAILED</td><td>VM is in inconsistent state</td><td>The VM is in a state that is invalid for this operation</td><td>Manual intervention might be needed to fix the state of the VM</td><td>N</td></tr><tr><td>11 OUT_OF_RANGE</td><td>Multiple VMs found</td><td>Multiple VMs found with matching machine object names</td><td>Orphan VM handler to cleanup orphan VMs / Manual intervention maybe required if orphan VM handler isn&#39;t enabled.</td><td>Y</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Retry with an alternate logic or implement this method at the provider. Most methods by default are in this state</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Needs manual intervension to fix this</td><td>N</td></tr><tr><td>14 UNAVAILABLE</td><td>Not Available</td><td>Unavailable indicates the service is currently unavailable.</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>16 UNAUTHENTICATED</td><td>Missing provider credentials</td><td>Request does not have valid authentication credentials for the operation</td><td>Fix the provider credentials</td><td>N</td></tr><tr><td>17 UNINITIALIZED</td><td>Failed Initialization</td><td>VM Instance could not be initializaed</td><td>Initialization is reattempted in next reconcile cycle</td><td>N</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="listmachines" tabindex="-1"><code>ListMachines</code> <a class="header-anchor" href="#listmachines" aria-label="Permalink to &quot;\`ListMachines\`&quot;">​</a></h4><p>A Provider can OPTIONALLY implement this driver call. Else should return a <code>UNIMPLEMENTED</code> status in error. The Provider SHALL return the information about all the machines associated with the <code>MachineClass</code>. Make sure to use appropriate filters to achieve the same to avoid data transfer overheads. This optional driver call helps in cleaning up orphan VMs present in the cluster. If not implemented, any orphan VM that might have been created incorrectly by the MCM/Provider (due to bugs in code/infra) might require manual clean up.</p><ul><li>If the Provider succeeded in returning a list of <code>Machine.Name</code> with their corresponding <code>ProviderID</code>, then return <code>0 OK</code>.</li><li>The <code>ListMachineResponse</code> contains a map of <code>MachineList</code> whose <ul><li>Key is expected to contain the <code>ProviderID</code> &amp;</li><li>Value is expected to contain the <code>Machine.Name</code> corresponding to it&#39;s kubernetes machine CR object</li></ul></li><li>The provider can OPTIONALY make use of the secrets supplied in the <code>Secrets</code> map in the <code>ListMachinesRequest</code> to communicate with the provider.</li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ListMachines lists all the machines that might have been created by the supplied machineClass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ListMachines(context.Context, *ListMachinesRequest) (*ListMachinesResponse, error)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ListMachinesRequest is the request object to get a list of VMs belonging to a machineClass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type ListMachinesRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Secret backing the machineClass object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	Secret *corev1.Secret</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ListMachinesResponse is the response object of the list of VMs belonging to a machineClass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type ListMachinesResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineList is the map of list of machines. Format for the map should be &lt;ProviderID, MachineName&gt;.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineList map[string]string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="listmachines-errors" tabindex="-1">ListMachines Errors <a class="header-anchor" href="#listmachines-errors" aria-label="Permalink to &quot;ListMachines Errors&quot;">​</a></h5><p>If the provider is unable to complete the ListMachines call successfully, it MUST return a non-ok machine code in the machine status. If the conditions defined below are encountered, the provider MUST return the specified machine error code. The MCM MUST implement the specified error recovery behavior when it encounters the machine error code.</p><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call for listing all VMs associated with <code>ProviderSpec</code> was successful.</td><td></td><td>N</td></tr><tr><td>1 CANCELED</td><td>Cancelled</td><td>Call was cancelled. Perform any pending clean-up tasks and return the call</td><td></td><td>N</td></tr><tr><td>2 UNKNOWN</td><td>Something went wrong</td><td>Not enough information on what went wrong</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>3 INVALID_ARGUMENT</td><td>Re-check supplied parameters</td><td>Re-check the supplied <code>ProviderSpec</code> and make sure that all required fields are present in their desired value format. Exact issue to be given in <code>.message</code></td><td>Update <code>ProviderSpec</code> to fix issues.</td><td>N</td></tr><tr><td>4 DEADLINE_EXCEEDED</td><td>Timeout</td><td>The call processing exceeded supplied deadline</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>7 PERMISSION_DENIED</td><td>Insufficent permissions</td><td>The requestor doesn&#39;t have enough permissions to list VMs and it&#39;s required dependencies</td><td>Update requestor permissions to grant the same</td><td>N</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Retry with an alternate logic or implement this method at the provider. Most methods by default are in this state</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Needs manual intervension to fix this</td><td>N</td></tr><tr><td>14 UNAVAILABLE</td><td>Not Available</td><td>Unavailable indicates the service is currently unavailable.</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>16 UNAUTHENTICATED</td><td>Missing provider credentials</td><td>Request does not have valid authentication credentials for the operation</td><td>Fix the provider credentials</td><td>N</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="getvolumeids" tabindex="-1"><code>GetVolumeIDs</code> <a class="header-anchor" href="#getvolumeids" aria-label="Permalink to &quot;\`GetVolumeIDs\`&quot;">​</a></h4><p>A Provider can OPTIONALLY implement this driver call. Else should return a <code>UNIMPLEMENTED</code> status in error. This driver call will be called by the MCM to get the <code>VolumeIDs</code> for the list of <code>PersistentVolumes (PVs)</code> supplied. This OPTIONAL (but recommended) driver call helps in serailzied eviction of pods with PVs while draining of machines. This implies applications backed by PVs would be evicted one by one, leading to shorter application downtimes.</p><ul><li>On succesful returnal of a list of <code>Volume-IDs</code> for all supplied <code>PVSpecs</code>, the Provider MUST reply <code>0 OK</code>.</li><li>The <code>GetVolumeIDsResponse</code> is expected to return a repeated list of <code>strings</code> consisting of the <code>VolumeIDs</code> for <code>PVSpec</code> that could be extracted.</li><li>If for any <code>PV</code> the Provider wasn&#39;t able to identify the <code>Volume-ID</code>, the provider MAY chose to ignore it and return the <code>Volume-IDs</code> for the rest of the <code>PVs</code> for whom the <code>Volume-ID</code> was found.</li><li>Getting the <code>VolumeID</code> from the <code>PVSpec</code> depends on the Cloud-provider. You can extract this information by parsing the <code>PVSpec</code> based on the <code>ProviderType</code><ul><li><a href="https://github.com/kubernetes/api/blob/release-1.15/core/v1/types.go#L297-L339" target="_blank" rel="noreferrer">https://github.com/kubernetes/api/blob/release-1.15/core/v1/types.go#L297-L339</a></li><li><a href="https://github.com/kubernetes/api/blob/release-1.15//core/v1/types.go#L175-L257" target="_blank" rel="noreferrer">https://github.com/kubernetes/api/blob/release-1.15//core/v1/types.go#L175-L257</a></li></ul></li><li>This operation MUST be idempotent.</li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GetVolumeIDsRequest is the request object to get a list of VolumeIDs for a PVSpec</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GetVolumeIDsRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// PVSpecsList is a list of PV specs for whom volume-IDs are required</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// Plugin should parse this raw data into pre-defined list of PVSpecs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	PVSpecs []*corev1.PersistentVolumeSpec</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GetVolumeIDsResponse is the response object of the list of VolumeIDs for a PVSpec</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GetVolumeIDsResponse struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// VolumeIDs is a list of VolumeIDs.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	VolumeIDs []string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h5 id="getvolumeids-errors" tabindex="-1">GetVolumeIDs Errors <a class="header-anchor" href="#getvolumeids-errors" aria-label="Permalink to &quot;GetVolumeIDs Errors&quot;">​</a></h5><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>The call getting list of <code>VolumeIDs</code> for the list of <code>PersistentVolumes</code> was successful.</td><td></td><td>N</td></tr><tr><td>1 CANCELED</td><td>Cancelled</td><td>Call was cancelled. Perform any pending clean-up tasks and return the call</td><td></td><td>N</td></tr><tr><td>2 UNKNOWN</td><td>Something went wrong</td><td>Not enough information on what went wrong</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>3 INVALID_ARGUMENT</td><td>Re-check supplied parameters</td><td>Re-check the supplied <code>PVSpecList</code> and make sure that it is in the desired format. Exact issue to be given in <code>.message</code></td><td>Update <code>PVSpecList</code> to fix issues.</td><td>N</td></tr><tr><td>4 DEADLINE_EXCEEDED</td><td>Timeout</td><td>The call processing exceeded supplied deadline</td><td>Retry operation after sometime</td><td>Y</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this service.</td><td>Retry with an alternate logic or implement this method at the provider. Most methods by default are in this state</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Needs manual intervension to fix this</td><td>N</td></tr><tr><td>14 UNAVAILABLE</td><td>Not Available</td><td>Unavailable indicates the service is currently unavailable.</td><td>Retry operation after sometime</td><td>Y</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h4 id="generatemachineclassformigration" tabindex="-1"><code>GenerateMachineClassForMigration</code> <a class="header-anchor" href="#generatemachineclassformigration" aria-label="Permalink to &quot;\`GenerateMachineClassForMigration\`&quot;">​</a></h4><p>A Provider SHOULD implement this driver call, else it MUST return a <code>UNIMPLEMENTED</code> status in error. This driver call will be called by the Machine Controller to try to perform a machineClass migration for an unknown machineClass Kind. This helps in migration of one kind of machineClass to another kind. For instance an machineClass custom resource of <code>AWSMachineClass</code> to <code>MachineClass</code>.</p><ul><li>On successful generation of machine class the Provider MUST reply <code>0 OK</code> (or) <code>nil</code> error.</li><li><code>GenerateMachineClassForMigrationRequest</code> expects the provider-specific machine class (eg. AWSMachineClass) to be supplied as the <code>ProviderSpecificMachineClass</code>. The provider is responsible for unmarshalling the golang struct. It also passes a reference to an existing <code>MachineClass</code> object.</li><li>The provider is expected to fill in this<code>MachineClass</code> object based on the conversions.</li><li>An optional <code>ClassSpec</code> containing the <code>type ClassSpec struct</code> is also provided to decode the provider info.</li><li><code>GenerateMachineClassForMigration</code> is only responsible for filling up the passed <code>MachineClass</code> object.</li><li>The task of creating the new <code>CR</code> of the new kind (MachineClass) with the same name as the previous one and also annotating the old machineClass CR with a migrated annotation and migrating existing references is done by the calling library implicitly.</li><li>This operation MUST be idempotent.</li></ul><div class="language-protobuf vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">protobuf</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GenerateMachineClassForMigrationRequest is the request for generating the generic machineClass</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// for the provider specific machine class</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GenerateMachineClassForMigrationRequest struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ProviderSpecificMachineClass is provider specfic machine class object.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// E.g. AWSMachineClass</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	ProviderSpecificMachineClass any</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// MachineClass is the machine class object generated that is to be filled up</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	MachineClass *v1alpha1.MachineClass</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">	// ClassSpec contains the class spec object to determine the machineClass kind</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	ClassSpec *v1alpha1.ClassSpec</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// GenerateMachineClassForMigrationResponse is the response for generating the generic machineClass</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// for the provider specific machine class</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type GenerateMachineClassForMigrationResponse struct{}</span></span></code></pre></div><h5 id="migratemachineclass-errors" tabindex="-1">MigrateMachineClass Errors <a class="header-anchor" href="#migratemachineclass-errors" aria-label="Permalink to &quot;MigrateMachineClass Errors&quot;">​</a></h5><table tabindex="0"><thead><tr><th>machine Code</th><th>Condition</th><th>Description</th><th>Recovery Behavior</th><th>Auto Retry Required</th></tr></thead><tbody><tr><td>0 OK</td><td>Successful</td><td>Migration of provider specific machine class was successful</td><td>Machine reconcilation is retried once the new class has been created</td><td>Y</td></tr><tr><td>12 UNIMPLEMENTED</td><td>Not implemented</td><td>Unimplemented indicates operation is not implemented or not supported/enabled in this provider.</td><td>None</td><td>N</td></tr><tr><td>13 INTERNAL</td><td>Major error</td><td>Means some invariants expected by underlying system has been broken. If you see one of these errors, something is very broken.</td><td>Might need manual intervension to fix this</td><td>Y</td></tr></tbody></table><p>The status <code>message</code> MUST contain a human readable description of error, if the status <code>code</code> is not <code>OK</code>. This string MAY be surfaced by MCM to end users.</p><h2 id="configuration-and-operation" tabindex="-1">Configuration and Operation <a class="header-anchor" href="#configuration-and-operation" aria-label="Permalink to &quot;Configuration and Operation&quot;">​</a></h2><h3 id="supervised-lifecycle-management" tabindex="-1">Supervised Lifecycle Management <a class="header-anchor" href="#supervised-lifecycle-management" aria-label="Permalink to &quot;Supervised Lifecycle Management&quot;">​</a></h3><ul><li>For Providers packaged in software form: <ul><li>Provider Packages SHOULD use a well-documented container image format (e.g., Docker, OCI).</li><li>The chosen package image format MAY expose configurable Provider properties as environment variables, unless otherwise indicated in the section below. Variables so exposed SHOULD be assigned default values in the image manifest.</li><li>A Provider Supervisor MAY programmatically evaluate or otherwise scan a Provider Package’s image manifest in order to discover configurable environment variables.</li><li>A Provider SHALL NOT assume that an operator or Provider Supervisor will scan an image manifest for environment variables.</li></ul></li></ul><h4 id="environment-variables" tabindex="-1">Environment Variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment Variables&quot;">​</a></h4><ul><li>Variables defined by this specification SHALL be identifiable by their <code>MC_</code> name prefix.</li><li>Configuration properties not defined by the MC specification SHALL NOT use the same <code>MC_</code> name prefix; this prefix is reserved for common configuration properties defined by the MC specification.</li><li>The Provider Supervisor SHOULD supply all RECOMMENDED MC environment variables to a Provider.</li><li>The Provider Supervisor SHALL supply all REQUIRED MC environment variables to a Provider.</li></ul><h5 id="logging" tabindex="-1">Logging <a class="header-anchor" href="#logging" aria-label="Permalink to &quot;Logging&quot;">​</a></h5><ul><li>Providers SHOULD generate log messages to ONLY standard output and/or standard error. <ul><li>In this case the Provider Supervisor SHALL assume responsibility for all log lifecycle management.</li></ul></li><li>Provider implementations that deviate from the above recommendation SHALL clearly and unambiguously document the following: <ul><li>Logging configuration flags and/or variables, including working sample configurations.</li><li>Default log destination(s) (where do the logs go if no configuration is specified?)</li><li>Log lifecycle management ownership and related guidance (size limits, rate limits, rolling, archiving, expunging, etc.) applicable to the logging mechanism embedded within the Provider.</li></ul></li><li>Providers SHOULD NOT write potentially sensitive data to logs (e.g. secrets).</li></ul><h5 id="available-services" tabindex="-1">Available Services <a class="header-anchor" href="#available-services" aria-label="Permalink to &quot;Available Services&quot;">​</a></h5><ul><li>Provider Packages MAY support all or a subset of CMI services; service combinations MAY be configurable at runtime by the Provider Supervisor. <ul><li>This specification does not dictate the mechanism by which mode of operation MUST be discovered, and instead places that burden upon the VM Provider.</li></ul></li><li>Misconfigured provider software SHOULD fail-fast with an OS-appropriate error code.</li></ul><h5 id="linux-capabilities" tabindex="-1">Linux Capabilities <a class="header-anchor" href="#linux-capabilities" aria-label="Permalink to &quot;Linux Capabilities&quot;">​</a></h5><ul><li>Providers SHOULD clearly document any additionally required capabilities and/or security context.</li></ul><h5 id="cgroup-isolation" tabindex="-1">Cgroup Isolation <a class="header-anchor" href="#cgroup-isolation" aria-label="Permalink to &quot;Cgroup Isolation&quot;">​</a></h5><ul><li>A Provider MAY be constrained by cgroups.</li></ul><h5 id="resource-requirements" tabindex="-1">Resource Requirements <a class="header-anchor" href="#resource-requirements" aria-label="Permalink to &quot;Resource Requirements&quot;">​</a></h5><ul><li>VM Providers SHOULD unambiguously document all of a Provider’s resource requirements.</li></ul><h3 id="deploying" tabindex="-1">Deploying <a class="header-anchor" href="#deploying" aria-label="Permalink to &quot;Deploying&quot;">​</a></h3><ul><li><strong>Recommended:</strong> The MCM and Provider are typically expected to run as two containers inside a common <code>Pod</code>.</li><li>However, for the security reasons they could execute on seperate Pods provided they have a secure way to exchange data between them.</li></ul>`,99)]))}const m=t(n,[["render",o]]);export{u as __pageData,m as default};

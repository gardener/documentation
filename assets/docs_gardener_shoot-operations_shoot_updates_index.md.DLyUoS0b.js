import{_ as a,c as i,o,a2 as s}from"./chunks/framework.Bfq10Vlj.js";const u=JSON.parse('{"title":"Shoot Updates and Upgrades","description":"","frontmatter":{"aliases":["/docs/gardener/shoot_updates/"],"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/usage/shoot-operations","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/shoot-operations/shoot_updates.md","to":"shoot_updates.md"},"persona":"Users","title":"Shoot Updates and Upgrades","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/shoot-operations/shoot_updates/index.md","filePath":"docs/gardener/shoot-operations/shoot_updates.md","lastUpdated":null}'),t={name:"docs/gardener/shoot-operations/shoot_updates/index.md"};function n(r,e,d,l,c,h){return o(),i("div",null,e[0]||(e[0]=[s(`<h1 id="shoot-updates-and-upgrades" tabindex="-1">Shoot Updates and Upgrades <a class="header-anchor" href="#shoot-updates-and-upgrades" aria-label="Permalink to &quot;Shoot Updates and Upgrades&quot;">​</a></h1><p>This document describes what happens during shoot updates (changes incorporated in a newly deployed Gardener version) and during shoot upgrades (changes for version controllable by end-users).</p><h2 id="updates" tabindex="-1">Updates <a class="header-anchor" href="#updates" aria-label="Permalink to &quot;Updates&quot;">​</a></h2><p>Updates to all aspects of the shoot cluster happen when the gardenlet reconciles the <code>Shoot</code> resource.</p><h3 id="when-are-reconciliations-triggered" tabindex="-1">When are Reconciliations Triggered <a class="header-anchor" href="#when-are-reconciliations-triggered" aria-label="Permalink to &quot;When are Reconciliations Triggered&quot;">​</a></h3><p>Generally, when you change the specification of your <code>Shoot</code> the reconciliation will start immediately, potentially updating your cluster. Please note that you can also confine the reconciliation triggered due to your specification updates to the cluster&#39;s maintenance time window. Please find more information in <a href="/docs/gardener/shoot/shoot_maintenance/#confine-specification-changesupdates-roll-out">Confine Specification Changes/Updates Roll Out</a>.</p><p>You can also annotate your shoot with special operation annotations (for more information, see <a href="/docs/gardener/shoot-operations/shoot_operations/">Trigger Shoot Operations</a>), which will cause the reconciliation to start due to your actions.</p><p>There is also an automatic reconciliation by Gardener. The period, i.e., how often it is performed, depends on the configuration of the Gardener administrators/operators. In some Gardener installations the operators might enable &quot;reconciliation in maintenance time window only&quot; (for more information, see <a href="/docs/gardener/shoot/shoot_maintenance/#cluster-reconciliation">Cluster Reconciliation</a>), which will result in at least one reconciliation during the time configured in the <code>Shoot</code>&#39;s <code>.spec.maintenance.timeWindow</code> field.</p><h3 id="which-updates-are-applied" tabindex="-1">Which Updates are Applied <a class="header-anchor" href="#which-updates-are-applied" aria-label="Permalink to &quot;Which Updates are Applied&quot;">​</a></h3><p>As end-users can only control the <code>Shoot</code> resource&#39;s specification but not the used Gardener version, they don&#39;t have any influence on which of the updates are rolled out (other than those settings configurable in the <code>Shoot</code>). A Gardener operator can deploy a new Gardener version at any point in time. Any subsequent reconciliation of <code>Shoot</code>s will update them by rolling out the changes incorporated in this new Gardener version.</p><p>Some examples for such shoot updates are:</p><ul><li>Add a new/remove an old component to/from the shoot&#39;s control plane running in the seed, or to/from the shoot&#39;s system components running on the worker nodes.</li><li>Change the configuration of an existing control plane/system component.</li><li>Restart of existing control plane/system components (this might result in a short unavailability of the Kubernetes API server, e.g., when etcd or a kube-apiserver itself is being restarted)</li></ul><h3 id="behavioural-changes" tabindex="-1">Behavioural Changes <a class="header-anchor" href="#behavioural-changes" aria-label="Permalink to &quot;Behavioural Changes&quot;">​</a></h3><p>Generally, some of such updates (e.g., configuration changes) could theoretically result in different behaviour of controllers. If such changes would be backwards-incompatible, then we usually follow one of those approaches (depends on the concrete change):</p><ul><li>Only apply the change for new clusters.</li><li>Expose a new field in the <code>Shoot</code> resource that lets users control this changed behaviour to enable it at a convenient point in time.</li><li>Put the change behind an alpha feature gate (disabled by default) in the gardenlet (only controllable by Gardener operators), which will be promoted to beta (enabled by default) in subsequent releases (in this case, end-users have no influence on when the behaviour changes - Gardener operators should inform their end-users and provide clear timelines when they will enable the feature gate).</li></ul><h2 id="upgrades" tabindex="-1">Upgrades <a class="header-anchor" href="#upgrades" aria-label="Permalink to &quot;Upgrades&quot;">​</a></h2><p>We consider shoot upgrades to change either the:</p><ul><li>Kubernetes version (<code>.spec.kubernetes.version</code>)</li><li>Kubernetes version of the worker pool if specified (<code>.spec.provider.workers[].kubernetes.version</code>)</li><li>Machine image version of at least one worker pool (<code>.spec.provider.workers[].machine.image.version</code>)</li></ul><p>Generally, an upgrade is also performed through a reconciliation of the <code>Shoot</code> resource, i.e., the same concepts as for <a href="/docs/gardener/shoot-operations/shoot_updates/#updates">shoot updates</a> apply. If an end-user triggers an upgrade (e.g., by changing the Kubernetes version) after a new Gardener version was deployed but before the shoot was reconciled again, then this upgrade might incorporate the changes delivered with this new Gardener version.</p><p>The <code>UpdateStrategy</code> field in the Shoot specification (<code>.spec.provider.workers[].updateStrategy</code>) gives users the flexibility to define how the <code>machine-controller-manager</code> handles worker pool updates during the upgrade process. Currently gardener support three update strategies:</p><ul><li><code>AutoRollingUpdate</code></li><li><code>AutoInPlaceUpdate</code></li><li><code>ManualInPlaceUpdate</code></li></ul><blockquote><p>⚠️ The above strategies generally require draining the node when changes are made to the <code>Shoot</code> specification. The specific changes that trigger this behavior will be discussed in later sections. For all other spec changes like Kubernetes patch version update, the upgrade is executed without draining the node and the shoot worker nodes remain unchanged. In case of Kubernetes patch version, only the <code>kubelet</code> process is restarted with the updated Kubernetes version binary.</p></blockquote><h3 id="rolling-updates" tabindex="-1">Rolling Updates <a class="header-anchor" href="#rolling-updates" aria-label="Permalink to &quot;Rolling Updates&quot;">​</a></h3><p>The upgrade is performed in a &quot;rolling update&quot; manner, during which nodes in the worker pool are replaced. Similar to how pods in Kubernetes are updated when backed by a <code>Deployment</code>. Worker nodes are terminated one after another and replaced by new nodes. The existing workload is gracefully drained and evicted from the old worker nodes to new worker nodes, respecting the configured <code>PodDisruptionBudget</code>s (see <a href="https://kubernetes.io/docs/tasks/run-application/configure-pdb/" target="_blank" rel="noreferrer">Specifying a Disruption Budget for your Application</a>).</p><h4 id="automatic-rolling-updates" tabindex="-1">Automatic Rolling Updates <a class="header-anchor" href="#automatic-rolling-updates" aria-label="Permalink to &quot;Automatic Rolling Updates&quot;">​</a></h4><p>When the auto rolling update strategy is selected, the update process is fully orchestrated by the Gardener and the <code>machine-controller-manager</code>. The <code>machine-controller-manager</code> sequentially terminates the worker nodes and replaces them with new nodes.</p><blockquote><p>ℹ️ This is the <code>default</code> update strategy.</p></blockquote><p>To create workers with <code>AutoRollingUpdate</code>, either omit the <code>Shoot&#39;</code>s <code>.spec.provider.workers[].updateStrategy</code> field (it will default to <code>AutoRollingUpdate</code>) or explicitly set the field to <code>AutoRollingUpdate</code>.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    workers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cpu-worker</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      maxSurge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      maxUnavailable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      updateStrategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">AutoRollingUpdate</span></span></code></pre></div><h4 id="customize-rolling-update-behaviour-of-shoot-worker-nodes" tabindex="-1">Customize Rolling Update Behaviour of Shoot Worker Nodes <a class="header-anchor" href="#customize-rolling-update-behaviour-of-shoot-worker-nodes" aria-label="Permalink to &quot;Customize Rolling Update Behaviour of Shoot Worker Nodes&quot;">​</a></h4><p>The <code>.spec.provider.workers[]</code> list exposes two fields that you might configure based on your workload&#39;s needs: <code>maxSurge</code> and <code>maxUnavailable</code>. The same concepts <a href="https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-update-deployment" target="_blank" rel="noreferrer">like in Kubernetes</a> apply. Additionally, you might customize how the <code>machine-controller-manager</code> is behaving. You can configure the following fields in <code>.spec.provider.worker[].machineControllerManager</code>:</p><ul><li><code>machineDrainTimeout</code>: Timeout (in duration) used while draining of machine before deletion, beyond which <code>machine-controller-manager</code> forcefully deletes the machine (default: <code>2h</code>).</li><li><code>machineHealthTimeout</code>: Timeout (in duration) used while re-joining (in case of temporary health issues) of a machine before it is declared as failed (default: <code>10m</code>).</li><li><code>machineCreationTimeout</code>: Timeout (in duration) used while joining (during creation) of a machine before it is declared as failed (default: <code>10m</code>).</li><li><code>maxEvictRetries</code>: Maximum number of times evicts would be attempted on a pod before it is forcibly deleted during the draining of a machine (default: <code>10</code>).</li><li><code>nodeConditions</code>: List of case-sensitive node-conditions which will change a machine to a <code>Failed</code> state after the <code>machineHealthTimeout</code> duration. It may further be replaced with a new machine if the machine is backed by a machine-set object (defaults: <code>KernelDeadlock</code>, <code>ReadonlyFilesystem</code> , <code>DiskPressure</code>).</li></ul><h4 id="rolling-update-triggers" tabindex="-1">Rolling Update Triggers <a class="header-anchor" href="#rolling-update-triggers" aria-label="Permalink to &quot;Rolling Update Triggers&quot;">​</a></h4><p>A rolling update of the shoot worker nodes is triggered for some changes to your worker pool specification (<code>.spec.provider.workers[]</code>, even if you don&#39;t change the Kubernetes or machine image version). The complete list of fields that trigger a rolling update:</p><ul><li><code>.spec.kubernetes.version</code> (except for patch version changes)</li><li><code>.spec.provider.workers[].machine.image.name</code></li><li><code>.spec.provider.workers[].machine.image.version</code></li><li><code>.spec.provider.workers[].machine.type</code></li><li><code>.spec.provider.workers[].volume.type</code></li><li><code>.spec.provider.workers[].volume.size</code></li><li><code>.spec.provider.workers[].providerConfig</code> (provider extension dependent with feature gate <code>NewWorkerPoolHash</code>)</li><li><code>.spec.provider.workers[].cri.name</code></li><li><code>.spec.provider.workers[].kubernetes.version</code> (except for patch version changes)</li><li><code>.spec.systemComponents.nodeLocalDNS.enabled</code> (for Kubernetes version &lt; 1.34 or if <code>kube-proxy</code> runs in <code>IPVS</code> mode)</li><li><code>.status.credentials.rotation.certificateAuthorities.lastInitiationTime</code> (changed by Gardener when a shoot CA rotation is initiated) when worker pool is not part of <code>.status.credentials.rotation.certificateAuthorities.pendingWorkersRollouts[]</code></li><li><code>.status.credentials.rotation.serviceAccountKey.lastInitiationTime</code> (changed by Gardener when a shoot service account signing key rotation is initiated) when worker pool is not part of <code>.status.credentials.rotation.serviceAccountKey.pendingWorkersRollouts[]</code></li></ul><p>If feature gate <code>NewWorkerPoolHash</code> is enabled:</p><ul><li><code>.spec.kubernetes.kubelet.kubeReserved</code> (unless a worker pool-specific value is set)</li><li><code>.spec.kubernetes.kubelet.systemReserved</code> (unless a worker pool-specific value is set)</li><li><code>.spec.kubernetes.kubelet.evictionHard</code> (unless a worker pool-specific value is set)</li><li><code>.spec.kubernetes.kubelet.cpuManagerPolicy</code> (unless a worker pool-specific value is set)</li><li><code>.spec.provider.workers[].kubernetes.kubelet.kubeReserved</code></li><li><code>.spec.provider.workers[].kubernetes.kubelet.systemReserved</code></li><li><code>.spec.provider.workers[].kubernetes.kubelet.evictionHard</code></li><li><code>.spec.provider.workers[].kubernetes.kubelet.cpuManagerPolicy</code></li></ul><p>Changes to <code>kubeReserved</code> or <code>systemReserved</code> do not trigger a node roll if their sum does not change.</p><p>Generally, the provider extension controllers might have additional constraints for changes leading to rolling updates, so please consult the respective documentation as well. In particular, if the feature gate <code>NewWorkerPoolHash</code> is enabled and a worker pool uses the new hash, then the <code>providerConfig</code> as a whole is not included. Instead only fields selected by the provider extension are considered.</p><h3 id="in-place-updates" tabindex="-1">In-Place Updates <a class="header-anchor" href="#in-place-updates" aria-label="Permalink to &quot;In-Place Updates&quot;">​</a></h3><p>For scenarios where users want to retain the current nodes and avoid deletion during updates, Gardener provides the option of <code>in-place</code> updates. The upgrade is performed without replacing the underlying machines. Although there is an exception where new nodes are created with the updated configuration and old ones are terminated. One such exception is discussed in <a href="/docs/gardener/shoot-operations/shoot_updates/#automatic-in-place-updates">Automatic inplace update</a> section.</p><p>The existing workload is gracefully drained and evicted from the worker nodes, respecting the configured <code>PodDisruptionBudget</code>s (see <a href="https://kubernetes.io/docs/tasks/run-application/configure-pdb/" target="_blank" rel="noreferrer">Specifying a Disruption Budget for your Application</a>).</p><blockquote><p>ℹ️ The <code>in-place</code> updates feature is controlled by the <code>InPlaceNodeUpdates</code> feature gate in the <code>gardener-apiserver</code>. This feature is currently in the <code>Alpha</code> stage and may introduce breaking changes in future releases. For more information, see <a href="/docs/gardener/deployment/feature_gates/">feature-usage</a>.</p></blockquote><p>For in-place updates, the first requirement is that the operating system must support them. For a specific machine image version, the configuration for in-place updates must be defined in the <code>CloudProfile</code> under <code>spec.machineImages[].versions[].inPlaceUpdates</code>:</p><ul><li>The <code>inPlaceUpdates.supported</code> field must be set to <code>true</code>.</li><li>The <code>inPlaceUpdates.minVersionForUpdate</code> field specifies the minimum version from which an in-place update to the target machine image version can be performed.</li></ul><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">machineImages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">- </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gardenlinux</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  versions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1632.0.0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    inPlaceUpdates</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      supported</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      minVersionForUpdate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1630.0.0</span></span></code></pre></div><p>The <code>inPlaceUpdates</code> field in the Shoot status provides details about in-place updates for the Shoot workers. It includes the <code>pendingWorkerUpdates</code> field, which lists the worker pools that are awaiting in-place updates.</p><h4 id="customize-in-place-update-behaviour-of-shoot-worker-nodes" tabindex="-1">Customize In-Place Update Behaviour of Shoot Worker Nodes <a class="header-anchor" href="#customize-in-place-update-behaviour-of-shoot-worker-nodes" aria-label="Permalink to &quot;Customize In-Place Update Behaviour of Shoot Worker Nodes&quot;">​</a></h4><p>In addition to customisable fields mentioned in <a href="/docs/gardener/shoot-operations/shoot_updates/#customize-rolling-update-behaviour-of-shoot-worker-nodes"></a> section, you can configure the following fields in <code>.spec.provider.worker[].machineControllerManager</code>:</p><ul><li><code>MachineInPlaceUpdateTimeout</code>: Timeout (in duration) after which an in-place update is declared as failed.</li><li><code>DisableHealthTimeout</code>: A boolean value that, when set to <code>true</code>, ignores the health timeout. As a result, machines are never marked as failed, and unhealthy machines are not deleted. The default value is <code>true</code> for in-place updates.</li></ul><h4 id="in-place-update-triggers" tabindex="-1">In-Place Update Triggers <a class="header-anchor" href="#in-place-update-triggers" aria-label="Permalink to &quot;In-Place Update Triggers&quot;">​</a></h4><p>An in-place update of the shoot worker nodes is triggered for rolling update triggers listed under <a href="/docs/gardener/shoot-operations/shoot_updates/#rolling-update-triggers">Rolling Update Triggers</a> except for the following:</p><ul><li><code>.spec.provider.workers[].machine.image.name</code></li><li><code>.spec.provider.workers[].machine.type</code></li><li><code>.spec.provider.workers[].volume.type</code></li><li><code>.spec.provider.workers[].volume.size</code></li><li><code>.spec.provider.workers[].cri.name</code></li><li><code>.spec.systemComponents.nodeLocalDNS.enabled</code> (for Kubernetes version &lt; 1.34 or if <code>kube-proxy</code> runs in <code>IPVS</code> mode)</li></ul><blockquote><p>There are validations which restricts changing the above mentioned exception fields when <code>in-place</code> updates strategy is configured.</p></blockquote><p>When a worker pool is undergoing an in-place update, applying subsequent updates to the same worker pool is restricted. If an in-place update fails and nodes are left in a problematic state, user intervention is required to manually fix the nodes. In cases where a subsequent update is necessary to resolve the issue, users can update the worker pool after adding the force update annotation <code>gardener.cloud/operation=force-in-place-update</code> on the Shoot. Refer to <a href="/docs/gardener/shoot-operations/shoot_operations/#force-update-a-worker-pool-with-inplace-update-strategy">Force-update a worker pool with InPlace update strategy</a> for more details.</p><blockquote><p>⚠️ Changing the update strategy from <code>AutoRollingUpdate</code> to <code>AutoInPlaceUpdate</code>/<code>ManualInPlaceUpdate</code> (and vice versa) is not allowed. However, switching between <code>AutoInPlaceUpdate</code> and <code>ManualInPlaceUpdate</code> is permitted.</p></blockquote><h4 id="automatic-in-place-updates" tabindex="-1">Automatic In-Place Updates <a class="header-anchor" href="#automatic-in-place-updates" aria-label="Permalink to &quot;Automatic In-Place Updates&quot;">​</a></h4><p>In case of AutoInPlaceUpdate update strategy, the update process is fully orchestrated by Gardener and the <code>machine-controller-manager</code>. No user intervention is required. Set <code>.spec.provider.workers[].updateStrategy</code> field in the <code>Shoot</code> spec to <code>AutoInPlaceUpdate</code>.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    workers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cpu-worker</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      maxSurge</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      maxUnavailable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      updateStrategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">AutoInPlaceUpdate</span></span></code></pre></div><p>During automatic in-place updates, if the <code>maxSurge</code> value is set to greater than 0, the <code>machine-controller-manager</code> creates new nodes equal to the <code>maxSurge</code> value. All old nodes, except for those equal to the <code>maxSurge</code> value, are updated in place, and the old nodes corresponding to the <code>maxSurge</code> value are terminated. If <code>maxSurge</code> is set to <code>0</code>, no new nodes are created and all old nodes are updated in-place.</p><p>The <code>inPlaceUpdates.pendingWorkerUpdates.autoInPlaceUpdate</code> field in the Shoot status lists the names of worker pools that are pending updates with this strategy.</p><h4 id="manual-in-place-updates" tabindex="-1">Manual In-Place Updates <a class="header-anchor" href="#manual-in-place-updates" aria-label="Permalink to &quot;Manual In-Place Updates&quot;">​</a></h4><p>The <code>ManualInPlaceUpdate</code> strategy allows users to control and orchestrate the update process manually. Set <code>.spec.provider.workers[].updateStrategy</code> field in the <code>Shoot</code> spec to <code>ManualInPlaceUpdate</code>. When doing this, do not set the <code>maxSurge</code> or <code>maxUnavailable</code> fields, as they do not apply to this update strategy.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  provider</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    workers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cpu-worker</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      updateStrategy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ManualInPlaceUpdate</span></span></code></pre></div><p>Once <code>machine-controller-manager</code> labels nodes with <code>node.machine.sapcloud.io/candidate-for-update</code>, user can select the candidate nodes for update by labeling them with <code>node.machine.sapcloud.io/selected-for-update=true</code>:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kubectl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">node-nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> node.machine.sapcloud.io/selected-for-update=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span></code></pre></div><p>The <code>ManualInPlaceWorkersUpdated</code> <a href="/docs/gardener/shoot/shoot_status/#constraints">constraint</a> in the shoot status indicates that at least one worker pool with the <code>ManualInPlaceUpdate</code> strategy is pending an update. Shoot reconciliation will still succeed even if there are worker pools pending updates.</p><p>The <code>inPlaceUpdates.pendingWorkerUpdates.manualInPlaceUpdate</code> field in the <code>Shoot</code> status lists the names of worker pools that are pending updates with this strategy.</p><h2 id="related-documentation" tabindex="-1">Related Documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to &quot;Related Documentation&quot;">​</a></h2><ul><li><a href="/docs/gardener/shoot-operations/shoot_operations/">Shoot Operations</a></li><li><a href="/docs/gardener/shoot/shoot_maintenance/">Shoot Maintenance</a></li><li><a href="/docs/gardener/shoot/shoot_maintenance/#confine-specification-changesupdates-roll-out">Confine Specification Changes/Updates Roll Out To Maintenance Time Window</a>.</li></ul>`,70)]))}const g=a(t,[["render",n]]);export{u as __pageData,g as default};

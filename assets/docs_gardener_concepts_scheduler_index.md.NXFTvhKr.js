import{_ as s,c as t,o as a,a2 as i}from"./chunks/framework.Bfq10Vlj.js";const u=JSON.parse('{"title":"Gardener Scheduler","description":"Understand the configuration and flow of the controller that assigns a seed cluster to newly created shoots","frontmatter":{"description":"Understand the configuration and flow of the controller that assigns a seed cluster to newly created shoots","github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/concepts","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/concepts/scheduler.md","to":"scheduler.md"},"title":"Gardener Scheduler","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/concepts/scheduler/index.md","filePath":"docs/gardener/concepts/scheduler.md","lastUpdated":null}'),o={name:"docs/gardener/concepts/scheduler/index.md"};function n(d,e,r,c,l,h){return a(),t("div",null,e[0]||(e[0]=[i(`<h1 id="gardener-scheduler" tabindex="-1">Gardener Scheduler <a class="header-anchor" href="#gardener-scheduler" aria-label="Permalink to &quot;Gardener Scheduler&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The Gardener Scheduler is in essence a controller that watches newly created shoots and assigns a seed cluster to them. Conceptually, the task of the Gardener Scheduler is very similar to the task of the Kubernetes Scheduler: finding a seed for a shoot instead of a node for a pod.</p><p>Either the scheduling strategy or the shoot cluster purpose hereby determines how the scheduler is operating. The following sections explain the configuration and flow in greater detail.</p><h2 id="why-is-the-gardener-scheduler-needed" tabindex="-1">Why Is the Gardener Scheduler Needed? <a class="header-anchor" href="#why-is-the-gardener-scheduler-needed" aria-label="Permalink to &quot;Why Is the Gardener Scheduler Needed?&quot;">​</a></h2><h3 id="_1-decoupling" tabindex="-1">1. Decoupling <a class="header-anchor" href="#_1-decoupling" aria-label="Permalink to &quot;1. Decoupling&quot;">​</a></h3><p>Previously, an admission plugin in the Gardener API server conducted the scheduling decisions. This implies changes to the API server whenever adjustments of the scheduling are needed. Decoupling the API server and the scheduler comes with greater flexibility to develop these components independently.</p><h3 id="_2-extensibility" tabindex="-1">2. Extensibility <a class="header-anchor" href="#_2-extensibility" aria-label="Permalink to &quot;2. Extensibility&quot;">​</a></h3><p>It should be possible to easily extend and tweak the scheduler in the future. Possibly, similar to the Kubernetes scheduler, hooks could be provided which influence the scheduling decisions. It should be also possible to completely replace the standard Gardener Scheduler with a custom implementation.</p><h2 id="algorithm-overview" tabindex="-1">Algorithm Overview <a class="header-anchor" href="#algorithm-overview" aria-label="Permalink to &quot;Algorithm Overview&quot;">​</a></h2><p>The following <strong>sequence</strong> describes the steps involved to determine a seed candidate:</p><ol><li>Determine usable seeds with &quot;usable&quot; defined as follows: <ul><li>no <code>.metadata.deletionTimestamp</code></li><li><code>.spec.settings.scheduling.visible</code> is <code>true</code></li><li><code>.status.lastOperation</code> is not <code>nil</code></li><li>conditions <code>GardenletReady</code>, <code>BackupBucketsReady</code> (if available) are <code>true</code></li></ul></li><li>Filter seeds: <ul><li>matching <code>.spec.seedSelector</code> in <code>CloudProfile</code> used by the <code>Shoot</code></li><li>matching <code>.spec.seedSelector</code> in <code>Shoot</code></li><li>having no network intersection with the <code>Shoot</code>&#39;s networks (due to the VPN connectivity between seeds and shoots their networks must be disjoint)</li><li>whose taints (<code>.spec.taints</code>) are tolerated by the <code>Shoot</code> (<code>.spec.tolerations</code>)</li><li>whose access restrictions (<code>.spec.accessRestrictions</code>) are supporting those configured in the <code>Shoot</code> (<code>.spec.accessRestrictions</code>)</li><li>whose capacity for shoots would not be exceeded if the shoot is scheduled onto the seed, see <a href="/docs/gardener/concepts/scheduler/#ensuring-seeds-capacity-for-shoots-is-not-exceeded">Ensuring seeds capacity for shoots is not exceeded</a></li><li>which have at least three zones in <code>.spec.provider.zones</code> if shoot requests a high available control plane with failure tolerance type <code>zone</code>.</li></ul></li><li>Apply active <a href="/docs/gardener/concepts/scheduler/#strategies">strategy</a> e.g., <em>Minimal Distance strategy</em></li><li>Choose least utilized seed, i.e., the one with the least number of shoot control planes, will be the winner and written to the <code>.spec.seedName</code> field of the <code>Shoot</code>.</li></ol><p>In order to put the scheduling decision into effect, the scheduler sends an update request for the <code>Shoot</code> resource to the API server. After validation, the <code>gardener-apiserver</code> updates the <code>Shoot</code> to have the <code>spec.seedName</code> field set. Subsequently, the <code>gardenlet</code> picks up and starts to create the cluster on the specified seed.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>The Gardener Scheduler configuration has to be supplied on startup. It is a mandatory and also the only available flag. <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardener-scheduler.yaml" target="_blank" rel="noreferrer">This yaml file</a> holds an example scheduler configuration.</p><p>Most of the configuration options are the same as in the Gardener Controller Manager (leader election, client connection, ...). However, the Gardener Scheduler on the other hand does not need a TLS configuration, because there are currently no webhooks configurable.</p><h2 id="strategies" tabindex="-1">Strategies <a class="header-anchor" href="#strategies" aria-label="Permalink to &quot;Strategies&quot;">​</a></h2><p>The scheduling strategy is defined in the <em><strong>candidateDeterminationStrategy</strong></em> of the scheduler&#39;s configuration and can have the possible values <code>SameRegion</code> and <code>MinimalDistance</code>. The <code>SameRegion</code> strategy is the default strategy.</p><h3 id="same-region-strategy" tabindex="-1">Same Region strategy <a class="header-anchor" href="#same-region-strategy" aria-label="Permalink to &quot;Same Region strategy&quot;">​</a></h3><p>The Gardener Scheduler reads the <code>spec.provider.type</code> and <code>.spec.region</code> fields from the <code>Shoot</code> resource. It tries to find a seed that has the identical <code>.spec.provider.type</code> and <code>.spec.provider.region</code> fields set. If it cannot find a suitable seed, it adds an event to the shoot stating that it is unschedulable.</p><h3 id="minimal-distance-strategy" tabindex="-1">Minimal Distance strategy <a class="header-anchor" href="#minimal-distance-strategy" aria-label="Permalink to &quot;Minimal Distance strategy&quot;">​</a></h3><p>The Gardener Scheduler tries to find a valid seed with minimal distance to the shoot&#39;s intended region. Distances are configured via <code>ConfigMap</code>(s), usually per cloud provider in a Gardener landscape. The configuration is structured like this:</p><ul><li>It refers to one or multiple <code>CloudProfile</code>s via annotation <code>scheduling.gardener.cloud/cloudprofiles</code>.</li><li>It contains the declaration as <code>region-config</code> via label <code>scheduling.gardener.cloud/purpose</code>.</li><li>If a <code>CloudProfile</code> is referred by multiple <code>ConfigMap</code>s, only the first one is considered.</li><li>The <code>data</code> fields configure actual distances, where <em>key</em> relates to the <code>Shoot</code> region and <em>value</em> contains distances to <code>Seed</code> regions.</li></ul><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">v1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ConfigMap</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;name&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">garden</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  annotations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    scheduling.gardener.cloud/cloudprofiles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cloudprofile-name-1{,optional-cloudprofile-name-2,...}</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  labels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    scheduling.gardener.cloud/purpose</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">region-config</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  region-1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    region-2: 10</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    region-3: 20</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    ...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  region-2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    region-1: 10</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    region-3: 10</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    ...</span></span></code></pre></div><blockquote><p>Gardener provider extensions for public cloud providers usually have an example weight <code>ConfigMap</code> in their repositories. We suggest to check them out before defining your own data.</p></blockquote><p>If a valid seed candidate cannot be found after consulting the distance configuration, the scheduler will fall back to the Levenshtein distance to find the closest region. Therefore, the region name is split into a base name and an orientation. Possible orientations are <code>north</code>, <code>south</code>, <code>east</code>, <code>west</code> and <code>central</code>. The distance then is twice the Levenshtein distance of the region&#39;s base name plus a correction value based on the orientation and the provider.</p><p>If the orientations of shoot and seed candidate match, the correction value is 0, if they differ it is 2 and if either the seed&#39;s or the shoot&#39;s region does not have an orientation it is 1. If the provider differs, the correction value is additionally incremented by 2.</p><p>Because of this, a matching region with a matching provider is always preferred.</p><h3 id="special-handling-based-on-shoot-cluster-purpose" tabindex="-1">Special handling based on shoot cluster purpose <a class="header-anchor" href="#special-handling-based-on-shoot-cluster-purpose" aria-label="Permalink to &quot;Special handling based on shoot cluster purpose&quot;">​</a></h3><p>Every shoot cluster can have a purpose that describes what the cluster is used for, and also influences how the cluster is setup (see <a href="/docs/gardener/shoot/shoot_purposes/">Shoot Cluster Purpose</a> for more information).</p><p>In case the shoot has the <code>testing</code> purpose, then the scheduler only reads the <code>.spec.provider.type</code> from the <code>Shoot</code> resource and tries to find a <code>Seed</code> that has the identical <code>.spec.provider.type</code>. The region does not matter, i.e., <code>testing</code> shoots may also be scheduled on a seed in a complete different region if it is better for balancing the whole Gardener system.</p><h2 id="shoots-binding-subresource" tabindex="-1"><code>shoots/binding</code> Subresource <a class="header-anchor" href="#shoots-binding-subresource" aria-label="Permalink to &quot;\`shoots/binding\` Subresource&quot;">​</a></h2><p>The <code>shoots/binding</code> subresource is used to bind a <code>Shoot</code> to a <code>Seed</code>. On creation of a shoot cluster/s, the scheduler updates the binding automatically if an appropriate seed cluster is available. Only an operator with the necessary RBAC can update this binding manually. This can be done by changing the <code>.spec.seedName</code> of the shoot. However, if a different seed is already assigned to the shoot, this will trigger a control-plane migration. For required steps, please see <a href="/docs/gardener/control_plane_migration/#triggering-the-migration">Triggering the Migration</a>.</p><h2 id="spec-schedulername-field-in-the-shoot-specification" tabindex="-1"><code>spec.schedulerName</code> Field in the <code>Shoot</code> Specification <a class="header-anchor" href="#spec-schedulername-field-in-the-shoot-specification" aria-label="Permalink to &quot;\`spec.schedulerName\` Field in the \`Shoot\` Specification&quot;">​</a></h2><p>Similar to the <code>spec.schedulerName</code> field in <code>Pod</code>s, the <code>Shoot</code> specification has an optional <code>.spec.schedulerName</code> field. If this field is set on creation, only the scheduler which relates to the configured name is responsible for scheduling the shoot. The <code>default-scheduler</code> name is reserved for the default scheduler of Gardener. Affected Shoots will remain in <code>Pending</code> state if the mentioned scheduler is not present in the landscape.</p><h2 id="spec-seedname-field-in-the-shoot-specification" tabindex="-1"><code>spec.seedName</code> Field in the <code>Shoot</code> Specification <a class="header-anchor" href="#spec-seedname-field-in-the-shoot-specification" aria-label="Permalink to &quot;\`spec.seedName\` Field in the \`Shoot\` Specification&quot;">​</a></h2><p>Similar to the <code>.spec.nodeName</code> field in <code>Pod</code>s, the <code>Shoot</code> specification has an optional <code>.spec.seedName</code> field. If this field is set on creation, the shoot will be scheduled to this seed. However, this field can only be set by users having RBAC for the <code>shoots/binding</code> subresource. If this field is not set, the <code>scheduler</code> will assign a suitable seed automatically and populate this field with the seed name.</p><h2 id="seedselector-field-in-the-shoot-specification" tabindex="-1"><code>seedSelector</code> Field in the <code>Shoot</code> Specification <a class="header-anchor" href="#seedselector-field-in-the-shoot-specification" aria-label="Permalink to &quot;\`seedSelector\` Field in the \`Shoot\` Specification&quot;">​</a></h2><p>Similar to the <code>.spec.nodeSelector</code> field in <code>Pod</code>s, the <code>Shoot</code> specification has an optional <code>.spec.seedSelector</code> field. It allows the user to provide a label selector that must match the labels of the <code>Seed</code>s in order to be scheduled to one of them. The labels on the <code>Seed</code>s are usually controlled by Gardener administrators/operators - end users cannot add arbitrary labels themselves. If provided, the Gardener Scheduler will only consider as &quot;suitable&quot; those seeds whose labels match those provided in the <code>.spec.seedSelector</code> of the <code>Shoot</code>.</p><p>By default, only seeds with the same provider as the shoot are selected. By adding a <code>providerTypes</code> field to the <code>seedSelector</code>, a dedicated set of possible providers (<code>*</code> means all provider types) can be selected.</p><h2 id="ensuring-a-seed-s-capacity-for-shoots-is-not-exceeded" tabindex="-1">Ensuring a Seed&#39;s Capacity for Shoots Is Not Exceeded <a class="header-anchor" href="#ensuring-a-seed-s-capacity-for-shoots-is-not-exceeded" aria-label="Permalink to &quot;Ensuring a Seed&#39;s Capacity for Shoots Is Not Exceeded&quot;">​</a></h2><p>Seeds have a practical limit of how many shoots they can accommodate. Exceeding this limit is undesirable, as the system performance will be noticeably impacted. Therefore, the scheduler ensures that a seed&#39;s capacity for shoots is not exceeded by taking into account a maximum number of shoots that can be scheduled onto a seed.</p><p>This mechanism works as follows:</p><ul><li>The <code>gardenlet</code> is configured with certain <em>resources</em> and their total <em>capacity</em> (and, for certain resources, the amount <em>reserved</em> for Gardener), see <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardenlet.yaml" target="_blank" rel="noreferrer">/example/20-componentconfig-gardenlet.yaml</a>. Currently, the only such resource is the maximum number of shoots that can be scheduled onto a seed.</li><li>The <code>gardenlet</code> seed controller updates the <code>capacity</code> and <code>allocatable</code> fields in the Seed status with the capacity of each resource and how much of it is actually available to be consumed by shoots. The <code>allocatable</code> value of a resource is equal to <code>capacity</code> minus <code>reserved</code>.</li><li>When scheduling shoots, the scheduler filters out all candidate seeds whose allocatable capacity for shoots would be exceeded if the shoot is scheduled onto the seed.</li></ul><h2 id="failure-to-determine-a-suitable-seed" tabindex="-1">Failure to Determine a Suitable Seed <a class="header-anchor" href="#failure-to-determine-a-suitable-seed" aria-label="Permalink to &quot;Failure to Determine a Suitable Seed&quot;">​</a></h2><p>In case the scheduler fails to find a suitable seed, the operation is being retried with exponential backoff. The reason for the failure will be reported in the <code>Shoot</code>&#39;s <code>.status.lastOperation</code> field as well as a Kubernetes event (which can be retrieved via <code>kubectl -n &lt;namespace&gt; describe shoot &lt;shoot-name&gt;</code>).</p><h2 id="current-limitation-future-plans" tabindex="-1">Current Limitation / Future Plans <a class="header-anchor" href="#current-limitation-future-plans" aria-label="Permalink to &quot;Current Limitation / Future Plans&quot;">​</a></h2><ul><li>Azure unfortunately has a geographically non-hierarchical naming pattern and does not start with the continent. This is the reason why we will exchange the implementation of the <code>MinimalDistance</code> strategy with a more suitable one in the future.</li></ul>`,48)]))}const g=s(o,[["render",n]]);export{u as __pageData,g as default};

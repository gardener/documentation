import{_ as o,c as t,o as r,a2 as a}from"./chunks/framework.Bfq10Vlj.js";const g=JSON.parse('{"title":"Gardener Controller Manager","description":"Understand where the gardener-controller-manager runs and its functionalities","frontmatter":{"description":"Understand where the gardener-controller-manager runs and its functionalities","github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/concepts","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/concepts/controller-manager.md","to":"controller-manager.md"},"title":"Gardener Controller Manager","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/concepts/controller-manager/index.md","filePath":"docs/gardener/concepts/controller-manager.md","lastUpdated":null}'),n={name:"docs/gardener/concepts/controller-manager/index.md"};function c(i,e,s,l,d,h){return r(),t("div",null,e[0]||(e[0]=[a('<h1 id="gardener-controller-manager" tabindex="-1">Gardener Controller Manager <a class="header-anchor" href="#gardener-controller-manager" aria-label="Permalink to &quot;Gardener Controller Manager&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The <code>gardener-controller-manager</code> (often referred to as &quot;GCM&quot;) is a component that runs next to the Gardener API server, similar to the Kubernetes Controller Manager. It runs several controllers that do not require talking to any seed or shoot cluster. Also, as of today, it exposes an HTTP server that is serving several health check endpoints and metrics.</p><p>This document explains the various functionalities of the <code>gardener-controller-manager</code> and their purpose.</p><h2 id="controllers" tabindex="-1">Controllers <a class="header-anchor" href="#controllers" aria-label="Permalink to &quot;Controllers&quot;">​</a></h2><h3 id="bastion-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/bastion" target="_blank" rel="noreferrer"><code>Bastion</code> Controller</a> <a class="header-anchor" href="#bastion-controller" aria-label="Permalink to &quot;[`Bastion` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/bastion)&quot;">​</a></h3><p><code>Bastion</code> resources have a limited lifetime which can be extended up to a certain amount by performing a heartbeat on them. The <code>Bastion</code> controller is responsible for deleting expired or rotten <code>Bastion</code>s.</p><ul><li>&quot;expired&quot; means a <code>Bastion</code> has exceeded its <code>status.expirationTimestamp</code>.</li><li>&quot;rotten&quot; means a <code>Bastion</code> is older than the configured <code>maxLifetime</code>.</li></ul><p>The <code>maxLifetime</code> defaults to 24 hours and is an option in the <code>BastionControllerConfiguration</code> which is part of <code>gardener-controller-manager</code>s <code>ControllerManagerControllerConfiguration</code>, see <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardener-controller-manager.yaml" target="_blank" rel="noreferrer">the example config file</a> for details.</p><p>The controller also deletes <code>Bastion</code>s in case the referenced <code>Shoot</code>:</p><ul><li>no longer exists</li><li>is marked for deletion (i.e., have a non-<code>nil</code> <code>.metadata.deletionTimestamp</code>)</li><li>was migrated to another seed (i.e., <code>Shoot.spec.seedName</code> is different than <code>Bastion.spec.seedName</code>).</li></ul><p>The deletion of <code>Bastion</code>s triggers the <code>gardenlet</code> to perform the necessary cleanups in the Seed cluster, so some time can pass between deletion and the <code>Bastion</code> actually disappearing. Clients like <code>gardenctl</code> are advised to not re-use <code>Bastion</code>s whose deletion timestamp has been set already.</p><p>Refer to <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/15-manage-bastions-and-ssh-key-pair-rotation.md" target="_blank" rel="noreferrer">GEP-15</a> for more information on the lifecycle of <code>Bastion</code> resources.</p><h3 id="certificatesigningrequest-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/certificatesigningrequest" target="_blank" rel="noreferrer"><code>CertificateSigningRequest</code> Controller</a> <a class="header-anchor" href="#certificatesigningrequest-controller" aria-label="Permalink to &quot;[`CertificateSigningRequest` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/certificatesigningrequest)&quot;">​</a></h3><p>After the <a href="/docs/gardener/concepts/gardenlet/">gardenlet</a> gets deployed on the Seed cluster, it needs to establish itself as a trusted party to communicate with the Gardener API server. It runs through a bootstrap flow similar to the <a href="https://kubernetes.io/docs/reference/access-authn-authz/kubelet-tls-bootstrapping/" target="_blank" rel="noreferrer">kubelet bootstrap</a> process.</p><p>On startup, the gardenlet uses a <code>kubeconfig</code> with a <a href="https://kubernetes.io/docs/reference/access-authn-authz/bootstrap-tokens/" target="_blank" rel="noreferrer">bootstrap token</a> which authenticates it as being part of the <code>system:bootstrappers</code> group. This kubeconfig is used to create a <a href="https://kubernetes.io/docs/reference/access-authn-authz/certificate-signing-requests/" target="_blank" rel="noreferrer"><code>CertificateSigningRequest</code></a> (CSR) against the Gardener API server.</p><p>The controller in <code>gardener-controller-manager</code> checks whether the <code>CertificateSigningRequest</code> has the expected organization, common name and usages which the gardenlet would request.</p><p>The controller auto-approves CSRs based on the type of gardenlet deployment and validates the appropriate permissions:</p><h4 id="seed-client-certificates-seedclient-subresource" tabindex="-1">Seed Client Certificates (<code>seedclient</code> subresource) <a class="header-anchor" href="#seed-client-certificates-seedclient-subresource" aria-label="Permalink to &quot;Seed Client Certificates (`seedclient` subresource)&quot;">​</a></h4><p>For regular gardenlets managing seed clusters, the controller auto-approves CSRs when:</p><ol><li>The CSR matches the requirements for a seed client certificate (correct organization, common name, and usages)</li><li>The requesting client has permission to create the <code>certificatesigningrequests/seedclient</code> subresource</li></ol><h4 id="self-hosted-shoot-client-certificates-shootclient-subresource" tabindex="-1">Self-Hosted Shoot Client Certificates (<code>shootclient</code> subresource) <a class="header-anchor" href="#self-hosted-shoot-client-certificates-shootclient-subresource" aria-label="Permalink to &quot;Self-Hosted Shoot Client Certificates (`shootclient` subresource)&quot;">​</a></h4><p>For gardenlets responsible for self-hosted shoots, the controller performs additional security validation:</p><ol><li>The CSR must match the requirements for a shoot client certificate (correct organization, common name, and usages)</li><li>The requesting client must have permission to create the <code>certificatesigningrequests/shootclient</code> subresource</li><li><strong>Bootstrap Token Validation</strong>: The CSR must be requested via a bootstrap token with a specific description format containing the shoot&#39;s namespace and name</li><li><strong>Subject Matching</strong>: The certificate request&#39;s subject must match the shoot metadata extracted from the bootstrap token description</li></ol><h4 id="permission-model" tabindex="-1">Permission Model <a class="header-anchor" href="#permission-model" aria-label="Permalink to &quot;Permission Model&quot;">​</a></h4><p>Both types of CSRs rely on the same RBAC setup:</p><ul><li>Clients in the <code>system:bootstrappers</code> group are bound to the <code>gardener.cloud:system:seed-bootstrapper</code> <code>ClusterRole</code></li><li>This ClusterRole grants permission to create both <code>certificatesigningrequests/seedclient</code> and <code>certificatesigningrequests/shootclient</code> subresources</li><li>The gardenlet&#39;s bootstrap kubeconfig contains a bootstrap token that authenticates it as part of the <code>system:bootstrappers</code> group</li></ul><p>This dual approval mechanism ensures that both regular seed gardenlets and self-hosted shoot gardenlets can obtain client certificates while maintaining appropriate security controls for each deployment model.</p><h3 id="cloudprofile-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/cloudprofile" target="_blank" rel="noreferrer"><code>CloudProfile</code> Controller</a> <a class="header-anchor" href="#cloudprofile-controller" aria-label="Permalink to &quot;[`CloudProfile` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/cloudprofile)&quot;">​</a></h3><p><code>CloudProfile</code>s are essential when it comes to reconciling <code>Shoot</code>s since they contain constraints (like valid machine types, Kubernetes versions, or machine images) and sometimes also some global configuration for the respective environment (typically via provider-specific configuration in <code>.spec.providerConfig</code>).</p><p>Consequently, to ensure that <code>CloudProfile</code>s in-use are always present in the system until the last referring <code>Shoot</code> or <code>NamespacedCloudProfile</code> gets deleted, the controller adds a finalizer which is only released when there is no <code>Shoot</code> or <code>NamespacedCloudProfile</code> referencing the <code>CloudProfile</code> anymore.</p><h3 id="namespacedcloudprofile-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/namespacedcloudprofile" target="_blank" rel="noreferrer"><code>NamespacedCloudProfile</code> Controller</a> <a class="header-anchor" href="#namespacedcloudprofile-controller" aria-label="Permalink to &quot;[`NamespacedCloudProfile` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/namespacedcloudprofile)&quot;">​</a></h3><p><code>NamespacedCloudProfile</code>s provide a project-scoped extension to <code>CloudProfile</code>s, allowing for adjustments of a parent <code>CloudProfile</code> (e.g. by overriding expiration dates of Kubernetes versions or machine images). This allows for modifications without global project visibility. Like <code>CloudProfile</code>s do in their spec, <code>NamespacedCloudProfile</code>s also expose the resulting <code>Shoot</code> constraints as a <code>CloudProfileSpec</code> in their status.</p><p>The controller ensures that <code>NamespacedCloudProfile</code>s in-use remain present in the system until the last referring <code>Shoot</code> is deleted by adding a finalizer that is only released when there is no <code>Shoot</code> referencing the <code>NamespacedCloudProfile</code> anymore.</p><h3 id="controllerdeployment-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerdeployment" target="_blank" rel="noreferrer"><code>ControllerDeployment</code> Controller</a> <a class="header-anchor" href="#controllerdeployment-controller" aria-label="Permalink to &quot;[`ControllerDeployment` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerdeployment)&quot;">​</a></h3><p>Extensions are registered in the garden cluster via <code>ControllerRegistration</code> and deployment of respective extensions are specified via <code>ControllerDeployment</code>. For more info refer to <a href="/docs/gardener/extensions/registration/">Registering Extension Controllers</a>.</p><p>This controller ensures that <code>ControllerDeployment</code> in-use always exists until the last <code>ControllerRegistration</code> referencing them gets deleted. The controller adds a finalizer which is only released when there is no <code>ControllerRegistration</code> referencing the <code>ControllerDeployment</code> anymore.</p><h3 id="controllerregistration-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration" target="_blank" rel="noreferrer"><code>ControllerRegistration</code> Controller</a> <a class="header-anchor" href="#controllerregistration-controller" aria-label="Permalink to &quot;[`ControllerRegistration` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration)&quot;">​</a></h3><p>The <code>ControllerRegistration</code> controller makes sure that the required <a href="/docs/gardener/#extensions">Gardener Extensions</a> specified by the <a href="/docs/gardener/extensions/registration/#controllerregistrations"><code>ControllerRegistration</code></a> resources are present in the seed clusters. It also takes care of the creation and deletion of <code>ControllerInstallation</code> objects for a given seed cluster. The controller has three reconciliation loops.</p><h4 id="main-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/seed" target="_blank" rel="noreferrer">&quot;Main&quot; Reconciler</a> <a class="header-anchor" href="#main-reconciler" aria-label="Permalink to &quot;[&quot;Main&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/seed)&quot;">​</a></h4><p>This reconciliation loop watches the <code>Seed</code> objects and determines which <code>ControllerRegistration</code>s are required for them and reconciles the corresponding <code>ControllerInstallation</code> resources to reach the determined state. To begin with, it computes the kind/type combinations of extensions required for the seed. For this, the controller examines a live list of <code>ControllerRegistration</code>s, <code>ControllerInstallation</code>s, <code>BackupBucket</code>s, <code>BackupEntry</code>s, <code>Shoot</code>s, and <code>Secret</code>s from the garden cluster. For example, it examines the shoots running on the seed and deducts the kind/type, like <code>Infrastructure/gcp</code>. The seed (<code>seed.spec.provider.type</code>) and DNS (<code>seed.spec.dns.provider.type</code>) provider types are considered when calculating the list of required <code>ControllerRegistration</code>s, as well. It also decides whether they should always be deployed based on the <code>.spec.deployment.policy</code>. For the configuration options, please see this <a href="/docs/gardener/extensions/registration/#deployment-configuration-options">section</a>.</p><p>Based on these required combinations, each of them are mapped to <code>ControllerRegistration</code> objects and then to their corresponding <code>ControllerInstallation</code> objects (if existing). The controller then creates or updates the required <code>ControllerInstallation</code> objects for the given seed. It also deletes every existing <code>ControllerInstallation</code> whose referenced <code>ControllerRegistration</code> is not part of the required list. For example, if the shoots in the seed are no longer using the DNS provider <code>aws-route53</code>, then the controller proceeds to delete the respective <code>ControllerInstallation</code> object.</p><h4 id="controllerregistration-finalizer-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/controllerregistrationfinalizer" target="_blank" rel="noreferrer">&quot;<code>ControllerRegistration</code> Finalizer&quot; Reconciler</a> <a class="header-anchor" href="#controllerregistration-finalizer-reconciler" aria-label="Permalink to &quot;[&quot;`ControllerRegistration` Finalizer&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/controllerregistrationfinalizer)&quot;">​</a></h4><p>This reconciliation loop watches the <code>ControllerRegistration</code> resource and adds finalizers to it when they are created. In case a deletion request comes in for the resource, i.e., if a <code>.metadata.deletionTimestamp</code> is set, it actively scans for a <code>ControllerInstallation</code> resource using this <code>ControllerRegistration</code>, and decides whether the deletion can be allowed. In case no related <code>ControllerInstallation</code> is present, it removes the finalizer and marks it for deletion.</p><h4 id="seed-finalizer-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/seedfinalizer" target="_blank" rel="noreferrer">&quot;<code>Seed</code> Finalizer&quot; Reconciler</a> <a class="header-anchor" href="#seed-finalizer-reconciler" aria-label="Permalink to &quot;[&quot;`Seed` Finalizer&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/seedfinalizer)&quot;">​</a></h4><p>This loop also watches the <code>Seed</code> object and adds finalizers to it at creation. If a <code>.metadata.deletionTimestamp</code> is set for the seed, then the controller checks for existing <code>ControllerInstallation</code> objects which reference this seed. If no such objects exist, then it removes the finalizer and allows the deletion.</p><h4 id="extension-clusterrole-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/extensionclusterrole" target="_blank" rel="noreferrer">&quot;Extension <code>ClusterRole</code>&quot; Reconciler</a> <a class="header-anchor" href="#extension-clusterrole-reconciler" aria-label="Permalink to &quot;[&quot;Extension `ClusterRole`&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/controllerregistration/extensionclusterrole)&quot;">​</a></h4><p>This reconciler watches two resources in the garden cluster:</p><ul><li><code>ClusterRole</code>s labelled with <code>authorization.gardener.cloud/custom-extensions-permissions=true</code></li><li><code>ServiceAccount</code>s in seed namespaces matching the selector provided via the <code>authorization.gardener.cloud/extensions-serviceaccount-selector</code> annotation of such <code>ClusterRole</code>s.</li></ul><p>Its core task is to maintain a <code>ClusterRoleBinding</code> resource referencing the respective <code>ClusterRole</code>. This gets bound to all <code>ServiceAccount</code>s in seed namespaces whose labels match the selector provided via the <code>authorization.gardener.cloud/extensions-serviceaccount-selector</code> annotation of such <code>ClusterRole</code>s.</p><p>You can read more about the purpose of this reconciler in <a href="/docs/gardener/extensions/garden-api-access/#additional-permissions">this document</a>.</p><h3 id="credentialsbinding-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/credentialsbinding" target="_blank" rel="noreferrer"><code>CredentialsBinding</code> Controller</a> <a class="header-anchor" href="#credentialsbinding-controller" aria-label="Permalink to &quot;[`CredentialsBinding` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/credentialsbinding)&quot;">​</a></h3><p><code>CredentialsBinding</code>s reference <code>Secret</code>s, <code>WorkloadIdentity</code>s and <code>Quota</code>s and are themselves referenced by <code>Shoot</code>s.</p><p>The controller adds finalizers to the referenced objects to ensure they don&#39;t get deleted while still being referenced. Similarly, to ensure that <code>CredentialsBinding</code>s in-use are always present in the system until the last referring <code>Shoot</code> gets deleted, the controller adds a finalizer which is only released when there is no <code>Shoot</code> referencing the <code>CredentialsBinding</code> anymore.</p><p>Referenced <code>Secret</code>s and <code>WorkloadIdentity</code>s will also be labeled with <code>provider.shoot.gardener.cloud/&lt;type&gt;=true</code>, where <code>&lt;type&gt;</code> is the value of the <code>.provider.type</code> of the <code>CredentialsBinding</code>. Also, all referenced <code>Secret</code>s and <code>WorkloadIdentity</code>s, as well as <code>Quota</code>s, will be labeled with <code>reference.gardener.cloud/credentialsbinding=true</code> to allow for easily filtering for objects referenced by <code>CredentialsBinding</code>s.</p><h3 id="event-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/event" target="_blank" rel="noreferrer"><code>Event</code> Controller</a> <a class="header-anchor" href="#event-controller" aria-label="Permalink to &quot;[`Event` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/event)&quot;">​</a></h3><p>With the Gardener Event Controller, you can prolong the lifespan of events related to Shoot clusters. This is an optional controller which will become active once you provide the below mentioned configuration.</p><p>All events in K8s are deleted after a configurable time-to-live (controlled via a <a href="https://kubernetes.io/docs/reference/command-line-tools-reference/kube-apiserver/" target="_blank" rel="noreferrer">kube-apiserver argument</a> called <code>--event-ttl</code> (defaulting to 1 hour)). The need to prolong the time-to-live for Shoot cluster events frequently arises when debugging customer issues on live systems. This controller leaves events involving Shoots untouched, while deleting all other events after a configured time. In order to activate it, provide the following configuration:</p><ul><li><code>concurrentSyncs</code>: The amount of goroutines scheduled for reconciling events.</li><li><code>ttlNonShootEvents</code>: When an event reaches this time-to-live it gets deleted unless it is a Shoot-related event (defaults to <code>1h</code>, equivalent to the <code>event-ttl</code> default).</li></ul><blockquote><p>⚠️ In addition, you should also configure the <code>--event-ttl</code> for the kube-apiserver to define an upper-limit of how long Shoot-related events should be stored. The <code>--event-ttl</code> should be larger than the <code>ttlNonShootEvents</code> or this controller will have no effect.</p></blockquote><h3 id="exposureclass-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/exposureclass" target="_blank" rel="noreferrer"><code>ExposureClass</code> Controller</a> <a class="header-anchor" href="#exposureclass-controller" aria-label="Permalink to &quot;[`ExposureClass` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/exposureclass)&quot;">​</a></h3><p><code>ExposureClass</code> abstracts the ability to expose a Shoot clusters control plane in certain network environments (e.g. corporate networks, DMZ, internet) on all Seeds or a subset of the Seeds. For more information, see <a href="/docs/gardener/networking/exposureclasses/">ExposureClasses</a>.</p><p>Consequently, to ensure that <code>ExposureClass</code>es in-use are always present in the system until the last referring <code>Shoot</code> gets deleted, the controller adds a finalizer which is only released when there is no <code>Shoot</code> referencing the <code>ExposureClass</code> anymore.</p><h3 id="gardenlet-lifecycle-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/gardenletlifecycle" target="_blank" rel="noreferrer">Gardenlet Lifecycle Controller</a> <a class="header-anchor" href="#gardenlet-lifecycle-controller" aria-label="Permalink to &quot;[Gardenlet Lifecycle Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/gardenletlifecycle)&quot;">​</a></h3><p>The &quot;Lifecycle&quot; reconciler processes <code>Seed</code> and self-hosted <code>Shoot</code> objects which are enqueued every 10 seconds in order to check if the responsible <code>gardenlet</code> is still responding and operable. Therefore, it checks renewals via <code>Lease</code> objects in the garden cluster which are renewed regularly by the <code>gardenlet</code>.</p><p>In case a <code>Lease</code> is not renewed for the configured amount in <code>config.controllers.seed.monitorPeriod.duration</code>:</p><ol><li>The reconciler assumes that the <code>gardenlet</code> stopped operating and updates the <code>GardenletReady</code> condition to <code>Unknown</code>.</li><li>Additionally, the conditions and constraints of all affected <code>Shoot</code> resources are set to <code>Unknown</code> as well, because a striking <code>gardenlet</code> won&#39;t be able to maintain these conditions anymore.</li><li>For <code>Seed</code>s: If the gardenlet&#39;s client certificate has expired (identified based on the <code>.status.clientCertificateExpirationTimestamp</code> field in the <code>Seed</code> resource) and if it is managed by a <code>ManagedSeed</code>, then this will be triggered for a reconciliation. This will trigger the bootstrapping process again and allows gardenlets to obtain a fresh client certificate.</li></ol><h3 id="managedseedset-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/managedseedset" target="_blank" rel="noreferrer"><code>ManagedSeedSet</code> Controller</a> <a class="header-anchor" href="#managedseedset-controller" aria-label="Permalink to &quot;[`ManagedSeedSet` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/managedseedset)&quot;">​</a></h3><p><code>ManagedSeedSet</code> objects maintain a stable set of replicas of <code>ManagedSeed</code>s, i.e. they guarantee the availability of a specified number of identical <code>ManagedSeed</code>s on an equal number of identical <code>Shoot</code>s. The <code>ManagedSeedSet</code> controller creates and deletes <code>ManagedSeed</code>s and <code>Shoot</code>s in response to changes to the replicas and selector fields. For more information, refer to the <a href="https://github.com/gardener/gardener/blob/master/docs/proposals/13-automated-seed-management.md#managedseedsets" target="_blank" rel="noreferrer"><code>ManagedSeedSet</code> proposal document</a>.</p><ol><li>The reconciler first gets all the replicas of the given <code>ManagedSeedSet</code> in the <code>ManagedSeedSet</code>&#39;s namespace and with the matching selector. Each replica is a struct that contains a <code>ManagedSeed</code>, its corresponding <code>Seed</code> and <code>Shoot</code> objects.</li><li>Then the pending replica is retrieved, if it exists.</li><li>Next it determines the ready, postponed, and deletable replicas. <ul><li>A replica is considered <code>ready</code> when a <code>Seed</code> owned by a <code>ManagedSeed</code> has been registered either directly or by deploying <code>gardenlet</code> into a <code>Shoot</code>, the <code>Seed</code> is <code>Ready</code> and the <code>Shoot</code>&#39;s status is <code>Healthy</code>.</li><li>If a replica is not ready and it is not pending, i.e. it is not specified in the <code>ManagedSeed</code>&#39;s <code>status.pendingReplica</code> field, then it is added to the <code>postponed</code> replicas.</li><li>A replica is deletable if it has no scheduled <code>Shoot</code>s and the replica&#39;s <code>Shoot</code> and <code>ManagedSeed</code> do not have the <code>seedmanagement.gardener.cloud/protect-from-deletion</code> annotation.</li></ul></li><li>Finally, it checks the actual and target replica counts. If the actual count is less than the target count, the controller scales up the replicas by creating new replicas to match the desired target count. If the actual count is more than the target, the controller deletes replicas to match the desired count. Before scale-out or scale-in, the controller first reconciles the pending replica (there can always only be one) and makes sure the replica is ready before moving on to the next one. <ul><li><code>Scale-out</code>(actual count &lt; target count) <ul><li>During the scale-out phase, the controller first creates the <code>Shoot</code> object from the <code>ManagedSeedSet</code>&#39;s <code>spec.shootTemplate</code> field and adds the replica to the <code>status.pendingReplica</code> of the <code>ManagedSeedSet</code>.</li><li>For the subsequent reconciliation steps, the controller makes sure that the pending replica is ready before proceeding to the next replica. Once the <code>Shoot</code> is created successfully, the <code>ManagedSeed</code> object is created from the <code>ManagedSeedSet</code>&#39;s <code>spec.template</code>. The <code>ManagedSeed</code> object is reconciled by the <code>ManagedSeed</code> controller and a <code>Seed</code> object is created for the replica. Once the replica&#39;s <code>Seed</code> becomes ready and the <code>Shoot</code> becomes healthy, the replica also becomes ready.</li></ul></li><li><code>Scale-in</code>(actual count &gt; target count) <ul><li>During the scale-in phase, the controller first determines the replica that can be deleted. From the deletable replicas, it chooses the one with the lowest priority and deletes it. Priority is determined in the following order: <ul><li>First, compare replica statuses. Replicas with &quot;less advanced&quot; status are considered lower priority. For example, a replica with <code>StatusShootReconciling</code> status has a lower value than a replica with <code>StatusShootReconciled</code> status. Hence, in this case, a replica with a <code>StatusShootReconciling</code> status will have lower priority and will be considered for deletion.</li><li>Then, the replicas are compared with the readiness of their <code>Seed</code>s. Replicas with non-ready <code>Seed</code>s are considered lower priority.</li><li>Then, the replicas are compared with the health statuses of their <code>Shoot</code>s. Replicas with &quot;worse&quot; statuses are considered lower priority.</li><li>Finally, the replica ordinals are compared. Replicas with lower ordinals are considered lower priority.</li></ul></li></ul></li></ul></li></ol><h3 id="quota-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/quota" target="_blank" rel="noreferrer"><code>Quota</code> Controller</a> <a class="header-anchor" href="#quota-controller" aria-label="Permalink to &quot;[`Quota` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/quota)&quot;">​</a></h3><p><code>Quota</code> object limits the resources consumed by shoot clusters either per provider secret or per project/namespace.</p><p>Consequently, to ensure that <code>Quota</code>s in-use are always present in the system until the last <code>SecretBinding</code> or <code>CredentialsBinding</code> that references them gets deleted, the controller adds a finalizer which is only released when there is no <code>SecretBinding</code> or <code>CredentialsBinding</code> referencing the <code>Quota</code> anymore.</p><h3 id="project-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project" target="_blank" rel="noreferrer"><code>Project</code> Controller</a> <a class="header-anchor" href="#project-controller" aria-label="Permalink to &quot;[`Project` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project)&quot;">​</a></h3><p>There are multiple controllers responsible for different aspects of <code>Project</code> objects. Please also refer to the <a href="/docs/gardener/project/projects/"><code>Project</code> documentation</a>.</p><h4 id="main-reconciler-1" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/project" target="_blank" rel="noreferrer">&quot;Main&quot; Reconciler</a> <a class="header-anchor" href="#main-reconciler-1" aria-label="Permalink to &quot;[&quot;Main&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/project)&quot;">​</a></h4><p>This reconciler manages a dedicated <code>Namespace</code> for each <code>Project</code>. The namespace name can either be specified explicitly in <code>.spec.namespace</code> (must be prefixed with <code>garden-</code>) or it will be determined by the controller. If <code>.spec.namespace</code> is set, it tries to create it. If it already exists, it tries to adopt it. This will only succeed if the <code>Namespace</code> was previously labeled with <code>gardener.cloud/role=project</code> and <code>project.gardener.cloud/name=&lt;project-name&gt;</code>. This is to prevent end-users from being able to adopt arbitrary namespaces and escalate their privileges, e.g. the <code>kube-system</code> namespace.</p><p>After the namespace was created/adopted, the controller creates several <code>ClusterRole</code>s and <code>ClusterRoleBinding</code>s that allow the project members to access related resources based on their roles. These RBAC resources are prefixed with <code>gardener.cloud:system:project{-member,-viewer}:&lt;project-name&gt;</code>. Gardener administrators and extension developers can define their own roles. For more information, see <a href="/docs/gardener/extensions/project-roles/">Extending Project Roles</a> for more information.</p><p>In addition, operators can configure the Project controller to maintain a default <a href="https://kubernetes.io/docs/concepts/policy/resource-quotas/" target="_blank" rel="noreferrer">ResourceQuota</a> for project namespaces. Quotas can especially limit the creation of user facing resources, e.g. <code>Shoots</code>, <code>SecretBindings</code>, <code>CredentialsBinding</code>, <code>Secrets</code> and thus protect the garden cluster from massive resource exhaustion but also enable operators to align quotas with respective enterprise policies.</p><blockquote><p>⚠️ <strong>Gardener itself is not exempted from configured quotas</strong>. For example, Gardener creates <code>Secrets</code> for every shoot cluster in the project namespace and at the same time increases the available quota count. Please mind this additional resource consumption.</p></blockquote><p>The controller configuration provides a template section <code>controllers.project.quotas</code> where such a ResourceQuota (see the example below) can be deposited.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">controllers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    quotas</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">v1</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ResourceQuota</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          hard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            count/shoots.core.gardener.cloud</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;100&quot;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            count/secretbindings.core.gardener.cloud</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;10&quot;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            count/credentialsbindings.security.gardener.cloud</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;10&quot;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            count/secrets</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;800&quot;</span></span>\n<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      projectSelector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}</span></span></code></pre></div><p>The Project controller takes the specified <code>config</code> and creates a <code>ResourceQuota</code> with the name <code>gardener</code> in the project namespace. If a <code>ResourceQuota</code> resource with the name <code>gardener</code> already exists, the controller will only update fields in <code>spec.hard</code> which are <strong>unavailable</strong> at that time. This is done to configure a default <code>Quota</code> in all projects but to allow manual quota increases as the projects&#39; demands increase. <code>spec.hard</code> fields in the <code>ResourceQuota</code> object that are not present in the configuration are removed from the object. Labels and annotations on the <code>ResourceQuota</code> <code>config</code> get merged with the respective fields on existing <code>ResourceQuota</code>s. An optional <code>projectSelector</code> narrows down the amount of projects that are equipped with the given <code>config</code>. If multiple configs match for a project, then only the first match in the list is applied to the project namespace.</p><p>The <code>.status.phase</code> of the <code>Project</code> resources is set to <code>Ready</code> or <code>Failed</code> by the reconciler to indicate whether the reconciliation loop was performed successfully. Also, it generates <code>Event</code>s to provide further information about its operations.</p><p>When a <code>Project</code> is marked for deletion, the controller ensures that there are no <code>Shoots</code> left in the project namespace. Once all <code>Shoots</code> are gone, the <code>Namespace</code> and <code>Project</code> are released.</p><h4 id="stale-projects-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/stale" target="_blank" rel="noreferrer">&quot;Stale Projects&quot; Reconciler</a> <a class="header-anchor" href="#stale-projects-reconciler" aria-label="Permalink to &quot;[&quot;Stale Projects&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/stale)&quot;">​</a></h4><p>As Gardener is a large-scale Kubernetes as a Service, it is designed for being used by a large amount of end-users. Over time, it is likely to happen that some of the hundreds or thousands of <code>Project</code> resources are no longer actively used.</p><p>Gardener offers the &quot;stale projects&quot; reconciler which will take care of identifying such stale projects, marking them with a &quot;warning&quot;, and eventually deleting them after a certain time period. This reconciler is enabled by default and works as follows:</p><ol><li>Projects are considered as &quot;stale&quot;/not actively used when all of the following conditions apply: The namespace associated with the <code>Project</code> does not have any... <ol><li><code>Shoot</code> resources.</li><li><code>BackupEntry</code> resources.</li><li><code>Secret</code> resources that are referenced by a <code>SecretBinding</code> or a <code>CredentialsBinding</code> that is in use by a <code>Shoot</code> (not necessarily in the same namespace).</li><li><code>WorkloadIdentity</code> resources that are referenced by a <code>CredentialsBinding</code> that is in use by a <code>Shoot</code> (not necessarily in the same namespace).</li><li><code>Quota</code> resources that are referenced by a <code>SecretBinding</code> or a <code>CredentialsBinding</code> that is in use by a <code>Shoot</code> (not necessarily in the same namespace).</li><li>The time period when the project was used for the last time (<code>status.lastActivityTimestamp</code>) is longer than the configured <code>minimumLifetimeDays</code></li></ol></li></ol><p>If a project is considered &quot;stale&quot;, then its <code>.status.staleSinceTimestamp</code> will be set to the time when it was first detected to be stale. If it gets actively used again, this timestamp will be removed. After some time, the <code>.status.staleAutoDeleteTimestamp</code> will be set to a timestamp after which Gardener will auto-delete the <code>Project</code> resource if it still is not actively used.</p><p>The component configuration of the <code>gardener-controller-manager</code> offers to configure the following options:</p><ul><li><code>minimumLifetimeDays</code>: Don&#39;t consider newly created <code>Project</code>s as &quot;stale&quot; too early to give people/end-users some time to onboard and get familiar with the system. The &quot;stale project&quot; reconciler won&#39;t set any timestamp for <code>Project</code>s younger than <code>minimumLifetimeDays</code>. When you change this value, then projects marked as &quot;stale&quot; may be no longer marked as &quot;stale&quot; in case they are young enough, or vice versa.</li><li><code>staleGracePeriodDays</code>: Don&#39;t compute auto-delete timestamps for stale <code>Project</code>s that are unused for less than <code>staleGracePeriodDays</code>. This is to not unnecessarily make people/end-users nervous &quot;just because&quot; they haven&#39;t actively used their <code>Project</code> for a given amount of time. When you change this value, then already assigned auto-delete timestamps may be removed if the new grace period is not yet exceeded.</li><li><code>staleExpirationTimeDays</code>: Expiration time after which stale <code>Project</code>s are finally auto-deleted (after <code>.status.staleSinceTimestamp</code>). If this value is changed and an auto-delete timestamp got already assigned to the projects, then the new value will only take effect if it&#39;s increased. Hence, decreasing the <code>staleExpirationTimeDays</code> will not decrease already assigned auto-delete timestamps.</li></ul><blockquote><p>Gardener administrators/operators can exclude specific <code>Project</code>s from the stale check by annotating the related <code>Namespace</code> resource with <code>project.gardener.cloud/skip-stale-check=true</code>.</p></blockquote><h4 id="activity-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/activity" target="_blank" rel="noreferrer">&quot;Activity&quot; Reconciler</a> <a class="header-anchor" href="#activity-reconciler" aria-label="Permalink to &quot;[&quot;Activity&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/project/activity)&quot;">​</a></h4><p>Since the other two reconcilers are unable to actively monitor the relevant objects that are used in a <code>Project</code> (<code>Shoot</code>, <code>Secret</code>, etc.), there could be a situation where the user creates and deletes objects in a short period of time. In that case, the <code>Stale Project Reconciler</code> could not see that there was any activity on that project and it will still mark it as a <code>Stale</code>, even though it is actively used.</p><p>The <code>Project Activity Reconciler</code> is implemented to take care of such cases. An event handler will notify the reconciler for any activity and then it will update the <code>status.lastActivityTimestamp</code>. This update will also trigger the <code>Stale Project Reconciler</code>.</p><h3 id="secretbinding-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/secretbinding" target="_blank" rel="noreferrer"><code>SecretBinding</code> Controller</a> <a class="header-anchor" href="#secretbinding-controller" aria-label="Permalink to &quot;[`SecretBinding` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/secretbinding)&quot;">​</a></h3><p><code>SecretBinding</code>s reference <code>Secret</code>s and <code>Quota</code>s and are themselves referenced by <code>Shoot</code>s. The controller adds finalizers to the referenced objects to ensure they don&#39;t get deleted while still being referenced. Similarly, to ensure that <code>SecretBinding</code>s in-use are always present in the system until the last referring <code>Shoot</code> gets deleted, the controller adds a finalizer which is only released when there is no <code>Shoot</code> referencing the <code>SecretBinding</code> anymore.</p><p>Referenced <code>Secret</code>s will also be labeled with <code>provider.shoot.gardener.cloud/&lt;type&gt;=true</code>, where <code>&lt;type&gt;</code> is the value of the <code>.provider.type</code> of the <code>SecretBinding</code>. Also, all referenced <code>Secret</code>s, as well as <code>Quota</code>s, will be labeled with <code>reference.gardener.cloud/secretbinding=true</code> to allow for easily filtering for objects referenced by <code>SecretBinding</code>s.</p><h3 id="seed-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed" target="_blank" rel="noreferrer"><code>Seed</code> Controller</a> <a class="header-anchor" href="#seed-controller" aria-label="Permalink to &quot;[`Seed` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed)&quot;">​</a></h3><p>The Seed controller in the <code>gardener-controller-manager</code> reconciles <code>Seed</code> objects with the help of the following reconcilers.</p><h4 id="main-reconciler-2" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/secrets" target="_blank" rel="noreferrer">&quot;Main&quot; Reconciler</a> <a class="header-anchor" href="#main-reconciler-2" aria-label="Permalink to &quot;[&quot;Main&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/secrets)&quot;">​</a></h4><p>This reconciliation loop takes care of seed related operations in the garden cluster. When a new <code>Seed</code> object is created, the reconciler creates a new <code>Namespace</code> in the garden cluster <code>seed-&lt;seed-name&gt;</code>. <code>Namespaces</code> dedicated to single seed clusters allow us to segregate access permissions i.e., a <code>gardenlet</code> must not have permissions to access objects in all <code>Namespaces</code> in the garden cluster. There are objects in a Garden environment which are created once by the operator e.g., default domain secret, alerting credentials, and are required for operations happening in the <code>gardenlet</code>. Therefore, we not only need a seed specific <code>Namespace</code> but also a copy of these &quot;shared&quot; objects.</p><p>The &quot;main&quot; reconciler takes care about this replication:</p><table tabindex="0"><thead><tr><th>Kind</th><th>Namespace</th><th>Label Selector</th></tr></thead><tbody><tr><td>Secret</td><td>garden</td><td>gardener.cloud/role</td></tr></tbody></table><h4 id="backup-buckets-check-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/backupbucketscheck" target="_blank" rel="noreferrer">&quot;Backup Buckets Check&quot; Reconciler</a> <a class="header-anchor" href="#backup-buckets-check-reconciler" aria-label="Permalink to &quot;[&quot;Backup Buckets Check&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/backupbucketscheck)&quot;">​</a></h4><p>Every time a <code>BackupBucket</code> object is created or updated, the referenced <code>Seed</code> object is enqueued for reconciliation. It&#39;s the reconciler&#39;s task to check the <code>status</code> subresource of all existing <code>BackupBucket</code>s that reference this <code>Seed</code>. If at least one <code>BackupBucket</code> has <code>.status.lastError != nil</code>, the <code>BackupBucketsReady</code> condition on the <code>Seed</code> will be set to <code>False</code>, and consequently the <code>Seed</code> is considered as <code>NotReady</code>. If the <code>SeedBackupBucketsCheckControllerConfiguration</code> (which is part of <code>gardener-controller-manager</code>s component configuration) contains a <code>conditionThreshold</code> for the <code>BackupBucketsReady</code>, the condition will instead first be set to <code>Progressing</code> and eventually to <code>False</code> once the <code>conditionThreshold</code> expires. See <a href="https://github.com/gardener/gardener/blob/master/example/20-componentconfig-gardener-controller-manager.yaml" target="_blank" rel="noreferrer">the example config file</a> for details. Once the <code>BackupBucket</code> is healthy again, the seed will be re-queued and the condition will turn <code>true</code>.</p><h4 id="extensions-check-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/extensionscheck" target="_blank" rel="noreferrer">&quot;Extensions Check&quot; Reconciler</a> <a class="header-anchor" href="#extensions-check-reconciler" aria-label="Permalink to &quot;[&quot;Extensions Check&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/extensionscheck)&quot;">​</a></h4><p>This reconciler reconciles <code>Seed</code> objects and checks whether all <code>ControllerInstallation</code>s referencing them are in a healthy state. Concretely, all three conditions <code>Valid</code>, <code>Installed</code>, and <code>Healthy</code> must have status <code>True</code> and the <code>Progressing</code> condition must have status <code>False</code>. Based on this check, it maintains the <code>ExtensionsReady</code> condition in the respective <code>Seed</code>&#39;s <code>.status.conditions</code> list.</p><h4 id="reference-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/reference" target="_blank" rel="noreferrer">&quot;Reference&quot; Reconciler</a> <a class="header-anchor" href="#reference-reconciler" aria-label="Permalink to &quot;[&quot;Reference&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/seed/reference)&quot;">​</a></h4><p>Seed objects may specify references to other objects in the <code>garden</code> namespace in the garden cluster which are required for certain features. For example, operators can configure additional data for extensions via <code>.spec.resources[]</code>. Such objects need a special protection against deletion requests as long as they are still being referenced by one or multiple seeds.</p><p>Therefore, this reconciler checks <code>Seed</code>s for referenced objects and adds the finalizer <code>gardener.cloud/reference-protection</code> to their <code>.metadata.finalizers</code> list. The reconciled <code>Seed</code> also gets this finalizer to enable a proper garbage collection in case the <code>gardener-controller-manager</code> is offline at the moment of an incoming deletion request. When an object is not actively referenced anymore because the <code>Seed</code> specification has changed or all related seeds were deleted (are in deletion), the controller will remove the added finalizer again so that the object can safely be deleted or garbage collected.</p><p>This reconciler inspects the following references:</p><ul><li><code>Secret</code>s and <code>ConfigMap</code>s from <code>.spec.resources[]</code></li></ul><p>The checks naturally grow with the number of references that are added to the <code>Seed</code> specification.</p><h3 id="shoot-controller" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot" target="_blank" rel="noreferrer"><code>Shoot</code> Controller</a> <a class="header-anchor" href="#shoot-controller" aria-label="Permalink to &quot;[`Shoot` Controller](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot)&quot;">​</a></h3><h4 id="conditions-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/conditions" target="_blank" rel="noreferrer">&quot;Conditions&quot; Reconciler</a> <a class="header-anchor" href="#conditions-reconciler" aria-label="Permalink to &quot;[&quot;Conditions&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/conditions)&quot;">​</a></h4><p>In case the reconciled <code>Shoot</code> is registered via a <code>ManagedSeed</code> as a seed cluster, this reconciler merges the conditions in the respective <code>Seed</code>&#39;s <code>.status.conditions</code> into the <code>.status.conditions</code> of the <code>Shoot</code>. This is to provide a holistic view on the status of the registered seed cluster by just looking at the <code>Shoot</code> resource.</p><h4 id="hibernation-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/hibernation" target="_blank" rel="noreferrer">&quot;Hibernation&quot; Reconciler</a> <a class="header-anchor" href="#hibernation-reconciler" aria-label="Permalink to &quot;[&quot;Hibernation&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/hibernation)&quot;">​</a></h4><p>This reconciler is responsible for hibernating or awakening shoot clusters based on the schedules defined in their <code>.spec.hibernation.schedules</code>. It ignores <a href="/docs/gardener/shoot/shoot_status/#last-operation">failed <code>Shoot</code>s</a> and those marked for deletion.</p><h4 id="maintenance-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/maintenance" target="_blank" rel="noreferrer">&quot;Maintenance&quot; Reconciler</a> <a class="header-anchor" href="#maintenance-reconciler" aria-label="Permalink to &quot;[&quot;Maintenance&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/maintenance)&quot;">​</a></h4><p>This reconciler is responsible for maintaining shoot clusters based on the time window defined in their <code>.spec.maintenance.timeWindow</code>. It might auto-update the Kubernetes version or the operating system versions specified in the worker pools (<code>.spec.provider.workers</code>). It could also add some operation or task annotations. For more information, see <a href="/docs/gardener/shoot/shoot_maintenance/">Shoot Maintenance</a>.</p><h4 id="quota-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/quota" target="_blank" rel="noreferrer">&quot;Quota&quot; Reconciler</a> <a class="header-anchor" href="#quota-reconciler" aria-label="Permalink to &quot;[&quot;Quota&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/quota)&quot;">​</a></h4><p>This reconciler might auto-delete shoot clusters in case their referenced <code>SecretBinding</code> or <code>CredentialsBinding</code> is itself referencing a <code>Quota</code> with <code>.spec.clusterLifetimeDays != nil</code>. If the shoot cluster is older than the configured lifetime, then it gets deleted. It maintains the expiration time of the <code>Shoot</code> in the value of the <code>shoot.gardener.cloud/expiration-timestamp</code> annotation. This annotation might be overridden, however only by at most twice the value of the <code>.spec.clusterLifetimeDays</code>.</p><h4 id="reference-reconciler-1" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/reference" target="_blank" rel="noreferrer">&quot;Reference&quot; Reconciler</a> <a class="header-anchor" href="#reference-reconciler-1" aria-label="Permalink to &quot;[&quot;Reference&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/reference)&quot;">​</a></h4><p>Shoot objects may specify references to other objects in the garden cluster which are required for certain features. For example, users can configure various DNS providers via <code>.spec.dns.providers</code> and usually need to refer to a corresponding <code>Secret</code> with valid DNS provider credentials inside. Such objects need a special protection against deletion requests as long as they are still being referenced by one or multiple shoots.</p><p>Therefore, this reconciler checks <code>Shoot</code>s for referenced objects and adds the finalizer <code>gardener.cloud/reference-protection</code> to their <code>.metadata.finalizers</code> list. The reconciled <code>Shoot</code> also gets this finalizer to enable a proper garbage collection in case the <code>gardener-controller-manager</code> is offline at the moment of an incoming deletion request. When an object is not actively referenced anymore because the <code>Shoot</code> specification has changed or all related shoots were deleted (are in deletion), the controller will remove the added finalizer again so that the object can safely be deleted or garbage collected.</p><p>This reconciler inspects the following references:</p><ul><li>Admission plugin kubeconfig <code>Secret</code>s (<code>.spec.kubernetes.kubeAPIServer.admissionPlugins[].kubeconfigSecretName</code>)</li><li>Audit policy <code>ConfigMap</code>s (<code>.spec.kubernetes.kubeAPIServer.auditConfig.auditPolicy.configMapRef</code>)</li><li>DNS provider <code>Secret</code>s (<code>.spec.dns.providers[].secretName</code>)</li><li>Structured authentication <code>ConfigMap</code>s (<code>.spec.kubernetes.kubeAPIServer.structuredAuthentication.configMapName</code>)</li><li>Structured authorization <code>ConfigMap</code>s (<code>.spec.kubernetes.kubeAPIServer.structuredAuthorization.configMapName</code>)</li><li>Structured authorization kubeconfig <code>Secret</code>s (<code>.spec.kubernetes.kubeAPIServer.structuredAuthorization.kubeconfigs[].secretName</code>)</li><li><code>Secret</code>s and <code>ConfigMap</code>s from <code>.spec.resources[]</code></li></ul><p>The checks naturally grow with the number of references that are added to the <code>Shoot</code> specification.</p><h4 id="retry-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/retry" target="_blank" rel="noreferrer">&quot;Retry&quot; Reconciler</a> <a class="header-anchor" href="#retry-reconciler" aria-label="Permalink to &quot;[&quot;Retry&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/retry)&quot;">​</a></h4><p>This reconciler is responsible for retrying certain failed <code>Shoot</code>s. Currently, the reconciler retries only failed <code>Shoot</code>s with an error code <code>ERR_INFRA_RATE_LIMITS_EXCEEDED</code>. See <a href="/docs/gardener/shoot/shoot_status/#error-codes">Shoot Status</a> for more details.</p><h4 id="status-label-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/statuslabel" target="_blank" rel="noreferrer">&quot;Status Label&quot; Reconciler</a> <a class="header-anchor" href="#status-label-reconciler" aria-label="Permalink to &quot;[&quot;Status Label&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/statuslabel)&quot;">​</a></h4><p>This reconciler is responsible for maintaining the <code>shoot.gardener.cloud/status</code> label on <code>Shoot</code>s. See <a href="/docs/gardener/shoot/shoot_status/#status-label">Shoot Status</a> for more details.</p><h4 id="migration-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/migration" target="_blank" rel="noreferrer">&quot;Migration&quot; Reconciler</a> <a class="header-anchor" href="#migration-reconciler" aria-label="Permalink to &quot;[&quot;Migration&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shoot/migration)&quot;">​</a></h4><p>This reconciler is triggered for <code>Shoot</code>s currently in migration (i.e., <code>.spec.seedName != .status.seedName</code>). It maintains the <code>ReadyForMigration</code> constraint in the <code>.status.constraints[]</code> list. A <code>Shoot</code> is considered ready for migration if the destination <code>Seed</code> is up-to-date and healthy.</p><p>The main purpose of this constraint is to allow the <code>gardenlet</code> running in the source seed cluster to check if it can start with the migration flow without that it needs to directly read the destination <code>Seed</code> resource (for which it won&#39;t have permissions).</p><h4 id="shootstate-finalizer-reconciler" tabindex="-1"><a href="https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shootstate" target="_blank" rel="noreferrer">&quot;ShootState Finalizer&quot; Reconciler</a> <a class="header-anchor" href="#shootstate-finalizer-reconciler" aria-label="Permalink to &quot;[&quot;ShootState Finalizer&quot; Reconciler](https://github.com/gardener/gardener/tree/master/pkg/controllermanager/controller/shootstate)&quot;">​</a></h4><p>This reconciler is responsible for managing a finalizer (<code>core.gardener.cloud/shootstate</code>) on a <code>ShootState</code>. The finalizer ensures the <code>ShootState</code> will exist during migration of <code>Shoot</code>&#39;s control plane to another <code>Seed</code>.</p><p>The <code>ShootState</code> has to be present until the <code>Migrate</code> and <code>Restore</code> operations finish successfully. Otherwise, in corner cases of prior deletion, subsequent <code>Restore</code> operations of the <code>Shoot</code> will fail due to the missing <code>ShootState</code> resource.</p>',140)]))}const p=o(n,[["render",c]]);export{g as __pageData,p as default};

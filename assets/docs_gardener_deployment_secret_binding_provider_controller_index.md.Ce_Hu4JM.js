import{_ as r,c as o,o as t,a2 as i}from"./chunks/framework.Bfq10Vlj.js";const g=JSON.parse('{"title":"Secret Binding Provider Controller","description":"","frontmatter":{"github_repo":"https://github.com/gardener/gardener","github_subdir":"docs/deployment","params":{"github_branch":"master"},"path_base_for_github_subdir":{"from":"content/docs/gardener/deployment/secret_binding_provider_controller.md","to":"secret_binding_provider_controller.md"},"title":"Secret Binding Provider Controller","prev":false,"next":false},"headers":[],"relativePath":"docs/gardener/deployment/secret_binding_provider_controller/index.md","filePath":"docs/gardener/deployment/secret_binding_provider_controller.md","lastUpdated":null}'),n={name:"docs/gardener/deployment/secret_binding_provider_controller/index.md"};function d(a,e,l,c,s,p){return t(),o("div",null,e[0]||(e[0]=[i('<h1 id="secretbinding-provider-controller" tabindex="-1">SecretBinding Provider Controller <a class="header-anchor" href="#secretbinding-provider-controller" aria-label="Permalink to &quot;SecretBinding Provider Controller&quot;">​</a></h1><p>This page describes the process on how to enable the SecretBinding provider controller.</p><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>With Gardener v1.38.0, the SecretBinding resource now contains a new optional field <code>.provider.type</code> (details about the motivation can be found in <a href="https://github.com/gardener/gardener/issues/4888" target="_blank" rel="noreferrer">https://github.com/gardener/gardener/issues/4888</a>). To make the process of setting the new field automated and afterwards to enforce validation on the new field in backwards compatible manner, Gardener features the SecretBinding provider controller and a feature gate - <code>SecretBindingProviderValidation</code>.</p><h2 id="process" tabindex="-1">Process <a class="header-anchor" href="#process" aria-label="Permalink to &quot;Process&quot;">​</a></h2><p>A Gardener landscape operator can follow the following steps:</p><ol><li><p>Enable the SecretBinding provider controller of Gardener Controller Manager.</p><p>The SecretBinding provider controller is responsible for populating the <code>.provider.type</code> field of a SecretBinding based on its current usage by Shoot resources. For example, if a Shoot <code>crazy-botany</code> with <code>.provider.type=aws</code> is using a SecretBinding <code>my-secret-binding</code>, then the SecretBinding provider controller will take care to set the <code>.provider.type</code> field of the SecretBinding to the same provider type (<code>aws</code>). To enable the SecretBinding provider controller, set the <code>controller.secretBindingProvider.concurrentSyncs</code> field in the ControllerManagerConfiguration (e.g set it to <code>5</code>). Although that it is not recommended, the API allows Shoots from different provider types to reference the same SecretBinding (assuming that the backing Secret contains data for both of the provider types). To preserve the backwards compatibility for such SecretBindings, the provider controller will maintain the multiple provider types in the field (it will join them with the separator <code>,</code> - for example <code>aws,gcp</code>).</p></li><li><p>Disable the SecretBinding provider controller and enable the <code>SecretBindingProviderValidation</code> feature gate of Gardener API server.</p><p>The <code>SecretBindingProviderValidation</code> feature gate of Gardener API server enables a set of validations for the SecretBinding provider field. It forbids creating a Shoot that has a different provider type from the referenced SecretBinding&#39;s one. It also enforces immutability on the field. After making sure that SecretBinding provider controller is enabled and it populated the <code>.provider.type</code> field of a majority of the SecretBindings on a Gardener landscape (the SecretBindings that are unused will have their provider type unset), a Gardener landscape operator has to disable the SecretBinding provider controller and to enable the <code>SecretBindingProviderValidation</code> feature gate of Gardener API server. To disable the SecretBinding provider controller, set the <code>controller.secretBindingProvider.concurrentSyncs</code> field in the ControllerManagerConfiguration to <code>0</code>.</p></li></ol><h2 id="implementation-history" tabindex="-1">Implementation History <a class="header-anchor" href="#implementation-history" aria-label="Permalink to &quot;Implementation History&quot;">​</a></h2><ul><li>Gardener v1.38: The SecretBinding resource has a new optional field <code>.provider.type</code>. The SecretBinding provider controller is disabled by default. The <code>SecretBindingProviderValidation</code> feature gate of Gardener API server is disabled by default.</li><li>Gardener v1.42: The SecretBinding provider controller is enabled by default.</li><li>Gardener v1.51: The <code>SecretBindingProviderValidation</code> feature gate of Gardener API server is enabled by default and the SecretBinding provider controller is disabled by default.</li><li>Gardener v1.53: The <code>SecretBindingProviderValidation</code> feature gate of Gardener API server is unconditionally enabled (can no longer be disabled).</li><li>Gardener v1.55: The <code>SecretBindingProviderValidation</code> feature gate of Gardener API server and the SecretBinding provider controller are removed.</li></ul>',9)]))}const f=r(n,[["render",d]]);export{g as __pageData,f as default};

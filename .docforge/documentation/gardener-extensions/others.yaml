{{ $org := .org}}
structure:
- name: gardener-extension-shoot-dns-service
  properties:
    frontmatter:
      title: DNS services
      description: Gardener extension controller for DNS services for shoot clusters
  nodes:
  - nodesSelector:
      path: https://github.com/{{$org}}/gardener-extension-shoot-dns-service/blob/DEFAULT_BRANCH/.docforge/manifest.yaml
- name: gardener-extension-shoot-cert-service
  properties:
    frontmatter:
      title: Certificate services 
      description: Gardener extension controller for certificate services for shoot clusters
  nodes:
  - nodesSelector:
      path: https://github.com/{{$org}}/gardener-extension-shoot-cert-service/blob/DEFAULT_BRANCH/.docforge/manifest.yaml
